import { SCHEMA_VERSION } from './constants.js';

const baseEnvelopeProperties = {
  schema_version: { type: 'string' },
  command: { type: 'string' },
  status: { enum: ['ok', 'error'] },
  observed_at: { type: 'string' },
  data: { type: 'object' },
  warnings: { type: 'array' },
  errors: { type: 'array' },
  artifacts: { type: 'array' }
};

const artifactProperties = {
  schema_version: { type: 'string' },
  type: { type: 'string' },
  path: { type: 'string' },
  description: { type: 'string' }
};

const schemas = Object.freeze({
  envelope: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/envelope.schema.json',
    title: 'TraceCue Envelope',
    type: 'object',
    required: ['schema_version', 'command', 'status', 'observed_at', 'data', 'warnings', 'errors', 'artifacts'],
    properties: baseEnvelopeProperties,
    additionalProperties: true
  }),
  artifact: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/artifact.schema.json',
    title: 'TraceCue Artifact',
    type: 'object',
    required: ['schema_version', 'type', 'path', 'description'],
    properties: artifactProperties,
    additionalProperties: true
  }),
  identity_audit: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/identity-audit.schema.json',
    title: 'TraceCue Identity Audit',
    type: 'object',
    required: ['schema_version', 'identity', 'repository', 'compatibility', 'package', 'readiness', 'warnings'],
    properties: {
      schema_version: { type: 'string' },
      identity: { type: 'object' },
      repository: { type: 'object' },
      compatibility: { type: 'object' },
      package: { type: 'object' },
      readiness: { type: 'object' },
      warnings: { type: 'array' }
    },
    additionalProperties: true
  }),
  language_settings: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/language-settings.schema.json',
    title: 'TraceCue Language Settings',
    type: 'object',
    required: ['schema_version', 'kind', 'settings_path', 'dashboard_ui', 'source', 'artifact_output', 'safety', 'diagnostics', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      runtime_schema_version: { type: 'string' },
      kind: { type: 'string' },
      settings_path: { type: 'string' },
      storage: { type: 'object' },
      dashboard_ui: { type: 'object' },
      source: { type: 'object' },
      artifact_output: { type: 'object' },
      safety: { type: 'object' },
      diagnostics: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  localization_resources: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/localization-resources.schema.json',
    title: 'TraceCue Localization Resources',
    type: 'object',
    required: ['schema_version', 'resources_version', 'generated_at', 'locale_selection', 'baseline_locale', 'supported_locale_count', 'key_inventory', 'selected_resource', 'locale_resources', 'fallback', 'rtl_layout_guard', 'raw_evidence_policy', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      resources_version: { type: 'string' },
      generated_at: { type: 'string' },
      locale_selection: { type: 'string' },
      baseline_locale: { type: 'string' },
      supported_locale_count: { type: 'number' },
      key_inventory: { type: 'array' },
      selected_resource: { type: 'object' },
      locale_resources: { type: 'array' },
      fallback: { type: 'object' },
      rtl_layout_guard: { type: 'object' },
      raw_evidence_policy: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  report_templates: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/report-templates.schema.json',
    title: 'TraceCue Report Templates',
    type: 'object',
    required: ['schema_version', 'templates_version', 'generated_at', 'locale_selection', 'baseline_locale', 'supported_locale_count', 'template_inventory', 'selected_templates', 'locale_templates', 'fallback', 'raw_evidence_policy', 'rendering_contract', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      templates_version: { type: 'string' },
      generated_at: { type: 'string' },
      locale_selection: { type: 'string' },
      baseline_locale: { type: 'string' },
      supported_locale_count: { type: 'number' },
      template_inventory: { type: 'array' },
      selected_templates: { type: 'object' },
      locale_templates: { type: 'array' },
      fallback: { type: 'object' },
      raw_evidence_policy: { type: 'object' },
      rendering_contract: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  translation_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/translation-readiness.schema.json',
    title: 'TraceCue Translation Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'locale_selection', 'provider_policy', 'disclosure_plan', 'dry_run_preview', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      locale_selection: { type: 'string' },
      settings: { type: ['object', 'null'] },
      provider_policy: { type: 'object' },
      disclosure_plan: { type: 'object' },
      dry_run_preview: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  translation_dry_run: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/translation-dry-run.schema.json',
    title: 'TraceCue Translation Dry Run',
    type: 'object',
    required: ['schema_version', 'dry_run_version', 'generated_at', 'locale_selection', 'provider', 'status', 'items', 'raw_evidence_policy', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      dry_run_version: { type: 'string' },
      generated_at: { type: 'string' },
      locale_selection: { type: 'string' },
      provider: { type: 'string' },
      status: { type: 'string' },
      items: { type: 'array' },
      raw_evidence_policy: { type: 'object' },
      boundary: { type: 'object' },
      note: { type: 'string' }
    },
    additionalProperties: true
  }),
  release_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/release-readiness.schema.json',
    title: 'TraceCue Release Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'status', 'product_identity', 'package_metadata', 'decisions', 'local_checks', 'publication_boundary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      product_identity: { type: 'object' },
      package_metadata: { type: ['object', 'null'] },
      decisions: { type: 'object' },
      local_checks: { type: 'array' },
      publication_boundary: { type: 'object' },
      next_steps: { type: 'array' },
      diagnostics: { type: 'array' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  artifact_root_policy: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/artifact-root-policy.schema.json',
    title: 'TraceCue Artifact Root Policy',
    type: 'object',
    required: ['schema_version', 'policy_version', 'generated_at', 'product_identity', 'policy_source', 'mode', 'current_behavior', 'roots', 'migration', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      policy_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      policy_source: { type: 'object' },
      mode: { type: 'string' },
      current_behavior: { type: 'object' },
      roots: { type: 'array' },
      migration: { type: 'object' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  artifact_root_migration: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/artifact-root-migration.schema.json',
    title: 'TraceCue Artifact Root Migration',
    type: 'object',
    required: ['schema_version', 'migration_version', 'generated_at', 'status', 'product_identity', 'source_roots', 'target_root', 'candidate_count', 'plan_hash', 'candidates', 'execution_boundary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      migration_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      product_identity: { type: 'object' },
      policy_source: { type: 'object' },
      mode: { type: 'string' },
      source_roots: { type: 'array' },
      target_root: { type: 'string' },
      fixture_only: { type: 'boolean' },
      candidate_count: { type: 'number' },
      copy_count: { type: 'number' },
      conflict_count: { type: 'number' },
      plan_hash: { type: 'string' },
      candidates: { type: 'array' },
      execution_boundary: { type: 'object' },
      execution: { type: 'object' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  legacy_alias_audit: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/legacy-alias-audit.schema.json',
    title: 'TraceCue Legacy Alias Audit',
    type: 'object',
    required: ['schema_version', 'alias_audit_version', 'generated_at', 'product_identity', 'policy', 'summary', 'surfaces', 'replacement_map', 'migration_status', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      alias_audit_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      policy: { type: 'object' },
      summary: { type: 'object' },
      surfaces: { type: 'array' },
      replacement_map: { type: 'object' },
      invocation: { type: 'object' },
      migration_status: { type: 'object' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  legacy_alias_removal_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/legacy-alias-removal-readiness.schema.json',
    title: 'TraceCue Legacy Alias Removal Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'status', 'product_identity', 'policy', 'readiness', 'compatibility', 'blockers', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      product_identity: { type: 'object' },
      policy: { type: 'object' },
      audit_summary: { type: 'object' },
      readiness: { type: 'object' },
      compatibility: { type: 'object' },
      blockers: { type: 'array' },
      next_steps: { type: 'array' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  constrained_shell_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/constrained-shell-readiness.schema.json',
    title: 'TraceCue Constrained Shell Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'status', 'mode', 'product_identity', 'use_case_review', 'threat_model', 'command_schema', 'cli_plan', 'mcp_readiness', 'final_boundary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      mode: { type: 'string' },
      product_identity: { type: 'object' },
      use_case_review: { type: 'array' },
      threat_model: { type: 'object' },
      command_schema: { type: 'object' },
      cli_plan: { type: 'object' },
      mcp_readiness: { type: 'object' },
      final_boundary: { type: 'object' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  final_hardening_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/final-hardening-readiness.schema.json',
    title: 'TraceCue Final Hardening Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'status', 'product_identity', 'phase_range', 'regression_matrix', 'local_gate_plan', 'smoke_rebaseline', 'security_sweep', 'docs_english_scan', 'release_boundary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      product_identity: { type: 'object' },
      phase_range: { type: 'object' },
      regression_matrix: { type: 'array' },
      local_gate_plan: { type: 'array' },
      smoke_rebaseline: { type: 'object' },
      security_sweep: { type: 'object' },
      docs_english_scan: { type: 'object' },
      release_boundary: { type: 'object' },
      next_steps: { type: 'array' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  operation_registry: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-registry.schema.json',
    title: 'TraceCue Operation Registry',
    type: 'object',
    required: ['schema_version', 'registry_version', 'generated_at', 'product_identity', 'operation_selection', 'group_selection', 'risk_selection', 'summary', 'groups', 'risk_taxonomy', 'operations', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      registry_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      operation_selection: { type: 'string' },
      group_selection: { type: 'string' },
      risk_selection: { type: 'string' },
      summary: { type: 'object' },
      groups: { type: 'array' },
      risk_taxonomy: { type: 'array' },
      operations: { type: 'array' },
      boundary: { type: 'object' },
      next_steps: { type: 'array' }
    },
    additionalProperties: true
  }),
  operation_roadmap: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-roadmap.schema.json',
    title: 'TraceCue Operation Roadmap',
    type: 'object',
    required: ['schema_version', 'roadmap_version', 'generated_at', 'product_identity', 'phase_range', 'phase_selection', 'group_selection', 'risk_selection', 'summary', 'groups', 'phases', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      roadmap_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      phase_range: { type: 'object' },
      phase_selection: { type: ['string', 'number'] },
      group_selection: { type: 'string' },
      risk_selection: { type: 'string' },
      summary: { type: 'object' },
      groups: { type: 'array' },
      phases: { type: 'array' },
      boundary: { type: 'object' },
      notes: { type: 'array' }
    },
    additionalProperties: true
  }),
  operation_contracts: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-contracts.schema.json',
    title: 'TraceCue Operation Contracts',
    type: 'object',
    required: ['schema_version', 'contracts_version', 'generated_at', 'product_identity', 'phase_range', 'scope_selection', 'operation_selection', 'summary', 'risk_taxonomy', 'groups', 'selected_operations', 'contracts', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      contracts_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      phase_range: { type: 'object' },
      scope_selection: { type: 'string' },
      operation_selection: { type: 'string' },
      summary: { type: 'object' },
      risk_taxonomy: { type: 'array' },
      groups: { type: 'array' },
      selected_operations: { type: 'array' },
      contracts: { type: 'array' },
      boundary: { type: 'object' },
      notes: { type: 'array' }
    },
    additionalProperties: true
  }),
  operation_policy: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-policy.schema.json',
    title: 'TraceCue Operation Policy',
    type: 'object',
    required: ['schema_version', 'policy_version', 'generated_at', 'product_identity', 'scope_selection', 'operation_selection', 'policy_source', 'admin_policy', 'requirements', 'selected_operations', 'contracts', 'readiness', 'summary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      policy_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      scope_selection: { type: 'string' },
      operation_selection: { type: 'string' },
      policy_source: { type: 'object' },
      admin_policy: { type: 'object' },
      requirements: { type: 'object' },
      selected_operations: { type: 'array' },
      contracts: { type: 'array' },
      readiness: { type: 'array' },
      summary: { type: 'object' },
      boundary: { type: 'object' },
      notes: { type: 'array' }
    },
    additionalProperties: true
  }),
  operation_admin_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-admin-readiness.schema.json',
    title: 'TraceCue Operation Admin Readiness',
    type: 'object',
    required: ['schema_version', 'admin_readiness_version', 'generated_at', 'product_identity', 'scope_selection', 'operation_selection', 'policy_source', 'admin_policy', 'requirements', 'selected_operations', 'readiness', 'approval_boundary', 'summary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      admin_readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      scope_selection: { type: 'string' },
      operation_selection: { type: 'string' },
      policy_source: { type: 'object' },
      admin_policy: { type: 'object' },
      requirements: { type: 'object' },
      selected_operations: { type: 'array' },
      readiness: { type: 'array' },
      approval_boundary: { type: 'object' },
      summary: { type: 'object' },
      boundary: { type: 'object' },
      notes: { type: 'array' }
    },
    additionalProperties: true
  }),
  operation_provider_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/operation-provider-readiness.schema.json',
    title: 'TraceCue Operation Provider Readiness',
    type: 'object',
    required: ['schema_version', 'provider_readiness_version', 'generated_at', 'product_identity', 'scope_selection', 'operation_selection', 'selected_operations', 'admin_policy', 'requirements', 'provider_catalog', 'disclosure_contract', 'credential_guard', 'status_list_contract', 'readiness', 'summary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      provider_readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      product_identity: { type: 'object' },
      scope_selection: { type: 'string' },
      operation_selection: { type: 'string' },
      selected_operations: { type: 'array' },
      admin_policy: { type: 'object' },
      requirements: { type: 'object' },
      provider_catalog: { type: 'array' },
      disclosure_contract: { type: 'object' },
      credential_guard: { type: 'object' },
      status_list_contract: { type: 'object' },
      readiness: { type: 'array' },
      summary: { type: 'object' },
      boundary: { type: 'object' },
      notes: { type: 'array' }
    },
    additionalProperties: true
  }),
  finding: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/finding.schema.json',
    title: 'TraceCue Review Finding',
    type: 'object',
    required: ['id', 'category', 'severity', 'confidence', 'source', 'message', 'evidence', 'artifacts', 'repro'],
    properties: {
      id: { type: 'string' },
      category: {
        enum: [
          'browser_health',
          'layout_integrity',
          'interaction_quality',
          'accessibility_basics',
          'mock_fidelity',
          'evidence_quality'
        ]
      },
      severity: { enum: ['info', 'low', 'medium', 'high', 'critical'] },
      confidence: { enum: ['low', 'medium', 'high'] },
      source: { enum: ['deterministic', 'heuristic', 'model_advisory', 'owner_required'] },
      selector: { type: ['string', 'null'] },
      rect: { type: ['object', 'null'] },
      route: { type: ['string', 'null'] },
      viewport: { type: ['object', 'null'] },
      message: { type: 'string' },
      evidence: { type: 'object' },
      artifacts: { type: 'array' },
      repro: { type: 'array' },
      priority: { type: 'string' },
      impact: { type: 'string' },
      recommendation: { type: 'string' },
      fix_candidates: { type: 'array' },
      implementation_notes: { type: 'object' },
      owner_decision_required: { type: 'boolean' }
    },
    additionalProperties: true
  }),
  target_manifest: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/target-manifest.schema.json',
    title: 'TraceCue Target Manifest',
    type: 'object',
    required: ['baseUrl'],
    properties: {
      baseUrl: { type: 'string' },
      schemaVersion: { type: 'string' },
      name: { type: 'string' },
      scope: { type: 'object' },
      seeds: { type: 'array', items: { type: 'string' } },
      expectedRoutes: { type: 'array', items: { type: 'string' } },
      pages: { type: 'array' },
      sourceData: { type: 'array' },
      localContentUxAdvisory: { type: 'object' },
      viewportMatrix: { type: 'array' },
      actionPolicy: { type: 'object' },
      budgets: { type: 'object' },
      artifacts: { type: 'object' },
      masks: { type: 'array' },
      regions: { type: 'array' },
      appHints: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_evidence: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-evidence.schema.json',
    title: 'TraceCue Visual Evidence',
    type: 'object',
    required: ['schema_version', 'id', 'created_at', 'source', 'media', 'privacy', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      created_at: { type: 'string' },
      purpose: { type: 'string' },
      source: { type: 'object' },
      media: { type: 'object' },
      route: { type: ['string', 'null'] },
      viewport: { type: ['object', 'null'] },
      capture: { type: ['object', 'null'] },
      masks: { type: 'array' },
      regions: { type: 'array' },
      labels: { type: 'array' },
      privacy: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_dashboard: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-dashboard.schema.json',
    title: 'TraceCue Visual Review Dashboard',
    type: 'object',
    required: ['schema_version', 'generated_at', 'status', 'summary', 'latest', 'preparations', 'executions', 'results', 'language_settings', 'control_center_handoff', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { enum: ['empty', 'prepared', 'ready', 'owner_review_recommended'] },
      summary: { type: 'object' },
      latest: { type: 'object' },
      preparations: { type: 'array' },
      executions: { type: 'array' },
      results: { type: 'array' },
      language_settings: { type: 'object' },
      control_center_handoff: { type: 'object' },
      query: { type: 'object' },
      gate_effect: { enum: ['none'] },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_provider_policy: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-provider-policy.schema.json',
    title: 'TraceCue Visual Review Provider Policy',
    type: 'object',
    required: ['schema_version', 'scope', 'status', 'disclosure_mode', 'visual_evidence', 'disclosure', 'execution', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      scope: { type: 'string' },
      status: { enum: ['planned', 'blocked_external_transfer_requires_owner_review'] },
      disclosure_mode: { enum: ['metadata_only', 'local_reference', 'explicit_image_transfer'] },
      provider: { type: 'object' },
      model: { type: 'object' },
      surface: { type: 'object' },
      visual_evidence: { type: 'object' },
      disclosure: { type: 'object' },
      execution: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_result_preparation: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-result-preparation.schema.json',
    title: 'TraceCue Visual Review Result Preparation',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'source', 'visual_evidence', 'provider_policy', 'disclosure_policy', 'result_contract', 'execution', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['prepared', 'blocked_missing_visual_evidence'] },
      created_at: { type: 'string' },
      source: { type: 'object' },
      visual_evidence: { type: 'object' },
      provider_policy: { type: 'object' },
      disclosure_policy: { type: 'object' },
      result_contract: { type: 'object' },
      result_template: { type: 'object' },
      execution: { type: 'object' },
      dashboard_handoff: { type: 'object' },
      gate_effect: { enum: ['none'] },
      browser_launched: { type: 'boolean' },
      provider_call_performed: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      external_evidence_transfer: { type: 'boolean' },
      raw_pixels_included: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' },
      mcp_execution_exposed: { type: 'boolean' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_execution: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-execution.schema.json',
    title: 'TraceCue Visual Review Execution',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'execution_path', 'preparation_path', 'surface', 'provider', 'model', 'steps', 'dashboard_handoff', 'gate_effect', 'api_call_performed', 'automatic_upload', 'existing_review_mutated', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['completed', 'failed', 'blocked'] },
      mode: { type: 'string' },
      created_at: { type: 'string' },
      evaluated_at: { type: 'string' },
      completed_at: { type: ['string', 'null'] },
      execution_path: { type: 'string' },
      execution_receipt_path: { type: 'string' },
      preparation_id: { type: ['string', 'null'] },
      preparation_path: { type: 'string' },
      preparation_hash: { type: 'string' },
      result_path: { type: ['string', 'null'] },
      latest_result_path: { type: ['string', 'null'] },
      surface: { type: 'object' },
      provider: { type: 'object' },
      model: { type: 'object' },
      provider_adapter: { type: 'object' },
      steps: { type: 'object' },
      dashboard_handoff: { type: 'object' },
      disclosure_policy: { type: 'object' },
      gate_effect: { enum: ['none'] },
      provider_call_performed: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      external_evidence_transfer: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      credential_storage: { type: 'string' },
      persistent_credential_storage: { type: 'boolean' },
      credential_values_recorded: { type: 'boolean' },
      raw_response_stored: { type: 'boolean' },
      raw_provider_response_stored: { type: 'boolean' },
      raw_pixels_included: { type: 'boolean' },
      raw_pixels_read: { type: 'boolean' },
      raw_pixels_transferred: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' },
      mcp_execution_exposed: { type: 'boolean' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_result: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-result.schema.json',
    title: 'TraceCue Visual Review Result',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'preparation_id', 'visual_review_result', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['not_run', 'completed', 'needs_owner_review', 'rejected'] },
      preparation_id: { type: 'string' },
      visual_review_result: { type: 'object' },
      provider: { type: 'object' },
      model: { type: 'object' },
      advisory_findings: { type: 'array' },
      owner_decision_requests: { type: 'array' },
      evidence_refs: { type: 'array' },
      gate_effect: { enum: ['none'] },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  visual_review_aggregation: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/visual-review-aggregation.schema.json',
    title: 'TraceCue Visual Review Aggregation',
    type: 'object',
    required: ['schema_version', 'generated_at', 'status', 'source', 'summary', 'source_effects', 'review_agents', 'aggregation_findings', 'conflicts', 'owner_decision_requests', 'query', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { enum: ['completed', 'no_results'] },
      source: { type: 'object' },
      summary: { type: 'object' },
      source_effects: { type: 'object' },
      review_agents: { type: 'array' },
      aggregation_findings: { type: 'array' },
      conflicts: { type: 'array' },
      owner_decision_requests: { type: 'array' },
      query: { type: 'object' },
      gate_effect: { enum: ['none'] },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  image_review: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/image-review.schema.json',
    title: 'TraceCue Image Review',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'review', 'image_review', 'findings', 'metrics', 'environment', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['passed', 'needs_attention'] },
      created_at: { type: 'string' },
      review: { type: 'object' },
      image_review: { type: 'object' },
      findings: { type: 'array' },
      metrics: { type: 'object' },
      quality_signals: { type: 'object' },
      evidence_summary: { type: 'object' },
      environment: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agent_surface: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-surface.schema.json',
    title: 'TraceCue Agent Surface',
    type: 'object',
    required: ['id', 'kind', 'transport', 'status', 'external_evidence_transfer', 'credential_mode', 'implemented'],
    properties: {
      id: { type: 'string' },
      display_name: { type: 'string' },
      kind: { enum: ['subscription_surface', 'api_provider'] },
      transport: { type: 'string' },
      status: { enum: ['available', 'approval_required', 'disabled'] },
      automation: { type: 'string' },
      external_evidence_transfer: { type: 'boolean' },
      credential_mode: { type: 'string' },
      implemented: { type: 'boolean' },
      capabilities: { type: 'array' },
      boundaries: { type: 'object' },
      approval_required_for: { type: 'array' }
    },
    additionalProperties: true
  }),
  agent_task_package: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-task-package.schema.json',
    title: 'TraceCue Agent Task Package',
    type: 'object',
    required: ['schema_version', 'id', 'task', 'status', 'created_at', 'surface', 'source', 'disclosure_policy', 'evidence_packet', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      task: { type: 'string' },
      status: { enum: ['ready', 'error'] },
      created_at: { type: 'string' },
      surface: { type: 'object' },
      source: { type: 'object' },
      disclosure_policy: { type: 'object' },
      evidence_packet: { type: 'object' },
      prompt: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agent_request_status: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-request-status.schema.json',
    title: 'TraceCue Agent Request Status',
    type: 'object',
    required: ['schema_version', 'package_id', 'package_path', 'status', 'gate_effect', 'api_call_performed', 'automatic_upload', 'existing_review_mutated'],
    properties: {
      schema_version: { type: 'string' },
      package_id: { type: ['string', 'null'] },
      package_path: { type: 'string' },
      prompt_path: { type: ['string', 'null'] },
      receipt_path: { type: ['string', 'null'] },
      review_artifact_index_path: { type: ['string', 'null'] },
      review_id: { type: ['string', 'null'] },
      task: { type: ['string', 'null'] },
      status: { enum: ['waiting_for_agent', 'advisory_imported'] },
      created_at: { type: ['string', 'null'] },
      surface: { type: ['object', 'null'] },
      result_paths: { type: 'array' },
      latest_result_path: { type: ['string', 'null'] },
      advisory_findings: { type: 'number' },
      owner_decision_requests: { type: 'number' },
      gate_effect: { enum: ['none'] },
      external_evidence_transfer: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' },
      next_step: { type: 'string' }
    },
    additionalProperties: true
  }),
  agent_request_detail: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-request-detail.schema.json',
    title: 'TraceCue Agent Request Detail',
    type: 'object',
    required: ['schema_version', 'package_id', 'package_path', 'status', 'gate_effect', 'api_call_performed', 'automatic_upload', 'existing_review_mutated'],
    properties: {
      schema_version: { type: 'string' },
      package_id: { type: ['string', 'null'] },
      package_path: { type: 'string' },
      prompt_path: { type: ['string', 'null'] },
      receipt_path: { type: ['string', 'null'] },
      status: { enum: ['waiting_for_agent', 'advisory_imported'] },
      selected_result_path: { type: ['string', 'null'] },
      latest_result_path: { type: ['string', 'null'] },
      result_paths: { type: 'array' },
      created_at: { type: ['string', 'null'] },
      task: { type: ['string', 'null'] },
      surface: { type: ['object', 'null'] },
      source: { type: 'object' },
      package_summary: { type: 'object' },
      agent_advisory_summary: { type: ['object', 'null'] },
      dashboard_handoff: { type: 'object' },
      gate_effect: { enum: ['none'] },
      external_evidence_transfer: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' }
    },
    additionalProperties: true
  }),
  agent_workflow: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-workflow.schema.json',
    title: 'TraceCue Agent Workflow',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'workflow_path', 'package_path', 'steps', 'dashboard_handoff', 'provider_boundary', 'gate_effect', 'api_call_performed', 'automatic_upload', 'existing_review_mutated'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      name: { type: 'string' },
      status: { enum: ['waiting_for_agent', 'advisory_imported', 'package_missing'] },
      created_at: { type: ['string', 'null'] },
      updated_at: { type: ['string', 'null'] },
      evaluated_at: { type: 'string' },
      workflow_path: { type: 'string' },
      workflow_receipt_path: { type: ['string', 'null'] },
      package_id: { type: ['string', 'null'] },
      package_path: { type: ['string', 'null'] },
      prompt_path: { type: ['string', 'null'] },
      receipt_path: { type: ['string', 'null'] },
      review_artifact_index_path: { type: ['string', 'null'] },
      review_id: { type: ['string', 'null'] },
      task: { type: ['string', 'null'] },
      surface: { type: ['object', 'null'] },
      latest_result_path: { type: ['string', 'null'] },
      result_paths: { type: 'array' },
      report_paths: { type: 'array' },
      advisory_findings: { type: 'number' },
      owner_decision_requests: { type: 'number' },
      steps: { type: 'object' },
      dashboard_handoff: { type: 'object' },
      request_status: { type: ['object', 'null'] },
      request_detail: { type: ['object', 'null'] },
      provider_boundary: { type: 'object' },
      gate_effect: { enum: ['none'] },
      external_evidence_transfer: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agent_execution: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-execution.schema.json',
    title: 'TraceCue Agent Execution',
    type: 'object',
    required: ['schema_version', 'id', 'status', 'execution_path', 'package_path', 'surface', 'provider', 'model', 'steps', 'dashboard_handoff', 'gate_effect', 'api_call_performed', 'automatic_upload', 'existing_review_mutated'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['planned', 'running', 'completed', 'failed', 'blocked'] },
      mode: { type: 'string' },
      created_at: { type: ['string', 'null'] },
      evaluated_at: { type: 'string' },
      execution_path: { type: 'string' },
      execution_receipt_path: { type: ['string', 'null'] },
      package_id: { type: ['string', 'null'] },
      package_path: { type: 'string' },
      prompt_path: { type: ['string', 'null'] },
      review_artifact_index_path: { type: ['string', 'null'] },
      surface: { type: 'object' },
      provider: { type: 'object' },
      model: { type: 'object' },
      steps: { type: 'object' },
      dashboard_handoff: { type: 'object' },
      dashboard_status: { type: 'object' },
      disclosure_policy: { type: 'object' },
      provider_adapter: { type: 'object' },
      visual_review_provider_policy: { type: 'object' },
      raw_pixels_included: { type: 'boolean' },
      visual_review_provider_execution_authorized: { type: 'boolean' },
      normalized_agent_result_path: { type: ['string', 'null'] },
      latest_result_path: { type: ['string', 'null'] },
      agent_result_path: { type: ['string', 'null'] },
      execution_run_receipt_path: { type: ['string', 'null'] },
      gate_effect: { enum: ['none'] },
      external_evidence_transfer: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      automatic_upload: { type: 'boolean' },
      credential_storage: { type: 'string' },
      persistent_credential_storage: { type: 'boolean' },
      credential_values_recorded: { type: 'boolean' },
      raw_response_stored: { type: 'boolean' },
      raw_provider_response_stored: { type: 'boolean' },
      existing_review_mutated: { type: 'boolean' },
      mcp_execution_exposed: { type: 'boolean' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_proposal: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-proposal.schema.json',
    title: 'TraceCue Agentic Human Review Proposal',
    type: 'object',
    required: ['schema_version', 'proposal_version', 'human_review_schema_version', 'type', 'id', 'status', 'created_at', 'proposal_path', 'proposal_hash', 'source_request', 'structured_intent', 'human_review_contract', 'plan_candidate', 'human_explanation', 'review_effort', 'sub_agents', 'transfer_preview', 'provider', 'model', 'surface', 'next_commands', 'approval', 'boundary', 'gate_effect'],
    properties: {
      schema_version: { type: 'string' },
      proposal_version: { type: 'string' },
      human_review_schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_proposal' },
      id: { type: 'string' },
      status: { type: 'string' },
      created_at: { type: 'string' },
      proposal_path: { type: 'string' },
      proposal_hash: { type: 'string' },
      source_request: { type: 'object' },
      structured_intent: { type: 'object' },
      human_review_contract: { type: 'object' },
      plan_candidate: { type: 'object' },
      human_explanation: { type: 'object' },
      review_effort: { type: 'object' },
      default_subagent_effort: { type: 'string' },
      role_efforts: { type: 'array' },
      sub_agents: { type: 'array' },
      rounds: { type: 'array' },
      transfer_preview: { type: 'object' },
      provider: { type: 'object' },
      model: { type: 'object' },
      surface: { type: 'object' },
      next_commands: { type: 'object' },
      approval: { type: 'object' },
      boundary: { type: 'object' },
      gate_effect: { const: 'none' }
    },
    additionalProperties: true
  }),
  agentic_human_review_provider_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-provider-readiness.schema.json',
    title: 'TraceCue Agentic Human Review Provider Readiness',
    type: 'object',
    required: ['schema_version', 'type', 'generated_at', 'status', 'selected_provider', 'providers', 'policy', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_provider_readiness' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      selected_provider: { type: 'string' },
      providers: { type: 'array' },
      input_context: { type: 'object' },
      policy: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_report_quality: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-report-quality.schema.json',
    title: 'TraceCue Agentic Human Review Report Quality',
    type: 'object',
    required: ['schema_version', 'type', 'generated_at', 'result_path', 'completeness_score', 'evidence_coverage_score', 'verification_score', 'human_review_coverage_score', 'actionability_score', 'advisory_only', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_report_quality' },
      generated_at: { type: 'string' },
      quality_evaluator_version: { type: 'string' },
      result_path: { type: 'string' },
      result_id: { type: ['string', 'null'] },
      execution_id: { type: ['string', 'null'] },
      completeness_score: { type: 'number' },
      evidence_coverage_score: { type: 'number' },
      verification_score: { type: 'number' },
      human_review_coverage_score: { type: 'number' },
      actionability_score: { type: 'number' },
      role_count: { type: 'number' },
      finding_count: { type: 'number' },
      claim_count: { type: 'number' },
      owner_decision_count: { type: 'number' },
      quality_warnings: { type: 'array' },
      human_review_maturity: { type: 'object' },
      longitudinal_quality_evaluation: { type: 'object' },
      benchmark_completion_readiness: { type: ['object', 'null'] },
      xhigh_multi_round_review: { type: ['object', 'null'] },
      advisory_only: { const: true },
      gate_effect: { const: 'none' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_benchmark_case: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-benchmark-case.schema.json',
    title: 'TraceCue Agentic Human Review Benchmark Case',
    type: 'object',
    required: ['schema_version', 'type', 'benchmark_version', 'case', 'rubric_profile', 'calibration_contract', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_benchmark_case' },
      benchmark_version: { type: 'string' },
      case: { type: 'object' },
      rubric_profile: { type: 'object' },
      calibration_contract: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_benchmark_cases: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-benchmark-cases.schema.json',
    title: 'TraceCue Agentic Human Review Benchmark Cases',
    type: 'object',
    required: ['schema_version', 'type', 'benchmark_version', 'generated_at', 'cases', 'summary', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_benchmark_cases' },
      benchmark_version: { type: 'string' },
      generated_at: { type: 'string' },
      cases: { type: 'array' },
      summary: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_calibration_result: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-calibration-result.schema.json',
    title: 'TraceCue Agentic Human Review Calibration Result',
    type: 'object',
    required: ['schema_version', 'type', 'calibration_version', 'generated_at', 'result_path', 'case_id', 'scores', 'passed', 'warnings', 'advisory_only', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_calibration_result' },
      calibration_version: { type: 'string' },
      generated_at: { type: 'string' },
      result_path: { type: 'string' },
      result_id: { type: ['string', 'null'] },
      case_id: { type: 'string' },
      fixture_type: { type: 'string' },
      rubric_profile_id: { type: 'string' },
      scores: { type: 'object' },
      required_mentions: { type: 'array' },
      forbidden_claims: { type: 'array' },
      required_dimensions: { type: 'array' },
      passed: { type: 'boolean' },
      warnings: { type: 'array' },
      advisory_only: { const: true },
      gate_effect: { const: 'none' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_comparison: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-comparison.schema.json',
    title: 'TraceCue Agentic Human Review Comparison',
    type: 'object',
    required: ['schema_version', 'type', 'comparison_version', 'generated_at', 'baseline', 'candidate', 'deltas', 'summary', 'warnings', 'advisory_only', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_comparison' },
      comparison_version: { type: 'string' },
      comparison_kind: { type: 'string' },
      generated_at: { type: 'string' },
      baseline: { type: 'object' },
      candidate: { type: 'object' },
      deltas: { type: 'object' },
      summary: { type: 'object' },
      direct_vs_tracecue_analysis: { type: ['object', 'null'] },
      warnings: { type: 'array' },
      advisory_only: { const: true },
      gate_effect: { const: 'none' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_dogfood_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-dogfood-readiness.schema.json',
    title: 'TraceCue Agentic Human Review Dogfood Readiness',
    type: 'object',
    required: ['schema_version', 'type', 'readiness_version', 'generated_at', 'status', 'provider', 'dogfood_set', 'setup', 'boundary', 'advisory_only', 'gate_effect'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_dogfood_readiness' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      status: { type: 'string' },
      provider: { type: 'object' },
      dogfood_set: { type: 'object' },
      setup: { type: 'object' },
      human_review_maturity_plan: { type: 'object' },
      required_owner_controls: { type: 'array' },
      next_commands: { type: 'object' },
      boundary: { type: 'object' },
      advisory_only: { const: true },
      gate_effect: { const: 'none' }
    },
    additionalProperties: true
  }),
  agentic_human_review_dogfood_plan: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-dogfood-plan.schema.json',
    title: 'TraceCue Agentic Human Review Dogfood Plan',
    type: 'object',
    required: ['schema_version', 'type', 'dogfood_plan_version', 'generated_at', 'case', 'rubric_profile', 'provider', 'dogfood_set', 'workflow', 'evaluation_focus', 'manual_live_provider_policy', 'boundary', 'advisory_only', 'gate_effect'],
    properties: {
      schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_dogfood_plan' },
      dogfood_plan_version: { type: 'string' },
      generated_at: { type: 'string' },
      case: { type: 'object' },
      rubric_profile: { type: 'object' },
      provider: { type: 'object' },
      dogfood_set: { type: 'object' },
      human_review_maturity_plan: { type: 'object' },
      workflow: { type: 'object' },
      evaluation_focus: { type: 'object' },
      manual_live_provider_policy: { type: 'object' },
      boundary: { type: 'object' },
      advisory_only: { const: true },
      gate_effect: { const: 'none' }
    },
    additionalProperties: true
  }),
  agentic_human_review_plan: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-plan.schema.json',
    title: 'TraceCue Agentic Human Review Plan',
    type: 'object',
    required: ['schema_version', 'plan_version', 'human_review_schema_version', 'type', 'id', 'status', 'created_at', 'plan_path', 'package_path', 'package_hash', 'plan_hash', 'human_review_contract', 'human_explanation', 'review_effort', 'default_subagent_effort', 'sub_agents', 'transfer_permissions', 'disclosure', 'provider', 'model', 'surface', 'rubric', 'provider_instruction_contract', 'review_quality_benchmark', 'approval', 'execution', 'result_contract', 'gate_effect', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      plan_version: { type: 'string' },
      human_review_schema_version: { type: 'string' },
      type: { const: 'agentic_human_review_plan' },
      id: { type: 'string' },
      status: { type: 'string' },
      created_at: { type: 'string' },
      plan_path: { type: 'string' },
      package_path: { type: 'string' },
      package_hash: { type: 'string' },
      plan_hash: { type: 'string' },
      source: { type: 'object' },
      source_evidence_summary: { type: 'object' },
      intent: { type: 'string' },
      review_scope: { type: 'object' },
      human_review_contract: { type: 'object' },
      human_explanation: { type: 'object' },
      review_effort: { type: 'object' },
      default_subagent_effort: { type: 'string' },
      role_efforts: { type: 'array' },
      sub_agents: { type: 'array' },
      rounds: { type: 'array' },
      transfer_permissions: { type: 'object' },
      disclosure: { type: 'object' },
      provider: { type: 'object' },
      model: { type: 'object' },
      surface: { type: 'object' },
      rubric: { type: 'object' },
      provider_instruction_contract: { type: 'object' },
      review_quality_benchmark: { type: 'object' },
      approval: { type: 'object' },
      execution: { type: 'object' },
      result_contract: { type: 'object' },
      gate_effect: { const: 'none' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_package: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-package.schema.json',
    title: 'TraceCue Agentic Human Review Package',
    type: 'object',
    required: ['schema_version', 'package_version', 'human_review_schema_version', 'package_kind', 'id', 'created_at', 'package_path', 'task', 'human_review_input_contract', 'source', 'visual_evidence', 'content_evidence', 'semantic_evidence', 'technical_evidence', 'mechanical_review_summary', 'artifact_references', 'existing_review_state', 'disclosure', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      package_version: { type: 'string' },
      human_review_schema_version: { type: 'string' },
      package_kind: { const: 'agentic_human_review_package' },
      id: { type: 'string' },
      created_at: { type: 'string' },
      package_path: { type: 'string' },
      task: { type: 'object' },
      human_review_input_contract: { type: 'object' },
      source: { type: 'object' },
      visual_evidence: { type: 'object' },
      content_evidence: { type: 'object' },
      semantic_evidence: { type: 'object' },
      technical_evidence: { type: 'object' },
      mechanical_review_summary: { type: 'object' },
      artifact_references: { type: 'array' },
      existing_review_state: { type: 'object' },
      disclosure: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  human_review_rubric: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/human-review-rubric.schema.json',
    title: 'TraceCue Human Review Rubric',
    type: 'object',
    required: ['schema_version', 'rubric_version', 'human_review_schema_version', 'output_schema', 'areas', 'dimensions', 'required_sections', 'confidence_model', 'advisory_only', 'gate_effect'],
    properties: {
      schema_version: { type: 'string' },
      rubric_version: { type: 'string' },
      human_review_schema_version: { type: 'string' },
      output_schema: { const: 'agentic_human_review_advisory' },
      areas: { type: 'array' },
      dimensions: { type: 'array' },
      required_sections: { type: 'array' },
      confidence_model: { type: 'array' },
      advisory_only: { const: true },
      gate_effect: { const: 'none' }
    },
    additionalProperties: true
  }),
  agentic_human_review_advisory: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-advisory.schema.json',
    title: 'TraceCue Agentic Human Review Advisory',
    type: 'object',
    required: ['schema_version', 'id', 'result_type', 'human_review_schema_version', 'agentic_human_review_advisory', 'non_engineer_summary', 'subjective_perception', 'readability_comprehension', 'reader_experience_review', 'mechanical_vs_human_review', 'human_review_coverage', 'role_opinions', 'consensus_summary', 'dissent_summary', 'agentic_human_review_findings', 'agentic_human_review_action_plan', 'agentic_human_review_readiness', 'owner_decision_requests', 'provider', 'model', 'transfer_permissions', 'execution', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      result_type: { const: 'agentic_human_review_advisory' },
      human_review_schema_version: { type: 'string' },
      agentic_human_review_advisory: { type: 'object' },
      non_engineer_summary: { type: 'object' },
      subjective_perception: { type: 'object' },
      readability_comprehension: { type: 'object' },
      reader_experience_review: { type: 'object' },
      mechanical_vs_human_review: { type: 'object' },
      human_review_coverage: { type: 'object' },
      role_opinions: { type: 'array' },
      consensus_summary: { type: 'object' },
      dissent_summary: { type: 'object' },
      agentic_human_review_findings: { type: 'array' },
      agentic_human_review_action_plan: { type: 'object' },
      agentic_human_review_readiness: { type: 'object' },
      owner_decision_requests: { type: 'array' },
      provider: { type: 'object' },
      model: { type: 'object' },
      surface: { type: 'object' },
      transfer_permissions: { type: 'object' },
      execution: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agentic_human_review_receipt: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agentic-human-review-receipt.schema.json',
    title: 'TraceCue Agentic Human Review Receipt',
    type: 'object',
    required: ['schema_version', 'type', 'id', 'created_at', 'plan_path', 'plan_hash', 'provider_call_performed', 'api_call_performed', 'external_evidence_transfer', 'raw_provider_response_stored', 'credential_values_recorded', 'mcp_execution_exposed', 'gate_effect'],
    properties: {
      schema_version: { type: 'string' },
      type: { type: 'string' },
      id: { type: 'string' },
      created_at: { type: 'string' },
      execution_id: { type: 'string' },
      execution_path: { type: 'string' },
      result_path: { type: ['string', 'null'] },
      report_path: { type: ['string', 'null'] },
      plan_path: { type: 'string' },
      package_path: { type: 'string' },
      plan_hash: { type: 'string' },
      package_hash: { type: 'string' },
      approved_by: { type: 'string' },
      transfer_flags: { type: 'array' },
      provider_id: { type: ['string', 'null'] },
      model_id: { type: ['string', 'null'] },
      status: { type: 'string' },
      provider_call_performed: { type: 'boolean' },
      api_call_performed: { type: 'boolean' },
      external_evidence_transfer: { type: 'boolean' },
      raw_provider_response_stored: { const: false },
      credential_values_recorded: { const: false },
      mcp_execution_exposed: { const: false },
      gate_effect: { const: 'none' }
    },
    additionalProperties: true
  }),
  agent_advisory_result: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-advisory-result.schema.json',
    title: 'TraceCue Agent Advisory Result',
    type: 'object',
    required: ['schema_version', 'id', 'agent_advisory', 'agent_advisory_findings', 'agent_advisory_action_plan', 'agent_advisory_readiness', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      package_id: { type: ['string', 'null'] },
      package_path: { type: 'string' },
      imported_at: { type: 'string' },
      agent_advisory: { type: 'object' },
      agent_advisory_findings: { type: 'array' },
      agent_advisory_action_plan: { type: 'object' },
      agent_advisory_readiness: { type: 'object' },
      owner_decision_requests: { type: 'array' },
      warnings: { type: 'array' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  agent_disclosure_policy: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/agent-disclosure-policy.schema.json',
    title: 'TraceCue Agent Disclosure Policy',
    type: 'object',
    required: ['scope', 'raw_artifact_content_included', 'external_evidence_transfer', 'redaction_applied'],
    properties: {
      scope: { type: 'string' },
      raw_artifact_content_included: { type: 'boolean' },
      raw_dom_included: { type: 'boolean' },
      trace_content_included: { type: 'boolean' },
      screenshot_binary_included: { type: 'boolean' },
      console_payloads_included: { type: 'boolean' },
      network_payloads_included: { type: 'boolean' },
      source_data_values_included: { type: 'boolean' },
      local_artifact_paths_included: { type: 'boolean' },
      visual_evidence_metadata_included: { type: 'boolean' },
      raw_pixels_included: { type: 'boolean' },
      future_execute_required: { type: 'boolean' },
      provider_execution_authorized: { type: 'boolean' },
      external_evidence_transfer: { type: 'boolean' },
      requires_owner_review_before_external_transfer: { type: 'boolean' },
      redaction_applied: { type: 'boolean' }
    },
    additionalProperties: true
  }),
  review: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/review.schema.json',
    title: 'TraceCue Review Result',
    type: 'object',
    required: ['review', 'findings', 'metrics', 'environment'],
    properties: {
      review: { type: 'object' },
      findings: { type: 'array', items: { $ref: 'finding.schema.json' } },
      metrics: { type: 'object' },
      action_plan: { type: 'object' },
      review_advisory: { type: 'object' },
      manifest_suggestions: { type: 'array' },
      local_content_ux_advisory: { type: 'object' },
      content_ux_findings: { type: 'array' },
      content_ux_action_plan: { type: 'object' },
      content_ux_readiness: { type: 'object' },
      content_ux_page_handoff: { type: 'object' },
      content_ux_manifest_authoring: { type: 'object' },
      content_ux_review_brief: { type: 'object' },
      content_ux_rubric_evaluation: { type: 'object' },
      quality_signals: { type: 'object' },
      language_settings: { type: 'object' },
      resource_guard: { type: 'object' },
      evidence_summary: { type: 'object' },
      artifact_index: { type: 'object' },
      agent_advisory: { type: 'object' },
      agent_advisory_findings: { type: 'array' },
      agent_advisory_action_plan: { type: 'object' },
      agent_advisory_readiness: { type: 'object' },
      environment: { type: 'object' },
      coverage: { type: 'object' }
    },
    additionalProperties: true
  }),
  capture_handoff: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/capture-handoff.schema.json',
    title: 'TraceCue Capture Handoff',
    type: 'object',
    required: ['schema_version', 'handoff_version', 'id', 'status', 'generated_at', 'created_at', 'source', 'media', 'privacy', 'disclosure_policy', 'visual_evidence_contract', 'handoff', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      handoff_version: { type: 'string' },
      id: { type: 'string' },
      status: { type: 'string' },
      generated_at: { type: 'string' },
      created_at: { type: 'string' },
      source: { type: 'object' },
      media: { type: 'object' },
      privacy: { type: 'object' },
      disclosure_policy: { type: 'object' },
      visual_evidence_contract: { type: 'object' },
      handoff: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  desktop_review_provider_preparation_plan: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/desktop-review-provider-preparation-plan.schema.json',
    title: 'TraceCue Desktop Review Provider Preparation Plan',
    type: 'object',
    required: ['schema_version', 'plan_version', 'id', 'status', 'generated_at', 'source', 'media', 'readiness', 'provider_preparation', 'disclosure_policy', 'handoff_contract', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      plan_version: { type: 'string' },
      id: { type: 'string' },
      status: { enum: ['planned'] },
      generated_at: { type: 'string' },
      source: { type: 'object' },
      media: { type: 'object' },
      readiness: { type: 'object' },
      provider_preparation: { type: 'object' },
      disclosure_policy: { type: 'object' },
      handoff_contract: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  capture_plan: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/capture-plan.schema.json',
    title: 'TraceCue Capture Plan',
    type: 'object',
    required: ['schema_version', 'plan_version', 'generated_at', 'source_selection', 'summary', 'sources', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      plan_version: { type: 'string' },
      generated_at: { type: 'string' },
      source_selection: { type: 'string' },
      summary: { type: 'object' },
      sources: { type: 'array' },
      boundary: { type: 'object' },
      next_steps: { type: 'array' }
    },
    additionalProperties: true
  }),
  capture_readiness: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/capture-readiness.schema.json',
    title: 'TraceCue Capture Readiness',
    type: 'object',
    required: ['schema_version', 'readiness_version', 'generated_at', 'source_selection', 'probe', 'summary', 'capabilities', 'privacy_policy', 'artifact_contract', 'approval_bound_operations', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      readiness_version: { type: 'string' },
      generated_at: { type: 'string' },
      source_selection: { type: 'string' },
      probe: { type: 'object' },
      summary: { type: 'object' },
      capabilities: { type: 'array' },
      privacy_policy: { type: 'object' },
      artifact_contract: { type: 'object' },
      approval_bound_operations: { type: 'array' },
      boundary: { type: 'object' },
      next_steps: { type: 'array' }
    },
    additionalProperties: true
  }),
  capture_artifact: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/capture-artifact.schema.json',
    title: 'TraceCue Capture Artifact',
    type: 'object',
    required: ['schema_version', 'id', 'created_at', 'source_kind', 'selected_surface', 'artifact', 'media', 'privacy', 'receipt', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      created_at: { type: 'string' },
      source_kind: { enum: ['screen_capture', 'window_capture', 'desktop_app_capture'] },
      selected_surface: { type: 'object' },
      artifact: { type: 'object' },
      media: { type: 'object' },
      privacy: { type: 'object' },
      receipt: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  capture_receipt: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/capture-receipt.schema.json',
    title: 'TraceCue Capture Receipt',
    type: 'object',
    required: ['schema_version', 'id', 'created_at', 'operation', 'source_kind', 'selected_surface', 'artifact', 'disclosure', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      id: { type: 'string' },
      created_at: { type: 'string' },
      operation: { type: 'string' },
      source_kind: { enum: ['screen_capture', 'window_capture', 'desktop_app_capture'] },
      selected_surface: { type: 'object' },
      artifact: { type: 'object' },
      disclosure: { type: 'object' },
      boundary: { type: 'object' }
    },
    additionalProperties: true
  }),
  mcp_execution_gates: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/mcp-execution-gates.schema.json',
    title: 'TraceCue MCP Execution Gates',
    type: 'object',
    required: ['schema_version', 'policy_version', 'generated_at', 'server_name', 'profile_selection', 'operation_selection', 'summary', 'operations', 'boundary'],
    properties: {
      schema_version: { type: 'string' },
      policy_version: { type: 'string' },
      generated_at: { type: 'string' },
      server_name: { type: 'string' },
      profile_selection: { type: 'string' },
      operation_selection: { type: 'string' },
      summary: { type: 'object' },
      operations: { type: 'array' },
      registry: { type: 'object' },
      boundary: { type: 'object' },
      next_steps: { type: 'array' }
    },
    additionalProperties: true
  }),
  mcp_tool: Object.freeze({
    $schema: 'https://json-schema.org/draft/2020-12/schema',
    $id: 'https://trace-cue.local/schemas/mcp-tool.schema.json',
    title: 'TraceCue MCP Tool Contract',
    type: 'object',
    required: ['name', 'description', 'inputSchema'],
    properties: {
      name: { type: 'string' },
      description: { type: 'string' },
      inputSchema: { type: 'object' },
      minimumProfile: { type: 'string', enum: ['safe', 'full', 'admin'] },
      tags: {
        type: 'array',
        items: { type: 'string' }
      },
      effects: {
        type: 'object',
        properties: {
          browserLaunched: { type: 'boolean' },
          writesArtifacts: { type: 'boolean' },
          deletesFiles: { type: 'boolean' },
          providerCall: { type: 'boolean' },
          shellUsed: { type: 'boolean' },
          externalListener: { type: 'boolean' },
          externalUpload: { type: 'boolean' }
        },
        additionalProperties: false
      }
    },
    additionalProperties: true
  })
});

export function listSchemas() {
  return Object.keys(schemas).map((name) => ({
    name,
    schema_version: SCHEMA_VERSION,
    id: schemas[name].$id,
    title: schemas[name].title
  }));
}

export function getSchema(name) {
  return schemas[name] ?? null;
}

export function schemaNames() {
  return Object.keys(schemas);
}

export function schemaResult(name) {
  const schema = getSchema(name);
  if (!schema) {
    return {
      status: 'error',
      data: {},
      warnings: [],
      errors: [{
        code: 'SCHEMA_NOT_FOUND',
        message: `Unknown schema: ${name}`,
        details: { schema: name, available: schemaNames() }
      }],
      artifacts: []
    };
  }
  return {
    status: 'ok',
    data: { schema },
    warnings: [],
    errors: [],
    artifacts: []
  };
}

export function schemaListResult() {
  return {
    status: 'ok',
    data: { schemas: listSchemas() },
    warnings: [],
    errors: [],
    artifacts: []
  };
}
