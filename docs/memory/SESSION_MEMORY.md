# SESSION_MEMORY.md

Session memory reset: 2026-07-14.

Next-session handoff:

1. Work in `/home/masahiro/projects/agent-toolbox/trace-cue`.
2. Treat this file as temporary carryover only. Read `AGENTS.MD`, the parent repository `AGENTS.MD`, `docs/workflow/INSTRUCTION_MEMORY.md`, `docs/workflow/HANDOFF.md`, `docs/workflow/TASK_TRACKER.md`, `docs/product/REQUIREMENTS.md`, `docs/product/SPECIFICATION.md`, and `docs/product/IMPLEMENTATION_PLAN.md` before continuing.
3. Phase 182-187 Control Center AI setup and response-loss hardening is complete and recorded in `docs/workflow/HANDOFF.md`, `docs/workflow/TASK_TRACKER.md`, and the canonical product and workflow authorities.
4. New Review and Settings share one non-engineer setup dialog. Supported Codex subscription sign-in and session-only API setup require no terminal, environment-variable, or configuration-file editing. TraceCue `standard`, `deep`, and `xhigh` remain independent from provider model effort.
5. Production launch pairs the opened browser with one-use pairing, memory-only session, and separate CSRF capabilities. Direct serve is read-only. API keys stay in a bounded server-owned session vault and do not enter JSON, URLs, browser storage, process environments, workspace files, receipts, logs, artifacts, or public projections.
6. Browser requests have complete response deadlines. Empty, malformed, late, or lost responses reconcile only from authoritative state. Route generation guards prevent an older page action from replacing a newer page.
7. Repeat admission is exact-once across same-process and cross-process concurrency, response loss, malformed responses, active/history storage, restart, capacity, and admission-lock timeout. Persistent state contains only the key and request digests.
8. The active approved production mock contains 23 regenerated desktop/mobile PNGs. Phase 181, pre-recovery Phase 186, and pre-alignment Phase 187 references remain archived and were not deleted.
9. Local completion evidence is 422/422 no-browser tests, 25/25 TraceCue Playwright browser tests, passing mock interaction/responsive checks, and passing structure, document, security, design-system, workflow, package, repository, and product gates. Independent contract, exact-once, and browser-concurrency reviews have no remaining findings.
10. The parent lesson repository and FrameCue were read only and were not modified by this work. Complete GitHub CI, local/remote equality, and exact-HEAD evidence against the final tracked commit before reporting completion.
11. Keep stop conditions active: no npm publication, package/license changes, marketplace registration, arbitrary provider or endpoint expansion, MCP permission expansion, external upload, cleanup outside the artifact root, shell execution, browser profile reuse, persistent credential storage, or parent/consumer repository mutation without explicit approval.
