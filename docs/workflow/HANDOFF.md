# HANDOFF.md

## Current State

Playwright Test regression evidence integration is implemented as a disabled-by-default, advisory-only CLI/API/Control Center surface. The current slice adds mode settings for `disabled`, `import_only`, `local_run`, and `external_ci`, local artifact import, normalized-result-only E2E review-material projection with non-engineer review cards, evidence-quality limits, baseline comparison, and standard/deep/xhigh review inputs, CLI-only explicit local-run planning/execution with plan-hash gating, read-only GitHub Actions artifact retrieval through `gh run list/view/download`, approved external-CI fetch settings with latest-successful-run resolution, schemas, focused tests, Control Center Regression/Settings UI, and design-system component metadata. Control Center read-model generation remains body-free and must not spawn processes, launch browsers, call `gh`, contact networks, download artifacts, read raw artifact bodies, or scan heavily. Playwright Test evidence remains local and must not mutate TraceCue review findings, Agentic Human Review proof, release gates, product gates, MCP exposure, or remote CI state.

TraceCue Control Center source intake and display-language settings are implemented as bounded local browser actions on top of the read-only dashboard model. `/api/dashboard` remains GET-only and body-free. `/api/source-intake/proposal` requires an explicit confirmation token, accepts only workspace-relative source text and optional evidence/index paths, reuses the Agentic Human Review proposal path, writes local non-executing proposal artifacts, and suppresses full source text and chunk text from normal UI output. `/api/settings/display-language` requires an explicit confirmation token and writes only the Control Center display locale to the fixed `ops/DASHBOARD_SETTINGS.json` path. The React/Vite app now has Intake and Settings views, uses the product-local design system and locale policy, and still exposes no provider execution, shell execution, MCP JSON-RPC, browser automation, cleanup execution, external upload, or gate mutation.

Agentic Human Review dogfood evidence-pack summarization and review-pack projection are implemented as read-only owner-review surfaces. `agentic review dogfood evidence-pack summarize` accepts a workspace-confined dogfood evidence-pack manifest, evidence-set manifest, evidence-set output, or supported runtime wrapper, reuses evidence-set, claim-readiness, longitudinal-quality, claim-standard-gate, and owner-review-context logic, and emits `agentic_human_review_dogfood_evidence_pack_summary` with matrix status, owner-review digest, claim-review status, and advisory regeneration handoff. `agentic review dogfood evidence-pack review-pack` reuses the same preparation path and emits `agentic_human_review_dogfood_review_pack` with owner-facing status, standard/deep/xhigh matrix badges, grouped blockers, top owner actions, trust/safety flags, and pathless advanced references. The outputs suppress detailed result paths, source paths, raw hash values in review packs, raw provider responses, credential values, full source text, chunk text, candidate/reference prose, and concrete rerun commands. They do not call providers, read credentials, transfer evidence externally, write artifacts, launch browsers, expose MCP execution, automatically rerun commands, mutate deterministic findings, change release gates, satisfy proof contracts, or authorize human-equivalent or human-superior claims.

Agentic Human Review source-text quality owner-review context is implemented as an optional downstream diagnostic projection. Evidence-set summaries can reference a workspace-confined `agentic_human_review_source_text_quality` artifact, including supported CLI/API wrappers, and carry a whitelist-only `owner_review_context.source_text_quality` into evidence-set, human-baseline claim-readiness, longitudinal-quality, claim-standard-gate, and evidence-regeneration outputs. Existing evidence-set output context is treated as untrusted and re-sanitized before downstream propagation, unsafe provider/proof/claim/release/artifact-write/rerun authority flags are neutralized, and the public owner-context schema types the allowed source-text quality projection. The context exposes only bounded counts, statuses, booleans, effort names, pass-condition summaries, output-safety summaries, context diagnostics, and regeneration freshness by effort. It suppresses full source text, chunk text, candidate or reference prose, result paths, source locators, source titles, private source identity values, raw hashes, provider responses, and credentials. It does not change evidence-set warnings, claim-readiness conditions, claim-standard blockers, pass/fail state, equality or superiority claim states, proof status, release gates, provider execution approval, artifact-write permission, automatic rerun authority, or future claim-numerator eligibility.

Agentic Human Review source-text effort-matrix verification is implemented as a read-only local diagnostic surface. `agentic review quality source-text` requires workspace-confined standard, deep, and xhigh Agentic Human Review result artifacts generated with source-text metadata, optionally accepts a workspace-confined reference review, and emits `agentic_human_review_source_text_quality` with source-understanding completion, source-reading/source-understanding counts, source-type consistency, bounded same-source identity invariant status, source-reading/source-understanding source id consistency, editorial synthesis hashes and scores, pairwise effort deltas, xhigh counterpoint/evidence-limit/conclusion-change readiness, optional reference-review comparison summaries, output-safety diagnostics, warnings, and boundary flags. The output suppresses full source text, chunk text, direct raw source-text aliases, candidate full reviews, reference review prose, private source identity values, result paths, source locators, source titles, provider responses, credentials, and MCP execution. It does not call providers, write artifacts, mutate deterministic findings, change release gates, satisfy proof contracts, or authorize human-equivalent or human-superior claims.

Agentic Human Review editorial-quality comparison is implemented as a read-only local comparison surface. `agentic review compare --comparison-kind editorial-quality` accepts a workspace-confined reference review text or JSON artifact as the baseline and a candidate Agentic Human Review result, then compares the candidate `editorial_synthesis.full_review` against effort-aware reference targets. Output includes hashes, scores, deltas, strengths, gaps, target status, and diagnostics only; reference prose, candidate prose, source text, local paths, and source locators are not emitted. Unsafe raw media, binary, full-source, or credential-bearing reference inputs fail closed. This does not call providers, transfer evidence, expose MCP execution, mutate deterministic findings, change release gates, satisfy proof contracts, or authorize human-equivalent or human-superior claims.

Agentic Human Review source-text reading and source understanding are implemented as an additive full-source understanding layer for editorial synthesis. Proposals and plans can accept optional workspace-confined `--source-text` plain text or JSON for video transcripts, web-page text, PDF-extracted text, meeting notes, documents, transcripts, and other textual artifacts. TraceCue reads the full source text locally, chunks and hashes it without persisting full source text or chunk text, derives an effort-aware `source_reading_review` plus `source_understanding_review`, and lets editorial synthesis combine the full-source understanding with existing TraceCue findings, role opinions, recommendations, video evidence, and content evidence through an `editorial_integrator` record. The understanding layer records thesis, audience promise, narrative arc, turning points, must-not-miss points, tensions, limitations, reviewer implications, bounded evidence claims, assistant-reference quality target, and coverage metrics so the final review is not limited to summary-only material. The natural full-review composer now builds a target-independent editorial narrative plan from source understanding and cross-checks it against existing normalized AHR findings, reader-impact records, role opinions, and action-plan signals before composing prose. `standard` stays practical and source-grounded, `deep` adds audience value, concrete examples, and prioritization, and complete `xhigh` adds critique, verification, counterpoints, evidence limits, and conclusion-change conditions. The composer suppresses fake-provider scaffolding, `Step`/`role` markers, operational effort labels, quality-target labels, duplicate or near-duplicate source anchors, target-specific heuristics, and boundary boilerplate from the review body while keeping them in structured metadata. No-browser matrix coverage verifies standard/deep/xhigh source-text synthesis across video, web page, PDF, meeting notes, and document source types. Markdown reports now include `Source Reading` and `Source Understanding` sections with bounded key points, examples, cautions, must-not-miss points, source claims, and source excerpt refs. Report-quality now exposes source-understanding, grounded-claim, evidence-ref-resolution, useful-recommendation, and assistant-reference-quality diagnostics. Provider payloads can include only bounded source-reading and source-understanding review data under the existing page-text transfer boundary, and source-understanding refs are compacted to ids and hashes without excerpt text or source locators; raw media, raw binaries, base64/blob/data URI payloads, credential fields, full source text, chunk text, and raw/full structured fields are rejected. This does not implement FrameCue, add provider calls, store full transcripts or full documents, transfer full source text, expose AHR through MCP, mutate deterministic findings, change release gates, satisfy owner-baseline or benchmark proof, or authorize human-equivalent or human-superior claims.

Agentic Human Review effort-aware quality diagnostics are implemented as an additive report-quality classification layer. Embedded advisory `report_quality` and standalone `agentic review report-quality` now expose optional effort expectations, structured quality diagnostics, classification summaries, effort notes, and evaluator policy diagnostics. Missing dedicated critique or verification output is an expected effort gap for standard and deep results because those roles are not planned there, while incomplete xhigh still emits policy warnings, maturity gaps, and strict missing-condition diagnostics. Markdown reports separate effort notes from quality warnings and localize TraceCue-generated diagnostic chrome through artifact output language settings. This does not add standard/deep roles, change scoring formulas, call providers, store raw provider responses, expose MCP, satisfy proof contracts, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review generic content evidence intake and localized report chrome are implemented as an additive bounded-evidence extension to editorial synthesis. Proposals and plans can accept optional workspace-confined `--content-evidence` JSON for video, web page, PDF, meeting-note, document, transcript, or other content sources, while `--video-evidence` remains a compatibility input that is projected into generic supplemental `content_evidence`. The intake normalizes analyzer-neutral summaries, bounded content units, claims, limitations, coverage, and provenance; rejects raw media, raw binaries, base64/blob/data URI payloads, raw HTML/PDF bytes, full documents, full transcripts, and truthy raw/full privacy or boundary declarations while accepting explicit false-valued privacy or boundary metadata that declares raw/full content is absent; carries path-free content-evidence scope, quality, source-type display labels, bounded-evidence density, review-strength guidance, and source references into plans, packages, provider payload filters, advisory results, schemas, report-quality diagnostics, and Markdown reports; and lets editorial synthesis distinguish page-only review from page-plus-content-evidence review. TraceCue-owned Agentic Human Review report headings, labels, fallback text, composer connective phrases, source-type display labels, source-text preservation explanations, effort stance, xhigh stance, and evidence-scope lines resolve through artifact output language settings while provider-authored advisory text and source evidence text are preserved. This does not implement an analyzer, change FrameCue, download or upload remote content, call providers during planning, translate provider output or source evidence, store raw provider responses, expose MCP, create proof, satisfy owner-baseline or benchmark contracts, authorize claims, or mutate deterministic findings or release gates.

Agentic Human Review editorial synthesis is implemented as a local derived view over normalized advisory results. New results include optional `editorial_synthesis` data and Markdown reports render an `Editorial Synthesis` section with source-attributed observations, tensions, strengths, cautions, recommended direction, owner-decision context, bounded language-setting metadata, source-text preservation policy, and limited-output warnings when the result is too sparse. When usable generic `content_evidence` is present, the local composer builds the full review from bounded content summaries, excerpt units, observed claims, limitations, and existing advisory signals before generic run summaries, classifies content-evidence density to keep summary-only or metadata-only conclusions cautious, reflects standard/deep/xhigh effort stance without promoting non-xhigh output into verification, suppresses template-only paragraphs and near-duplicate sentences, and applies the same path to videos, web pages, PDFs, meeting notes, documents, transcripts, and other source types without target-specific branches. The synthesis resolves TraceCue-local artifact output language settings across the supported 14-locale contract and records source-text preservation when translation execution is disabled. The synthesis is not requested from providers or adapters, does not call APIs, does not translate source evidence or provider output, does not read raw pixels or credentials, does not store raw provider responses, does not expose MCP, does not create proof, does not satisfy owner-baseline or benchmark contracts, does not change report-quality metrics, and does not mutate deterministic findings or release gates.

Agentic Human Review owner-baseline label-obligation enforcement is implemented for reusable real-provider proof recovery. The Responses adapter now derives `required_owner_baseline_findings` from every required owner label in the approved baseline contract, preserves target-specific criterion fallback obligations only when a criterion has no linked owner label, validates canonical `owner_baseline_findings` against exact owner label ids, linked criterion ids, and catalog-backed evidence references, and filters repair templates by missing owner label ids. Human-baseline comparison now prefers evidence-backed exact owner-label or criterion findings over earlier broad unbacked text matches. This does not synthesize owner-label proof from prose, benchmark coverage rows, coverage patch output, target URLs, role-level findings, generic advisory findings, raw artifacts, raw provider responses, request payloads, credentials, or target-specific branches, and it does not expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review coverage patch repair is implemented for reusable real-provider dogfood recovery. After at least one full replacement repair attempt, when the only remaining contract failure is absent benchmark coverage rows, the Responses adapter can issue one bounded provider-authored coverage patch request with redacted prior-advisory context, exact missing-row targets, and allowed evidence-reference ids. The adapter merges only exact missing labels with catalog-backed evidence references, marks provider patch provenance, and re-runs the same fail-closed benchmark, owner-baseline, claim, and effort validation. This does not synthesize required mentions, required dimensions, owner-baseline findings, or prose-derived coverage, does not repair invalid existing rows with unknown evidence references, does not store raw provider responses or credentials, does not expose Agentic Human Review through MCP, does not authorize human-equivalent or human-superior claims, and does not mutate deterministic findings or release gates.

Agentic Human Review provider-output JSON wrapper recovery is implemented for reusable real-provider dogfood recovery. The Responses adapter now parses provider output in memory by accepting direct advisory JSON first, then only one unambiguous provider-authored advisory JSON object from JSON-string encoding, a single JSON Markdown fence, or prose wrapped around one balanced JSON object. Malformed JSON, multiple JSON candidates, arrays, primitives, non-JSON fences, and non-advisory output still fail closed. This does not increase repair attempts, synthesize advisory content, relax benchmark, owner-baseline, xhigh, claim, or evidence-reference validation, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review forbidden-claim absence completion is implemented for reusable real-provider dogfood recovery. The Responses adapter now requires nested benchmark coverage arrays in provider JSON schema when effective coverage is active, derives nested `minItems` from required coverage templates, includes path-oriented `coverage_repair_targets` in repair context, and after a provider repair attempt can complete only missing forbidden-claim absence rows from canonical required templates when the advisory does not assert the forbidden claim and a catalog-backed evidence reference is available. Completed rows are marked adapter-derived and advisory-only. This does not synthesize required mentions, required dimensions, owner-baseline findings, or prose-derived coverage, does not increase repair attempts, does not relax unknown-evidence-reference rejection, does not store raw provider responses or credentials, does not authorize human-equivalent or human-superior claims, does not expose Agentic Human Review through MCP, and does not mutate deterministic findings or release gates.

Persistent browser session Slices 0-8 are implemented locally as a generic retained-page workflow. TraceCue now has bounded full-profile `browser_debug_supervise`, persistent `session start/status/stop/act/observe/checkpoint/review`, manual-login checkpoints, local review handoff from the retained page, explicit storageState import/export confined to the configured artifact auth directory, admin-stdio-only MCP session tools, session-action and persistent-session schema coverage, parser/API/MCP tests, redaction coverage, architecture boundaries, and browser smoke coverage. The slice remains local-only: safe MCP, full MCP, and HTTP MCP do not expose persistent session tools; existing browser profiles are not reused; OAuth/password automation remains prohibited; storageState is not persisted by default; cookie/token values are not printed; and artifact roots remain local and ignored.

Agentic Human Review owner-baseline canonical proof enforcement is implemented for reusable real-provider dogfood recovery. Responses adapter validation now accepts target-specific owner-baseline proof only from canonical `owner_baseline_findings` records with required criterion ids, owner label ids, and catalog-backed evidence refs; role-level findings, generic advisory findings, and prose can no longer satisfy that proof contract by themselves. Normalized advisory results retain `owner_baseline_findings`, merge those records into comparison-visible `agentic_human_review_findings`, preserve safe evidence-reference aliases, and keep missing canonical proof repairable. This does not increase repair attempts, synthesize proof from prose, weaken benchmark or xhigh validation, store raw provider responses or credentials, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review staged placeholder repair context is implemented for reusable real-provider dogfood recovery. The Responses adapter now treats role outputs as placeholders when they are explicitly marked, reported as not provider-authored, or summarized with role-output did-not-return/not-available/missing-output language; such outputs no longer count as reported role/round coverage, while ordinary page-content unavailable-data findings remain valid output. Aggregate contract diagnostics and repair retries now carry bounded `placeholder_outputs` metadata with stage id, stage kind, role, round, reason, and planned-role match so the provider can replace the exact role output. This does not increase default repair attempts, synthesize role opinions, accept placeholder output as proof, weaken owner-baseline or benchmark validation, store raw provider responses or request payloads, persist credential values, add target-specific branches, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review Responses adapter contract recovery is implemented for reusable real-page standard/deep/xhigh dogfood. Adapter post-validation now aggregates benchmark, owner-baseline, optional claim, and full or staged effort contract failures before repair; repair context is missing-only and carries filtered required templates plus recommended local evidence-reference ids; staged adapter validation checks the current stage roles/round and final-stage synthesis; staged and loopback adapter failures persist only allowlisted diagnostics in execution records and run receipts; and the adapter CLI exposes request/response size limits in help and startup JSON. This does not increase default repair attempts, synthesize proof from prose, weaken owner-baseline or benchmark validation, treat intermediate stages as final evidence, store raw provider responses or request payloads, persist credential values, add target-specific branches, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review adapter claim candidate filtering is implemented for reusable real-provider dogfood recovery. Provider-authored `review_claims` are treated as optional proof candidates: unsupported, placeholder, unknown-role, equality, and superiority candidates are filtered before TraceCue result normalization, safe filtering diagnostics are recorded in adapter responses and advisory results, and `claim_integrity` keeps any run with rejected candidates out of future claim-numerator evidence. Staged final synthesis may use prior normalized stage roles as claim support, while stage outputs remain non-final evidence. This does not retry providers solely for optional claim candidates, synthesize replacement claims, weaken owner-baseline or benchmark validation, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review staged effort provider execution is implemented for reusable long real-provider dogfood. `agentic review run --execution-mode staged` is accepted for approved `standard`, `deep`, and `xhigh` plans and splits provider work by planned effort roles under the same plan hash, exact transfer flags, provider/model/surface contract, live dogfood gate, and advisory-only boundary. Intermediate stage outputs are stored only as normalized stage metadata for final aggregation and cannot become final proof, calibration evidence, comparison evidence, claim evidence, release approval, or human-equivalence/superiority evidence by themselves. Responses adapter payloads now carry compact stage context with no local paths, raw provider response storage, credential-value recording, tools, or persistent storage. One-shot execution remains the default. This does not automatically rerun providers, lower quality gates, weaken owner-baseline or benchmark validation, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review long dogfood HTTP transport hardening is implemented for reusable owner-contract provider runs. The generic API provider path and the Responses adapter upstream provider path now use a dependency-free repository-local bounded HTTP(S) transport by default, preserving injected test transports while avoiding hidden bundled fetch header-timeout defaults during slow response-header waits. Generic-provider request failures now include only allowlisted cause-name and cause-code diagnostics, and the adapter server keeps request/header/socket timeout policy explicit for long dogfood. This does not change timeout defaults, automatically rerun providers, store credential values or raw provider responses, expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline recovery and adapter timeout diagnostics are implemented for reusable real-page proof-data recovery. Human-baseline comparison now records whether the candidate result was generated with a matching owner-baseline requirement contract; evidence-set comparison records preserve that contract state; claim-standard-gate rerun plans add approval-required provider result targets when owner-baseline comparison cannot be fixed by local comparison regeneration alone; evidence regeneration planning resolves those targets only from matching owner-contract plans or explicit target-registry rows; future advisory results retain the path-free owner-baseline requirement contract; the Responses adapter reports its effective upstream provider timeout and returns only allowlisted upstream failure diagnostics. This does not automatically rerun providers, change default timeout values, store credential values or raw provider responses, expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review provider model-resolution hardening is implemented for reusable live-provider dogfood recovery. The generic API provider now declares runtime model environment metadata, abstract model ids, and a model-resolution policy; `agentic review run` resolves a concrete provider model before API payload construction and fails before fetch with `AGENTIC_REVIEW_PROVIDER_MODEL_UNRESOLVED` when only an abstract planning model is available. Successful runtime-model fallback records non-secret `model_resolution` metadata in payloads, execution records, receipts, and advisory results. The Responses adapter now reports source-aware model-resolution diagnostics and rejects abstract request models before provider fetch. This does not hard-code an upstream model, remove provider-neutral planning placeholders, store credential values or raw provider responses, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review claim-audit forbidden-claim provenance hardening is implemented for reusable owner-claim proof diagnostics. Claim audit now treats evidence-backed absence or coverage statements about configured forbidden claims as non-blocking diagnostics while keeping affirmative or ambiguous forbidden policy text as blocking claim-audit findings, and future derived `review_claims` avoid elevating forbidden-claim compliance findings into proof claims. This preserves strict forbidden-claim policy enforcement without weakening owner-baseline absence checks, claim-standard-gate fail-closed behavior, human-equivalent or human-superior claim blocking, provider execution boundaries, raw provider response storage boundaries, credential boundaries, MCP exclusion, deterministic review separation, or release gates.

Agentic Human Review Responses adapter provider payload compaction is implemented for reusable real-provider dogfood recovery. The adapter now keeps the original inbound TraceCue request for fail-closed post-validation while deriving a compact provider-facing `review_request` view that preserves plan hash, effort, roles, rounds, xhigh strict-output requirements, effective benchmark requirements, compact owner-baseline criteria, `required_benchmark_coverage`, `required_owner_baseline_findings`, `required_owner_baseline_coverage`, and catalog-backed evidence-reference ids. Duplicated long owner-baseline arrays, nested provider-instruction owner contracts, duplicated plan/package text and visual contracts, long owner summaries, and local paths are removed from the provider-facing request; generated request-size failures now include non-secret section byte counts. This does not increase request-size limits, accept prose-only owner evidence, weaken unknown evidence-reference rejection, synthesize proof records, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline effective coverage contract hardening is implemented for reusable real-provider dogfood recovery. Approved owner-baseline required mentions, required dimensions, and forbidden claims are now merged into the effective benchmark coverage contract, exposed to the Responses adapter as compact `required_benchmark_coverage` and `required_owner_baseline_coverage` templates, prioritized in the evidence-reference catalog, included in bounded repair context, and required as exact evidence-backed `benchmark_requirement_coverage` rows after provider normalization. Owner-baseline forbidden claims remain structured absence checks. This does not lower thresholds, synthesize proof from prose, increase repair attempts, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline adapter contract repair hardening is implemented for reusable real-provider dogfood recovery. The Responses adapter now derives compact required owner-baseline finding records from the approved baseline contract and request evidence catalog, includes those records in provider payloads and bounded repair context, preserves recommended evidence-reference ids in repair diagnostics, normalizes provider-authored structured findings from top-level advisory arrays, owner-baseline finding arrays, and planned role findings, and keeps unknown owner labels, unknown evidence references, and free-text-only baseline discussion fail-closed. This does not increase repair attempts, synthesize findings from baseline text, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review evidence regeneration planning is implemented as a read-only recovery planner for failed owner-claim proof data. `agentic review evidence-set regenerate plan --evidence-set <workspace-json> --claim-gate <workspace-json> [--target-registry <workspace-json>] --json` consumes evidence-set and claim-standard-gate artifacts, resolves provider rerun plan paths, plan hashes, and transfer flags only after validating an explicit approved-plan registry row or the generic result -> execution -> plan artifact chain, classifies provider-result repair, claim-audit, calibration, comparison, human-baseline comparison, and downstream summary/readiness/longitudinal/gate targets, and emits dependency-ordered command templates without executing them. Missing or tampered approved-plan data stays unresolved with warnings instead of producing a trusted provider-rerun command. Missing calibration cells are now included in the claim-standard-gate rerun plan. This does not authorize provider execution, automatic reruns, artifact writes, browser launch, MCP exposure, human-equivalent or human-superior claims, deterministic review mutation, or release-gate mutation.

Agentic Human Review proof-claim integrity and minimal rerun planning are implemented as reusable proof-quality diagnostics. Normalized advisory results now expose `claim_integrity`, strict claim-numerator eligibility rejects unsupported, placeholder, equality, or superiority claim text, and the Responses adapter validates or repairs `review_claims` only when they are backed by catalog evidence references or planned role output. Direct-vs-TraceCue and human-baseline comparisons now expose metric-level regressions, critical regressed metrics, missing owner labels, missing target-specific must-not-miss criteria, and evidence-backed forbidden-claim absence gaps. The claim standard gate now carries claim-integrity audit failures into blockers and emits a read-only minimal rerun plan with command templates instead of executing reruns. This does not authorize provider execution, automatic reruns, artifact writes from the gate, human-equivalent or human-superior claims, MCP exposure, raw provider response storage, credential storage, deterministic review mutation, or release-gate mutation.

Agentic Human Review claim-standard-gate enforcement is implemented as a read-only mechanical gate. `agentic review claim standard-gate --evidence-set <workspace-json> [--policy <workspace-json>] --json` now consumes an evidence set, reuses claim-readiness, longitudinal quality, comparison, owner-baseline, xhigh, synthetic/local evidence exclusion, and per-result claim-audit diagnostics, returns `owner_claim_review_ready` only when all conditions pass, and exits non-zero with blockers when the gate fails. Human-equivalent and human-superior claim states remain false. The existing local proof packet under `.browser-debug/target-url-ahr-evidence/2026-06-28T18-46-38-215Z-owner-baseline-contract/claim-review-proof-packet-2026-06-29.md` remains owner-review evidence, not equality or superiority authorization. This does not authorize provider execution, evidence transfer, MCP exposure, raw provider response storage, credential storage, deterministic review mutation, or release-gate mutation.

Agentic Human Review proof-readiness normalization and repair diagnostics are implemented. Evidence-set summaries now unwrap supported TraceCue CLI and public API runtime-result wrappers for calibration, comparison, and human-baseline artifacts while preserving raw advisory-result validation and rejecting run-runtime pointers as result evidence. Direct human-baseline comparison also accepts supported validation wrappers by extracting and revalidating the embedded baseline contract. Claim-readiness now reports separate blocker categories for missing results, mechanical incomplete results, failed calibration, and missing comparisons. The Responses adapter now requires owner label ids for owner-baseline criteria with owner labels, prioritizes owner-baseline and benchmark evidence catalog ids before optional artifact refs, keeps provider instructions and external provider payloads size-bounded by using compact contract views, treats `present=true` plus absence wording in forbidden-claim records as a repairable semantic contradiction, and sends repair attempts only redacted criterion, owner-label, required-field, forbidden-claim semantics, xhigh-condition, and allowed evidence-reference hints. This does not authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, weaken existing validations, or change release gates.

Agentic Human Review owner-baseline contract propagation is implemented for reusable proof-quality dogfood. Approved owner-labeled human baselines can now be supplied to `agentic review propose` and `agentic review plan` through `--human-baseline`; unapproved, synthetic, incomplete, or case-mismatched baselines fail closed; verified baselines become path-free owner-baseline requirement contracts in proposals, plans, plan hashes, provider instruction contracts, review-quality benchmark contracts, result contracts, and generic-provider payloads. The Responses adapter now requires structured findings for every required owner-label obligation and target-specific criterion fallback with owner label ids, linked criterion ids when present, and catalog-backed evidence references, and human-baseline comparison prefers evidence-backed exact owner-label or criterion findings while text-only matches remain insufficient. This does not create owner approvals, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review proof evidence aggregation and adapter benchmark contracts are hardened for repeatable real-provider dogfood. Evidence-set summaries now accept TraceCue CLI runtime-result wrappers for calibration, comparison, and human-baseline artifacts through explicit data-key unwrapping, while preserving raw result artifact validation. The Responses adapter now treats forbidden-claim `present` as claim presence rather than coverage, requires explicit absence confirmation, requires catalog-backed evidence references, rejects unknown provider-supplied evidence-reference ids, and keeps repair retries bounded. This does not create owner-approved baselines, complete the direct-vs-TraceCue matrix, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review now enforces `xhigh` as an observable mechanical effort contract instead of relying only on provider prompt wording. Provider capability snapshots, approved plans, API payloads, Responses adapter requests, advisory results, report-quality output, xhigh planning/simulation, evidence-set summaries, human-baseline comparisons, claim-readiness diagnostics, and longitudinal quality rollups now expose native effort binding, strict structured-output requirements, placeholder rejection, required role/round/critique/verification/synthesis coverage, structured benchmark records with local evidence-reference identifiers, repair-readiness metadata, multi-step execution readiness, and strict claim-numerator exclusion when the mechanical contract is incomplete. This does not claim human-equivalence or human-superiority, force provider-internal reasoning beyond public API controls, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review proof-readiness hardening is implemented for the real-provider dogfood pipeline. The slice strengthens the loopback Responses adapter output contract for benchmark-enabled requests, preserves structured advisory findings with evidence-reference identifiers, normalizes safe provider aliases without fabricating positive coverage, and makes evidence-set/claim-readiness diagnostics report explicit benchmark-case by effort matrices, case-level `direct-vs-tracecue` gaps, and weak calibration blockers. This work does not create owner-labeled baselines, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, store raw provider responses or credential values, mutate deterministic findings, or change release gates.

Agentic Human Review proof-policy hardening is implemented for the human-baseline dogfood pipeline. Synthetic, deterministic, fixture-only, fake, injected, and local-pipeline markers now prevent owner-labeled baseline verification and are reported separately from real-provider claim-numerator eligibility. Owner-label comparison scoring now requires structured candidate findings with local evidence references; text-only matches remain diagnostic and produce insufficient-evidence warnings. Deterministic fake-provider dogfood now emits structured local-reference findings for pipeline validation only, and evidence-set/claim-readiness summaries report ready comparison coverage by benchmark case plus the missing real-provider dogfood matrix while keeping equality and superiority claim flags false.

Agentic Human Review now has a read-only owner-labeled human baseline lifecycle for the next dogfood phase. `agentic review human-baseline registry|overlay|draft|approval|validate|compare|claim-readiness` supports reusable rubric/templates, target-specific case overlays, AI baseline drafts that cannot become proof, owner approval packets, approval-metadata-backed baseline validation, direct-vs-TraceCue comparison classification, evidence-set comparison readiness, and claim-readiness diagnostics. These commands operate on workspace-confined JSON and existing advisory results without provider calls, artifact writes, MCP exposure, deterministic gate mutation, credential reads, AI-draft proof, or equality/superiority claim authorization. Evidence-set summaries and longitudinal rollups now report owner-labeled baseline counts, ready human-baseline comparison counts, and missing baseline benchmark cases.

Agentic Human Review human-baseline approval now fails closed on missing target-specific must-not-miss criteria. Approval packets only produce `owner_labeled: true` when the draft includes target-specific must-not-miss criteria, every target-specific criterion is linked to a required owner label, and the linked owner label has local evidence references. Human-baseline validation repeats the same check for hand-written inputs, and human-baseline comparison scores target-specific must-not-miss coverage separately before reporting ready-for-owner-review. Generic benchmark criteria alone are no longer enough to create comparison-ready owner evidence.

Agentic Human Review now includes the first quality-gate foundation for moving standard/deep/xhigh real-page dogfood toward repeatable review evidence. Benchmark plans require structured `benchmark_requirement_coverage`, calibration now scores evidence-backed required mentions, required dimensions, and forbidden claims, `risk_and_misleading_content` is an explicit human-review dimension, and read-only diagnostics cover evidence-set validation/summary, batch comparison, evaluator policy warnings, xhigh round planning/simulation, longitudinal quality rollups, claim policy/audit checks, and claim-standard-gate enforcement. These diagnostics do not call providers, read credential values, write artifacts, expose MCP execution, mutate deterministic findings, change release gates, or authorize human-equivalent or human-superior claims.

Agentic Human Review generic API provider timeout is now optionally configurable through `AGENTIC_HUMAN_REVIEW_API_TIMEOUT_MS` for approved live dogfood runs that need more than the 30000 ms default. The timeout value is part of the provider capability contract and plan hash approval path, so changing it requires a fresh plan and cannot bypass exact transfer flags, credential non-disclosure, raw-provider-response non-storage, advisory-only output, no-MCP-execution, or no-release-gate-mutation boundaries.

Agentic Human Review now includes a loopback-only Responses-compatible adapter for manual live provider dogfood. The adapter is started separately with `npm run ahr:responses-adapter`, accepts the existing `generic-api-provider` request contract, keeps the inbound adapter token separate from the upstream provider key, builds no-store/no-tools Responses requests, strips or rejects local path and raw-pixel disclosures, parses provider output text into normalized advisory JSON, and stores no raw provider response or credential values. Existing AHR plan-hash, exact-transfer-flag, explicit `--execute`, advisory-only, no-MCP, no-release-gate-mutation, and deterministic-review-separation boundaries remain unchanged.

The manual live upstream dogfood run through the loopback adapter passed with `generic-api-provider`, `gpt-5.4-nano`, and `quick` effort. `agentic review run` completed with a real upstream API call, `external_evidence_transfer=true`, `raw_provider_response_stored=false`, `credential_values_recorded=false`, `gate_effect=none`, and `advisory_only=true`; `agentic review report-quality` returned `status=ok` with quick-effort warnings only. The adapter was stopped and port 8787 was confirmed closed.

Agentic Human Review now has read-only human-review maturity diagnostics for the next dogfood phase. `agentic review dogfood readiness` and `agentic review dogfood plan` expose the standard/deep/xhigh maturity plan and benchmark-case matrix. `agentic review report-quality` exposes `human_review_maturity` and `longitudinal_quality_evaluation`, including missing effort modes, missing benchmark cases, comparison/history requirements, and explicit no-claim flags for human-equivalent or human-superior judgment. These diagnostics call no providers, write no artifacts, expose no MCP execution, mutate no deterministic gates, and do not authorize live evidence transfer.

Agentic Human Review roadmap AHR-25-40 is implemented as the completion-readiness layer on top of AHR-13-24. TraceCue now has Visual Evidence Package v2 and Visible Text Reading contracts, manual-only `agentic review dogfood readiness`, read-only `agentic review dogfood plan`, explicit live dogfood opt-in metadata, transfer approval previews, xhigh round-plan v2 metadata, Quality Evaluator v3, Human Report v3, provider failure diagnostics in execution receipts, an `article-comprehension-risk` benchmark case, and `agentic review compare --comparison-kind direct-vs-tracecue`. Dogfood readiness and planning perform no provider calls, no credential-value reads, no evidence transfer, no browser launches, no artifact writes, and no gate mutation. Real provider execution still goes only through approved `agentic review run` with matching plan hash, package hash validation, provider capability hash, exact transfer flags, and explicit `--execute`.

Agentic Human Review roadmap AHR-13-24 is implemented as the provider dogfood, benchmark calibration, and orchestration-quality layer on top of AHR-01-12. TraceCue now records provider capability contracts and hashes in readiness/plans/payloads/results, rejects provider capability drift before approved runs, plans transferable evidence by class with explicit raw-byte exclusions, records rubric profiles and privacy disclosure audits, hardens provider API endpoint policy against URL credentials, sensitive query credentials, redirects, unsupported HTTP, oversized streaming responses, and unbounded raw response storage, and exposes read-only `agentic review benchmark list/show`, `agentic review calibrate`, and `agentic review compare` commands. Result artifacts now include role instruction coverage, consensus/dissent analysis, review-quality evaluation, calibration metadata, and privacy disclosure audits. Deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, raw provider response storage, and credential storage remain unchanged.

Agentic Human Review roadmap AHR-01-12 is implemented as a strengthening layer on top of Slices 26-42. TraceCue now emits `human_review_schema_version` v2 contracts, explicit human-review dimensions, provider instruction contracts, review-quality benchmark contracts, reader-experience review, mechanical-versus-human comparison, human-review coverage, actionability scoring, and richer non-engineer-readable Markdown reporting. Provider readiness now validates plan artifacts, report-quality validates advisory result contracts and optional execution/result pairing, proposal parsing rejects conflicting content sources, and generic API provider execution rejects non-loopback HTTP endpoints while omitting plan/execution paths and deterministic review paths from transferred payload metadata. Deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, raw provider response storage, and credential storage remain unchanged.

Agentic Human Review roadmap Slice 34-42 is implemented on top of Slice 26-33. TraceCue now supports `agentic review propose`, `agentic review plan --proposal`, `agentic review provider-readiness`, generic API provider execution through the existing approved `agentic review run` path, `agentic review report-quality`, proposal/provider-readiness/report-quality schemas, provider adapter isolation, role execution records, claims, critique/rebuttal/integration records, dogfood metadata, and report-quality metadata. Proposal and readiness commands do not authorize execution or transfer; run still requires matching plan hash, explicit `--execute`, package-hash validation, and exact transfer flags; Agentic Human Review remains advisory-only and excluded from safe/full/admin MCP profiles. This slice does not mutate deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, browser capture, OS capture, package publication, parent-repository files, or consumer-repository files, and it does not store raw provider responses or credential values.

Slices 6-25 / Phase 79-155 are complete as local bounded implementation surfaces on top of Slice 5 provider MCP execution/status-list hardening, Phase 71-73 operation provider readiness, Phase 69-70 operation admin readiness, Phase 65-68 operation policy/readiness foundation, Phase 61-64 operation contracts, Phase 60 operation registry, and Phase 60.1 operation roadmap boundary contracts. TraceCue now has cleanup plan hardening, capture readiness/no-capture gates, provider-free localization and translation readiness, local release readiness, artifact-root policy and migration readiness, legacy alias audit/removal readiness, constrained shell readiness, and final hardening readiness through CLI/API/MCP-safe reports, schemas, registry/capability metadata, and fail-closed execution boundaries. This implementation does not promote Phase 60-155 into `docs/product/` commitments and does not authorize npm publication, real artifact-root migration, legacy alias removal, constrained shell execution, capture execution, translation execution, remote CI triggering, HTTP `full` or `admin`, socket transport, parent-repository changes, or consumer-repository changes.

Phase 59 local language settings foundation is complete on top of Phase 58 remote repository rename completion. TraceCue now has repository-local dashboard settings, a 14-locale policy contract, separate dashboard display locale and artifact output language roles, read-only `settings show`, `settings language`, and `settings language policy` CLI/API surfaces, safe MCP inspection, language settings schema parity, review/dashboard `language_settings` metadata, and provider-free translation boundaries. This phase is additive and does not change existing review findings, metrics, action plans, quality signals, gates, MCP write/execute permissions, provider execution, artifact roots, identity aliases, parent-repository files, or consumer-repository files.

Phase 58 remote repository rename completion is complete on top of Phase 57 physical checkout rename completion, Phase 56 rename-readiness audit, Phase 55 multi-agent visual review aggregation hardening, Phase 54 MCP visual review exposure reporting, Phase 53 read-only visual review aggregation, Phase 52 desktop review provider safety bridge, Phase 51 desktop image review body, Phase 50 desktop review provider-preparation planning, Phase 49 existing workspace image capture metadata handoff, Phase 48 screen/window capture planning, Phase 47 MCP execution gate policy, Phase 46 visual review dashboard integration, Phase 45 CLI visual review execution, Phase 44 local visual review result preparation, Phase 43 standalone image review, Phase 42 visual review provider policy planning, and Phase 41 visual evidence metadata. The local checkout directory is now `trace-cue`, GitHub repository URL is now `https://github.com/xxxMasahiro/trace-cue`, and local `origin` points at the canonical remote. Artifact-root migration, legacy-alias removal, npm publication, and marketplace registration remain separate boundaries.

Phase 40 TraceCue identity migration is locally complete for canonical product/package/bin/plugin/MCP identity. The approved migration changes the canonical identity to TraceCue / `trace-cue` while retaining `browser-debug`, `browser-debug-mcp`, existing `browser_debug_*` MCP tool names, and `.browser-debug/` artifact-root compatibility for this slice.

Phase 39 consumer runtime-readiness guidance is implemented as a documentation-only dogfood follow-up on top of Phase 38. It documents that consumer reviews need the target app's full local runtime and that missing API/backend state can correctly surface as `needs_attention` or browser-health findings.

TraceCue has completed Phase 1, Phase 2a package/runtime design verification, the Phase 5 local MVP runtime slice, the Phase 7 local review-platform implementation, the Phase 8 local dogfood/plugin-readiness implementation, the Phase 9 local review-quality implementation, the Phase 10 local dogfood route-readiness implementation, the Phase 11 local page-expectation review implementation, the Phase 12 local rendered-state dogfood hardening implementation, the Phase 13 local dogfood signal refinement implementation, the Phase 14 local content UX advisory implementation, the Phase 15 local content UX heuristic strengthening implementation, the Phase 16 local content UX handoff output implementation, the Phase 17 local content UX practical handoff implementation, the Phase 18 local content UX review brief/rubric implementation, the Phase 19 local target manifest validation implementation, the Phase 20 local resource status preflight implementation, the Phase 21-24 local resource safety implementation, the Phase 25 local agent advisory handoff implementation, the Phase 26 local agent request status implementation, the Phase 27 local agent request detail implementation, the Phase 28 local agent workflow status implementation, the Phase 29 agent execution integration, the Phase 30 release-hardening slice, the Phase 31 MCP profile-gating slice, the Phase 32 rename-readiness slice, the Phase 33 MCP read-only agent status slice, the Phase 34 safe HTTP MCP foundation slice, the Phase 35 HTTP MCP integration-hardening slice, the Phase 36 MCP capability policy slice, the Phase 37 external-repository usage quickstart slice, the Phase 38 local-checkout MCP config dogfood hardening slice, the Phase 56 rename-readiness audit slice, the Phase 57 physical checkout rename slice, and the Phase 58 remote repository rename slice. Phase 34 exposes an explicit `--transport http --profile safe` MCP endpoint that is loopback-only, bearer-token gated, Host/Origin validated, request-size bounded, and safe-profile-only while preserving stdio compatibility and no-browser, no-delete, no-provider, no-shell, and no-write/execute boundaries. Phase 36 adds read-only `mcp capabilities` CLI/API/MCP output for inspecting profile, transport, admin policy, and excluded-operation boundaries without enabling write/execute/admin operations. Phase 37 adds packaged consumer usage guidance so external repositories can connect through CLI, MCP stdio, safe HTTP MCP, or the Codex plugin without source inspection. Phase 38 adds generated `local_checkout` MCP config metadata so unpublished local checkouts can connect without source inspection or PATH assumptions. Phase 56 adds read-only identity audit and renamed-checkout guardrails before physical directory movement. Phase 57 completes the local filesystem checkout move to `trace-cue`. Phase 58 completes the GitHub repository rename and local origin update. Phase 0 scaffold and document sync are complete, local Git is initialized, the initial scaffold commit exists, local CI configuration is present, and product-gate evidence has been recorded locally.

Phase 35 HTTP MCP integration hardening adds token-free `browser-debug mcp config` output, reusable stdio and safe HTTP MCP client setup metadata, documentation for external-repository use, and packed-install HTTP initialize smoke coverage. Phase 36 MCP capability policy adds read-only `browser-debug mcp capabilities` output and `browser_debug_mcp_capabilities` MCP tool exposure through safe/full/admin profiles. Phase 37 adds `docs/workflow/CONSUMER_USAGE.md`, README routing, plugin skill routing, package file-set inclusion, and packed-install coverage. Phase 38 extends generated MCP config with `local_checkout` stdio and safe HTTP launch metadata for unpublished checkout use. It does not expand MCP tools into HTTP `full` or `admin`, socket transport, remote HTTP listeners, cleanup, provider/API, shell, safe/full/HTTP persistent session control, credential handling, external upload, or profile reuse.

This file is paired with `docs/workflow/TASK_TRACKER.md`. Keep the HANDOFF and TASK_TRACKER workflow-state pair synchronized whenever task state changes.

## Current Handoff

- Before future implementation work, complete and present the `A.
  Pre-Implementation Proposal` and `B. Implementation Plan` gate outputs for
  the current range before any file mutation or state-mutating operation.
  Non-trivial implementation or workflow-authority changes require multiple
  `xhigh`-class subagent reviews with consolidated accepted, deferred, and
  rejected findings. This protocol correction is workflow-only and does not
  change runtime behavior, product features, MCP exposure, provider execution,
  security boundaries, release gates, or existing verification commands.
- For Control Center, use the Intake page or `/api/source-intake/proposal` only for confirmed local proposal creation from workspace-confined source text; use the Settings page or `/api/settings/display-language` only for Control Center chrome locale. Provider runs still require the normal approved Agentic Human Review plan/run flow outside the browser UI.
- Use `agentic review quality source-text --standard <result> --deep <result> --xhigh <result> [--reference-review <workspace-text-or-json>] --json` after standard/deep/xhigh source-text runs to verify bounded source understanding, same-source identity invariants, output-safety non-leakage, effort-specific editorial differences, xhigh critique readiness, and no full-source/chunk-text persistence without rerunning providers.
- When an evidence-set manifest includes a source-text quality artifact, use the optional `owner_review_context.source_text_quality` in evidence-set, claim-readiness, longitudinal-quality, claim-standard-gate, and evidence-regeneration outputs as owner review context only. Existing evidence-set context must be re-sanitized before reuse. It is not proof, does not alter warnings/conditions/blockers/pass states/claim states, and does not authorize concrete rerun execution.
- Agentic Human Review source-text reading is additive and local. Use `--source-text <workspace-text-or-json>` on `agentic review propose` or `agentic review plan` when the review needs full transcript, article, PDF-extracted text, meeting-note, document, or other textual source understanding.
- TraceCue reads that source locally and writes only metadata plus bounded `source_reading_review` and `source_understanding_review`; full source text and chunk text are not persisted in JSON, Markdown, or provider payloads.
- Editorial synthesis uses source understanding first when available, records the `editorial_integrator` handoff, then integrates existing role opinions, findings, recommendations, video evidence, and content evidence.
- Provider transfer remains under the existing `allow-page-text` boundary and includes only bounded source-reading and source-understanding review data, never full source text.
- Report-quality now includes source-understanding, grounded-claim, evidence-ref-resolution, useful-recommendation, and assistant-reference-quality diagnostics for this layer.
- The feature remains advisory-only and does not change deterministic findings, metrics, release gates, owner-baseline proof, benchmark proof, MCP exposure, provider approval requirements, or equality/superiority claim policy.

## What Has Been Decided

- Product name: TraceCue.
- Repository root: current TraceCue checkout confirmed with `pwd` and `git rev-parse --show-toplevel`; the local checkout directory is `trace-cue` and the GitHub remote is `https://github.com/xxxMasahiro/trace-cue`.
- Agentic Human Review is a dedicated CLI-only owner-layer. It may use `agentic review propose` and `agentic review provider-readiness` for non-executing intake and diagnostics, but execution must use `agentic review plan/run/status/list`, require an approved plan hash, package-hash validation, explicit `--execute`, and exact transfer flags before any provider call, produce `agentic_human_review_advisory` output only, stay out of MCP profiles, and remain separate from generic `agent execution`, deterministic review findings, metrics, release gates, and existing review artifacts. Schema v2 makes reader feeling, content comprehension, trust, visual UX, accessibility comprehension, risk and misleading content, improvement priority, and mechanical-versus-human comparison required advisory-review concerns. Structured benchmark coverage, evidence-set, batch, evaluator policy, xhigh, longitudinal, and claim-audit diagnostics are read-only quality evidence and are not approval to execute providers or make human-equivalent/human-superior claims.
- The current claim-review proof packet is ready for owner claim-standard review, but the product claim-standard document remains a non-authoritative draft and equality/superiority claim flags remain false until a separately approved standard is mechanically enforced.
- The Agentic Human Review Responses adapter is only a loopback conversion bridge for manual live dogfood. Generic provider execution still goes through `agentic review run`; the adapter does not approve transfer, does not replace plan hashes or exact transfer flags, does not expose MCP tools, does not store credentials or raw provider responses, and does not change deterministic gates.
- Language settings are TraceCue-local. Dashboard display locale and artifact output language are independent roles; repository documentation remains English, and translation execution remains disabled until a separately approved implementation defines provider or template boundaries.
- Operation registry, operation roadmap, operation contracts, operation policy, operation admin readiness, operation provider readiness, release readiness, artifact-root readiness, legacy alias removal readiness, constrained shell readiness, and final hardening readiness reporting are TraceCue-local and bounded. They record risky roadmap groups, risk taxonomy, required gates, phase A/B/C boundary contracts, approved admin-only agent execution exposure, compatibility state, and final local verification plans, but do not promote Phase 60-155 into `docs/product/` commitments or authorize cleanup execution through MCP, capture execution, translation execution, package publication, real artifact-root migration, legacy alias removal, shell execution, remote CI triggering, token issuance, token storage, generic harness enablement, credential value reads, external evidence transfer outside the bounded agent execution policy, or MCP write/execute expansion beyond admin-only agent execution plan/run.
- Main purpose: local Playwright-based browser debugging and UI/UX inspection for humans and AI agents.
- Main design choice: agent-independent CLI, not Playwright MCP.
- Debug strategy: fast headless observation by default, headed browser or DevTools for important visual and interaction checks.
- OSS path: local Git, GitHub through `gh`, CI, npm packaging, MVP implementation, release.
- Product-local gate passed.
- Lesson-side scaffold, authority, and workflow-pair checks passed.
- Root agent entry is `AGENTS.MD`; legacy `AGENT.md` is absent.
- `ops/PRODUCT_PROFILE.json` remains `menu_id=free-development` with display name `TraceCue`.
- `ops/PRODUCT_OPERATION_MODE.tsv` remains `parent_managed` with `managed_by_parent=true`.
- Local Git has been initialized and the initial branch is `main`.
- The first scaffold commit exists.
- Product-gate evidence is recorded under `.git/product-gate-evidence/`.
- Phase 2a uses `browser-debug` as the working CLI binary name.
- Phase 2a uses Node.js 20 or newer, ESM modules, local-first execution, and ephemeral browser contexts by default.
- The first implementation slice should be `doctor`, command parsing, deterministic JSON errors, and focused tests.
- The first Playwright slice should be one-shot `observe --url <url> --json` with artifacts under ignored `.browser-debug/`.
- Process-scoped browser supervision is opt-in and implemented after one-shot observation.
- Phase 2a design verification passed with `./tools/product-gate`.
- The repository now has private local package metadata, `bin/browser-debug.js`, ESM source modules, and `tests/cli.test.js`.
- `doctor`, command parsing, deterministic JSON errors, and planned no-browser stubs are implemented.
- `observe --url <url> --json` validates input, launches an ephemeral Chromium context, captures structured page state, writes local artifacts, and closes the context.
- `npm test` is wired into `./tools/test_product_repository.sh` and `./tools/product-gate`.
- Local verification passed with `npm test`, `./tools/product-gate`, `git diff --check`, `doctor --json`, and the no-browser `observe` stub.
- Playwright is installed as a runtime dependency.
- `session start`, `session close`, simple `act`, `report`, and `spec export` are implemented with local file-backed session metadata.
- `supervise --url <url> --actions <json-array>` is implemented for process-scoped ephemeral browser supervision and closes before CLI exit.
- `daemon start`, `daemon status`, and `daemon stop` are implemented for local background ephemeral browser supervision through a detached worker process, local metadata, and local process signals.
- `npm run test:browser` passed for local file observation and click action smoke coverage.
- `npm run test:browser` now covers local file observation, screenshot/trace artifacts, click actions, form controls, keyboard input, deterministic scroll, wait actions, reports, and spec export.
- `npm test` now covers headed/devtools launch-mode wiring through a deterministic injected browser type.
- `npm run test:pack` verifies the local npm package file set with `npm pack --dry-run --json` and a `/tmp` npm cache.
- `CHANGELOG.md` and `docs/workflow/RELEASE.md` now track unreleased local changes and publication blockers.
- `npm run test:browser` covers supervised ordered actions in one ephemeral context.
- `npm test` includes architecture regressions for generic runtime boundaries, shared evidence helpers, and local Node CLI packaging.
- `npm run test:browser` covers daemon start/status/stop, and `npm test` covers local daemon boundary regressions.
- Local GitHub Actions CI configuration is present under `.github/workflows/ci.yml`.
- `ops/CI_MANIFEST.tsv` and `tools/check_product_ci.sh` validate CI configuration without remote execution.
- `npm run release:check` provides local release-readiness verification without publishing.
- The public GitHub repository exists at `https://github.com/xxxMasahiro/trace-cue`.
- Local `main` tracks `origin/main` and has been fast-forwarded to the local MVP runtime commit.
- GitHub Actions `main` CI has passed on push for Node 20, Node 22, and browser smoke jobs.
- CI uses `actions/checkout@v5` and `actions/setup-node@v5` to avoid Node 20 action-runtime deprecation annotations from the first remote run.
- JSON schema versioning is defined for the local MVP as `0.1.0`: additive fields are compatible, while field removal, renaming, type changes, or vocabulary semantic changes require a schema version bump with docs and tests.
- Artifact retention is manual by default: generated artifacts remain under ignored `.browser-debug/` until the developer removes that local artifact root or explicitly runs artifact-root-scoped cleanup with a receipt. The CLI does not auto-delete artifacts.
- `observe --trace` is implemented and writes local Playwright trace zip artifacts with a page-content warning.
- Dashboard Control Center `http://127.0.0.1:5173/` was observed successfully with screenshot and trace artifacts.
- FrameCue Control Center `http://127.0.0.1:5174/` was not listening during verification.
- Final local verification passed with `npm test`, `npm run test:browser`, `./tools/product-gate`, `git diff --check`, lesson-side product scaffold check, lesson-side product repository authority status, and lesson-side workflow-pair sync check.
- The pre-implementation review-platform direction was recorded in developer memory after xhigh sub-agent review.
- The accepted framing is a Playwright-powered review and evidence core with CLI as the source of truth and a future thin local MCP stdio adapter over the same core.
- The Phase 7 plan covers review schema contracts, single-URL review MVP, target manifests, site review, route discovery, viewport matrices, risk-gated action exploration, optional mock comparison, local MCP adapter, optional model or vision review, and public API/package readiness.
- Phase 7 local implementation is complete for machine-readable schemas, `schema list`, `schema get`, `review --url`, `review --target`, route discovery, viewport execution, coverage artifacts, action risk classification, shell-safe structured input, conservative mock metrics, local stdio MCP adapter, package API exports, and package file-set readiness.
- `browser-debug review --url <url>` writes local observation, layout, screenshot, review, report, and optional mock metric artifacts.
- `browser-debug review --target <manifest>` uses generic target manifests and writes local coverage and aggregate review artifacts.
- `browser-debug-mcp` is a local stdio adapter with an allowlisted tool surface; it does not add HTTP/socket listeners, arbitrary shell, cleanup execution tools, external upload, profile reuse, OAuth, or credential handling.
- The package remains private and `UNLICENSED`; no npm publication or release promotion was performed.
- Review runtime code must remain generic. Dashboard Control Center and FrameCue Control Center coverage should use manifests, fixtures, or acceptance evidence rather than target-specific runtime branches.
- Review findings should include category, severity, confidence, selector, rectangle, evidence, artifacts, and reproduction data.
- `browser-debug target init --url <url>` creates reusable local target manifests under `.browser-debug/targets/`.
- Review findings now include priority, impact, recommendations, fix candidates, and implementation notes.
- Review output now includes `action_plan` and local heuristic `review_advisory` data for developer handoff.
- Target review supports Markdown reports through `--report`.
- The local MCP adapter exposes target manifest initialization and target review tools in addition to doctor, observe, single-URL review, and schema tools.
- Local plugin metadata exists under `.codex-plugin/plugin.json`, `.mcp.json`, and `skills/browser-debug-review/SKILL.md`.
- `templates/review-target-manifest.json` is the reusable starting point for app route and viewport review.
- Review output now includes `quality_signals` for visual hierarchy, responsive layout, interaction affordance, accessibility structure, evidence completeness, developer handoff, local release readiness, route coverage, and disabled model-review boundaries.
- Local review evidence now captures headings, landmarks, images, visible overlap candidates, and richer computed style data.
- Review findings now cover heading hierarchy, missing main landmarks, missing image alt text, low text contrast, visible overlap, and mobile touch-target sizing without adding new finding categories or target-specific runtime branches.
- Markdown review reports include a Quality Signals section for developer triage.
- Browser smoke coverage verifies quality signals, alt text, contrast, overlap, target coverage summaries, local release readiness, and `external_evidence_transfer=false`.
- Target review now enqueues manifest `expectedRoutes` as explicit review targets, so known unlinked app routes can be reviewed through the viewport matrix.
- Coverage artifacts now include `coverage.routes.expected`, and target quality signals report expected manifest route counts.
- Target review records route-budget skips with `reason=route_budget_exceeded` when `budgets.maxRoutes` prevents full coverage.
- Browser smoke coverage verifies unlinked expected route review and route-budget skip accounting with generic local fixtures.
- Target manifests now support optional `pages` entries for named page expectations, page-specific viewports, page-specific mock metrics, and priority.
- Target review checks page expected visible text and expected selectors, records page coverage under `coverage.pages`, and reports `quality_signals.page_expectations`.
- Single-URL and target reviews now include `evidence_summary` data and write local `review_artifact_index` artifacts under `.browser-debug/review-artifacts/`.
- Browser smoke coverage verifies page expectation checks, page-specific viewport execution, page-level mock metrics, artifact indexes, and page expectation report output.
- Layout evidence now records image load state, visible loading indicators, and empty table/list/grid containers.
- Review findings now flag broken visible images, lingering loading indicators after the review wait, and empty data containers without visible empty-state messaging.
- Single-URL and target quality signals now include rendered-state summaries.
- Markdown reports now include a Developer Triage section and rendered-state quality signal status.
- Target review output now includes manifest suggestions for missing page expectations, unpinned expected routes, exhausted route budgets, failed page checks, and rendered-state gaps.
- Browser smoke coverage verifies rendered-state findings, evidence summaries, developer triage reports, and manifest suggestions.
- Loading indicator evidence now ignores normal ready/progress business-state text unless explicit loading semantics or loading-like attributes are present.
- Browser smoke coverage verifies ready/progress business-state text is not reported as lingering loading UI.
- Target manifests now support opt-in `localContentUxAdvisory`, bounded inline `sourceData`, and page `expectations.dataBindings` for local source-to-screen advisory checks.
- `src/content-ux-advisory.js` is a pure local helper with no Playwright, filesystem access, artifact reads, external transfer, or target-specific runtime branches.
- Target review emits `local_content_ux_advisory` and `quality_signals.content_ux` only when manifest opt-in is enabled.
- Content UX advisory is additive and does not create review findings, change `metrics.finding_count`, change the existing `action_plan`, or change `quality_signals.release_readiness`.
- Markdown reports include a bounded Content UX Advisory section when enabled and do not copy source values or full page text.
- Schema registry and packaged schema files are covered by parity tests for the review and target manifest property sets.
- Target review evidence summaries now include bounded element evidence for selector-scoped advisory checks.
- Content UX advisory now evaluates selector-scoped text, explicit attributes, state attributes, and risk attributes from target manifest data bindings.
- Content UX advisory now evaluates `localContentUxAdvisory.requiredUserQuestions` and page `expectations.userQuestions` as advisory information-architecture and user-journey signals.
- `templates/status-dashboard-content-ux-target-manifest.json` provides a reusable disabled-by-default status-dashboard manifest example without runtime product-specific branches.
- Target review now emits additive `content_ux_findings`, `content_ux_action_plan`, and `content_ux_readiness` only when content UX advisory is enabled.
- `content_ux_findings` are separate from review `findings`; they do not change `metrics.finding_count`, the existing `action_plan`, or `quality_signals.release_readiness`.
- Markdown reports now include a bounded Content UX Developer Handoff section without copying source values or full page text.
- Content UX findings now include local advisory categories for status clarity, action clarity, navigation clarity, information architecture, source alignment, content contracts, coverage contracts, and review scope.
- Target review now emits additive `content_ux_page_handoff` and `content_ux_manifest_authoring` only when content UX advisory is enabled.
- Markdown reports now include page-level content UX summaries and manifest-authoring suggestion counts in the Content UX Developer Handoff section.
- Target review now emits additive `content_ux_review_brief` and `content_ux_rubric_evaluation` only when content UX advisory is enabled.
- Markdown reports now include bounded Content UX Review Brief summaries for page roles, decision needs, and rubric criteria.
- Packaged content UX templates and tests now use domain-neutral status-dashboard vocabulary while preserving selector-scoped state/risk coverage.
- `status_clarity` and `action_clarity` are the preferred default content UX categories; existing manifest rubric categories `workflow_state_clarity` and `next_action_clarity` remain accepted as legacy-compatible aliases.
- Lesson-specific Dashboard Control Center workflow, Git, CI, blocker, repository-selection, and next-safe-action semantics should live in lesson-owned target manifests or fixtures, not TraceCue runtime code.
- `browser-debug target validate --target <manifest> --json` validates edited manifests without launching a browser and returns normalized counts, authoring suggestions, review next commands, and local-first boundaries.
- The local MCP adapter exposes `browser_debug_target_validate`, and the local package API exports `runTargetValidate`.
- Target validation does not mutate manifests, expose sourceData values, upload evidence, reuse profiles, add external source loaders, add HTTP/socket MCP transport, or change review findings, metrics, existing action plans, or release readiness.
- `browser-debug resource status --json` reports local memory, swap, cgroup, pressure, and process memory signals without launching a browser or writing artifacts.
- The local MCP adapter exposes `browser_debug_resource_status`, and the local package API exports resource status collection and parsing helpers.
- Resource status preflight is read-only and does not mutate system cache, configure swap, delete files, execute shell commands, use privileged helpers, upload evidence, reuse profiles, or control arbitrary processes.
- `browser-debug review --resource-guard advisory|fail-critical|off` adds additive resource guard output, preflight, target route/viewport rechecks, screenshot/trace pressure warnings, and optional fail-critical no-launch behavior.
- `browser-debug daemon start --idle-timeout <duration>` and `browser-debug daemon start --max-lifetime <duration>` add optional local lifecycle guards to detached daemon workers.
- `browser-debug resource artifacts plan --json` and `browser-debug resource artifacts cleanup --dry-run --json` report local artifact usage and cleanup candidates without deleting files.
- `browser-debug resource artifacts cleanup --execute --json` deletes only selected regular files under the configured artifact root and writes a local receipt.
- The local MCP adapter exposes artifact usage planning only and does not expose cleanup execution.
- `browser-debug agent surfaces list --json`, `agent package`, `agent requests list`, `agent requests show`, `agent ingest`, and `agent report` are implemented for local subscription-agent handoff, read-only handoff status/detail tracking, and advisory result import.
- `browser-debug agent workflow create --package <path> --json`, `agent workflow status --workflow <path> --json`, `agent workflow index --json`, and `agent workflow report --workflow <path> --json` are implemented for local dashboard and automation tracking of package, prompt, agent-response, ingest, report-pending states, and bounded workflow summaries.
- Agent advisory output is separate from deterministic review findings, `metrics.finding_count`, existing `action_plan`, and `quality_signals.release_readiness`.
- Agent package/request-status/request-detail/workflow/ingest/report uses local package, prompt, workflow, result, report, and receipt artifacts under `.browser-debug/` without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, external evidence transfer, artifact writes from detail reads, or review artifact mutation.
- Phase 29 adds an `agent execution` layer instead of changing `agent workflow`. The current dashboard flow is package, dry-run execution plan, explicit execution run, execution status/list, normalized advisory result/report, and workflow status/index/report.
- Phase 29 subscription support uses configured local runner callbacks only, not SaaS web UI automation or free-form shell input.
- Phase 29 API support uses an env-only generic API adapter with dry-run planning, explicit `--execute`, bounded package/prompt disclosure, local receipts, no raw provider response storage, and advisory-only normalization.
- Phase 29 must not change review `findings`, `metrics.finding_count`, existing `action_plan`, `quality_signals.release_readiness`, resource guard behavior, artifact cleanup behavior, target manifest behavior, or existing `agent_workflow` status meanings.
- Phase 29 execution remains gated by local plans, explicit `--execute`, local receipts, and advisory-only normalization. After the approved Phase 74-76 bridge, only stdio `admin` may invoke the existing `agent execution plan/run` path through MCP; safe/full/HTTP and unrelated execution families remain excluded.
- Phase 30 adds `npm run test:pack-install`, which verifies the packed tarball from a temporary install layout without publishing, contacting npm registry, changing package naming, changing license, mutating marketplace state, uploading evidence, or storing credentials.
- Phase 31 adds launch-selected MCP profiles. No-profile `browser-debug-mcp` remains compatible with `full`; `safe` exposes no-browser/no-delete/no-provider tools; `full` may expose bounded process-scoped supervise; `admin` is explicit and, after approved later slices, may expose only the approved agent execution plan/run path and approved persistent session tools while continuing to exclude cleanup execution, non-admin persistent session control, shell tools, HTTP/socket transport, external upload, provider credentials, profile reuse, and arbitrary process control.
- Phase 31 confines MCP `@file` structured input to the workspace, rejecting absolute paths, parent traversal, symlink escapes, non-regular files, and oversized files without changing normal CLI `@file` behavior.
- Phase 32 centralizes package, CLI, MCP, plugin, repository, version, skill path, and temporary package-check identity metadata in `src/product-identity.js`.
- Phase 32 keeps the current package name, CLI commands, MCP server name, plugin name, display name, repository URL, private package state, and license unchanged while making future approved renames contract-driven.
- Phase 32 routes package dry-run and packed-install smoke temporary paths through identity-derived helpers and verifies package/plugin/MCP/API alignment through no-browser and packed-install tests.
- Phase 33 exposes read-only MCP tools for local agent surfaces, request status/detail, workflow status/index, and execution status/list.
- Phase 33 keeps MCP package generation, ingest, report writing, workflow creation, cleanup execution, shell tools, HTTP/socket transport, external upload, credential handling, and gate mutation unimplemented; execution planning/run exposure remains limited to the later approved Phase 74-76 stdio `admin` agent execution bridge.
- Phase 34 exposes only an explicit safe HTTP MCP transport with loopback bind, bearer-token enforcement, Host/Origin validation, request body limits, safe-profile tools, and isolated listener implementation.
- Phase 35 adds MCP client configuration guidance and smoke coverage only; it does not change the safe HTTP tool allowlist or expose write/execute capabilities.
- Phase 36 adds MCP capability policy inspection only; it reports current profile/transport/admin/exclusion state and does not expose write/execute/admin capabilities.
- Phase 37 adds external-repository usage guidance only; it does not change runtime command behavior, MCP permissions, publication state, marketplace state, identity names, or release blockers.
- Phase 38 adds local-checkout MCP config metadata only; it does not start servers, write client configs, read credentials, print token values, change MCP permissions, publish packages, or rename identities.
- Phase 39 adds consumer runtime-readiness guidance only; it does not change runtime command behavior, MCP permissions, target manifests automatically, publication state, marketplace state, identity names, or release blockers.
- Agentic Human Review AHR-41-44 adds real-provider dogfood opt-in enforcement, visible-text provenance, benchmark-completion readiness, transfer-flag masking for plan/package provider payload sections, and mechanical `xhigh` completion assessment. It remains advisory-only and does not mutate deterministic findings, metrics, release gates, MCP exposure, credential storage, or raw provider response storage.
- `visual review prepare --review-index <path>` is implemented as a no-browser, metadata-only preparation command for future visual review results. It reads review artifact indexes and visual evidence metadata only, writes local preparation artifacts and receipts, exposes package API helpers and schemas, and remains out of MCP.
- `visual review run --preparation <path> --surface <id> --provider <id> --model <id> --execute` is implemented as a CLI-only visual review execution command. It validates preparation artifacts, runs fake/local/API provider adapters with metadata/local references only, writes visual review execution/result artifacts and receipts, exposes status/list helpers, and remains out of MCP.
- `visual review dashboard --json` is implemented as a read-only local dashboard command and safe MCP tool (`browser_debug_visual_review_dashboard`). It summarizes preparation, execution, and result artifacts without writing artifacts, executing providers, reading raw pixels, or changing gates.
- `mcp execution gates --json` is implemented as a read-only local policy command and safe MCP tool (`browser_debug_mcp_execution_gates`). It reports required safety gates for future MCP write/execute exposure while preserving the approved Phase 74-76 stdio `admin` agent execution bridge as the only current MCP execution surface.
- `capture plan --json` is implemented as a read-only local planning command and safe MCP tool (`browser_debug_capture_plan`). It reports screen, window, and desktop app capture gates while keeping capture execution disabled.
- `capture readiness --json` and `capture status --json` are implemented as read-only local capability/privacy/schema-readiness reports and safe MCP tool `browser_debug_capture_readiness`. They do not call OS capture APIs, load native capture dependencies, enumerate windows or processes, read raw pixels, write artifacts, or expose execution.
- `capture run --source <screen|window|desktop-app> --execute --json` is implemented only as a fail-closed approval gate. It returns `CAPTURE_EXECUTION_NOT_AVAILABLE`, includes readiness boundary metadata, writes no artifacts, reads no pixels, and performs no OS capture.
- `capture handoff --image <workspace-image> --source <screen|window|desktop-app> --json` is implemented as a CLI/API-only metadata handoff for existing workspace images. It is not exposed through MCP and writes no artifacts.
- `settings locale resources --json`, `settings report templates --json`, and `translation readiness --json` are implemented as provider-free read-only CLI/API/MCP-safe inspection surfaces. They preserve raw evidence and canonical enums, write no files, call no providers, read no credentials, and change no gates.
- `translation dry-run --provider fake --json` is deterministic and provider-free for generated UI/report chrome only. `translation run --execute --json` fails closed with `TRANSLATION_EXECUTION_NOT_AVAILABLE` and does not call providers or write artifacts.
- `visual review plan --capture-handoff <workspace-json|-> --json` is implemented as a CLI/API-only read-only planning report for desktop review provider-preparation. It is not exposed through MCP, writes no artifacts, rereads no image bytes, and runs no providers.
- `operation roadmap --json` is implemented as a read-only CLI/API/MCP-safe governance report for draft phase A/B/C boundary contracts. It writes no artifacts, performs no live execution, issues no execution tokens, triggers no remote CI, and does not promote Phase 61-155 into formal product-plan commitments.
- `operation contracts --json` is implemented as a read-only CLI/API/MCP-safe contract report for Phase 61-64 risk taxonomy, gate schema, execute-token shape, and receipt shape. It writes no artifacts, performs no live execution, issues no tokens, writes no receipts, enables no harness, and does not promote Phase 65-155 into formal product-plan commitments.
- `operation policy --json` is implemented as a read-only CLI/API/MCP-safe policy/readiness report for Phase 65-68 admin policy defaults, CLI plan readiness, disabled generic harness readiness, safe MCP readiness, and approved admin-only agent execution exposure. It writes no artifacts, mutates no policy config, performs no live execution from the report, issues no tokens, writes no receipts from the report, enables no generic harness, and does not promote Phase 79-155 into formal product-plan commitments.
- `operation admin-readiness --json` is implemented as a read-only CLI/API/MCP-safe readiness report for Phase 69-70 MCP admin execute-token flow readiness, MCP admin generic harness bridge readiness, and approved admin-only agent execution bridge state. It writes no artifacts, performs no live execution from the report, issues no tokens, stores no tokens, writes no receipts from the report, dispatches no generic harness, and does not promote Phase 79-155 into formal product-plan commitments.
- `operation provider-readiness --json` is implemented as a read-only CLI/API/MCP-safe readiness report for Phase 71-78 provider MCP planning, bounded disclosure contract readiness, env credential guard names, approved admin-only fake/local/API execution exposure, and safe MCP provider status/list readiness. The readiness report writes no artifacts, performs no live execution, calls no providers, executes no local runners, reads no credential values, transfers no evidence, and does not promote Phase 79-155 into formal product-plan commitments.
- `resource artifacts plan --json` and MCP `browser_debug_resource_artifacts_plan` are implemented as cleanup planning only. Plans include candidate locks and deterministic plan hashes, MCP rejects execute-like arguments, and MCP cleanup execution remains unavailable.
- `resource artifacts cleanup --execute --json` remains CLI-only and now revalidates optional plan hashes plus path/type/size/mtime/content-hash candidate locks immediately before deletion, writes receipt audit fields, and does not delete directories.
- `review --image <workspace-image> --capture-handoff <workspace-json|-> --json` verifies capture handoff source path and media hash against the reviewed image before propagating caller-declared desktop provenance into visual evidence metadata.
- `visual review aggregate --preparation <workspace-json> --json` is implemented as a read-only local aggregation command over existing visual review result metadata. It writes no artifacts, calls no providers, reads no raw pixels, exposes no MCP tool, mutates no reviews, and changes no gates.
- Phase 34 keeps packaged `.mcp.json` as stdio compatibility and keeps HTTP `full` or `admin`, socket transport, remote HTTP listeners, package generation, ingest, report writing, workflow creation, cleanup execution, shell tools, external upload, profile reuse, credential handling, and gate mutation unimplemented; approved execution remains stdio `admin` agent execution plan/run only.
- No arbitrary source-data file or URL loader was added; external references remain ignored advisory signals until a separately approved loader design exists.
- Model/API review outside the Phase 29 agent execution adapter boundary, evidence leaving the local process beyond the bounded package/prompt disclosure policy, socket MCP transport, remote HTTP MCP listener, HTTP `full` or `admin`, persistent browser profile reuse, authentication automation, external upload, arbitrary shell execution, automatic cleanup, cleanup outside the configured artifact root, host memory-cache mutation, swap configuration, MCP cleanup execution, privileged helper execution, arbitrary process control, npm publication, package naming, and license changes remain approval-bound.
- The control-center browser surface is implemented as a React + Vite app under `control-center/`, with `control-center status --json` as the read model and `control-center serve` as the loopback built-asset/API server. The UI imports `docs/design-system/tokens.json` and `docs/design-system/components.json` for presentation control. The dashboard API remains GET-only, Host/Origin validated, and no-store. The only browser actions are confirmed local source-intake proposal creation and fixed-path display-language persistence; there is still no MCP JSON-RPC, provider execution, browser auto-launch, shell execution, raw artifact serving, external upload, or gate mutation.

## Next Step

After local and remote synchronization, the next safe Agentic Human Review step is to regenerate the real-page owner-contract standard/deep/xhigh dogfood cells using an adapter launched with explicit long `--timeout`, `--max-request-bytes`, and `--max-provider-response-bytes` values plus `AGENTIC_HUMAN_REVIEW_API_TIMEOUT_MS` set to the same approval-reviewed timeout, then rerun calibration, direct/provider/benchmark comparisons, owner-baseline comparison, evidence-set summary, claim-readiness, longitudinal quality, and claim-standard-gate. If owner-baseline or benchmark comparison still fails, inspect the persisted contract-failure diagnostics and the contract-aware rerun plan before spending additional provider budget.

Agentic Human Review AHR-01-44, the first manual live upstream dogfood pass, the local maturity/longitudinal-quality diagnostics, the first quality-gate foundation, owner-labeled human baseline comparison support, and proof-policy hardening are complete locally as advisory, approval-gated work. The next practical AHR work is approved standard/deep/xhigh real-page dogfood across multiple benchmark cases using real-provider structured findings with local evidence references and independently owner-approved baselines that do not carry synthetic or fixture-only markers, followed by report-quality, calibrate, human-baseline compare, compare batch, evidence-set summary, longitudinal rollup, and claim audit review to tune the rubric. Human-equivalent or human-superior claims remain explicitly unsupported. The `operation roadmap`, `operation contracts`, `operation policy`, `operation admin-readiness`, and `operation provider-readiness` reports remain local governance surfaces and do not make Phase 120-155 formal product-plan commitments or live execution permission. Parent-repository changes, consumer-repository changes, public package naming, npm publication, marketplace registration, MCP write/execute expansion beyond admin-only agent execution plan/run, artifact-root migration, legacy-alias removal, real capture execution, cleanup execution through MCP, live translation provider execution, MCP admin translation execution, and shell execution remain separate approval-bound work.

## Restart Notes

- Do not publish to npm yet.
- Keep `.browser-debug/`, screenshots, traces, storage state, cookies, credentials, and secret-like data out of committed files.
- Do not reuse existing browser profiles, persist storage state outside the approved explicit artifact-auth opt-in, automate OAuth/login flows, or upload artifacts without a security plan and approval.
- If product workflow commands need lesson context, use the product path explicitly to avoid mixing this repository with `task-tracker-repository`.
- Keep Phase 7 review code generic and manifest-driven.
- Keep stdio MCP as the packaged compatibility default. HTTP MCP is available only through explicit safe-profile loopback/token launch and must not be expanded to `full`, `admin`, remote bind, socket transport, shell tools, cleanup execution, provider/API execution, or write-producing advisory tools without a separate approved phase.
- Keep model or vision review out of deterministic local gates.
- Keep target-specific Control Center details in manifests, fixtures, or acceptance evidence.
- Keep content UX source data bounded and inline unless a separate approved loader design is added.
- Keep plugin marketplace registration out of local implementation unless explicitly approved.
- Treat `resource status` and `resource_guard` as local safety signals; do not turn them into host cleanup, swap configuration, cache deletion, privileged helper execution, external upload, or arbitrary process control without a separate approved task.
- Treat artifact cleanup as artifact-root-only and explicit; preserve plan hashes, candidate locks, receipt audit fields, and no-directory-delete behavior. Do not add automatic cleanup, cleanup outside `.browser-debug/`, or MCP cleanup execution without a separate approved task.
- Keep MCP profiles launch-scoped and fail-closed. Prefer `--profile safe` for low-trust MCP clients and do not treat `admin` as permission for cleanup execution, agent/API execution, shell tools, HTTP `full` or `admin`, socket transport, external upload, profile reuse, provider credentials, or arbitrary process control.
- Treat agent advisory package/request-status/request-detail/workflow/import/report as local handoff operations; only `agent execution run` may call a provider adapter after a matching dry-run execution plan and explicit `--execute`.
- Treat agent execution as a separate layer over local packages. Do not overload existing workflow status semantics or mutate review artifacts.
- For Phase 29, preserve dry-run execution planning before any runner/provider execution.
- For Phase 29 API execution, read credential values only from named environment variables and never record them.
- For Phase 29 subscription execution, use configured local runner callbacks only; do not accept free-form shell commands or automate SaaS web UIs.
- Keep raw screenshots, traces, DOM, console payloads, network payloads, sourceData values, report bodies, raw provider responses, cookies, storage state, and existing browser profile data out of execution artifacts unless a separate explicit design changes the disclosure policy.
- Treat packed install smoke as local release evidence only; it does not authorize npm publication, package rename, license change, npm token handling, plugin marketplace registration, or external evidence transfer.
- Treat product identity metadata as rename readiness only; it does not authorize package rename, repository rename, plugin rename, MCP server rename, CLI command rename, public package naming, license change, marketplace registration, npm publication, or external evidence transfer.
- Treat MCP read-only agent status tools as inspection only; do not expand them into package generation, ingest, report writing, workflow creation, execution planning, execution run, cleanup execution, provider/API execution, shell tools, external upload, socket transport, remote HTTP listener, HTTP `full` or `admin`, credential handling, or gate mutation without a new approved phase.
- Treat `docs/workflow/CONSUMER_USAGE.md` as usage guidance only; it does not authorize publication, marketplace registration, remote listeners, MCP execution, cleanup execution, provider/API execution, shell tools, credential handling, or external evidence transfer.
- Treat target runtime readiness findings as consumer application state until proven otherwise. Check the consumer app's full local stack and API base settings before treating API 404s or `needs_attention` as TraceCue connection failures.
- Treat operation registry, operation roadmap, operation contracts, operation policy, operation admin readiness, and operation provider readiness output as policy inspection only; do not interpret registry entries, roadmap phase contracts, contract shapes, readiness output, capability exclusions, credential env names, or execution-gate entries as permission to promote draft phases, issue execution tokens, store tokens, write receipts outside the approved agent execution path, enable generic harnesses, read credential values, transfer evidence outside bounded agent execution, trigger remote CI, execute providers outside approved admin agent execution, delete files, capture pixels, translate evidence, publish packages, migrate artifact roots, remove aliases, run shell commands, or expand MCP permissions.

## Stop Conditions

- Missing canonical docs under `docs/product/` or `docs/workflow/`.
- Root-level duplicate product documents.
- Any committed secret-like data.
- External service, OAuth, webhook, browser profile reuse, or artifact upload requested without a security plan and approval.
- Any design path that requires arbitrary shell execution or persistent credential storage.
- Review platform code adds app-specific runtime branches for individual Control Centers.
- Content UX advisory starts reading arbitrary manifest paths or remote source URLs without explicit approval and security documentation.
- MCP adapter code adds socket listeners, remote HTTP listeners, HTTP `full` or `admin`, external upload, arbitrary shell execution, cleanup execution tools, or persistent storage without explicit approval.
- Plugin metadata adds external upload, profile reuse, credential storage, marketplace mutation, or network transport without explicit approval.
- Resource safety code mutates system cache, changes swap configuration, deletes outside the configured artifact root, executes shell commands, uses privileged helpers, uploads evidence, reuses profiles, exposes MCP cleanup execution, or controls arbitrary processes without explicit approval.
- Agent advisory code calls provider APIs, uploads evidence, stores credentials, exposes MCP agent execution, mutates review artifacts, or changes deterministic review gates without explicit approval.
- Agent execution code changes existing deterministic review outputs, existing `agent_workflow` status meanings, resource guard behavior, artifact cleanup behavior, or release-readiness semantics.
- Agent execution code stores credential values, raw provider responses, raw screenshots, trace contents, raw DOM, console payloads, network payloads, sourceData values, report bodies, cookies, storage state, existing browser profile data, or raw review artifacts.
- Agent execution code bypasses dry-run planning, explicit `--execute`, env-only credential loading, local receipts, bounded disclosure, or advisory-only normalization.
- Agent execution code exposes execution through MCP, accepts free-form shell commands, automates SaaS web UIs, adds OAuth/login automation, persists credentials, or adds HTTP/socket provider control paths.
- Product identity work starts changing package names, repository names, plugin names, MCP server names, CLI command names, public package naming, license, marketplace registration, npm publication, or external evidence transfer without explicit approval.
- MCP read-only agent status or HTTP transport work starts writing advisory artifacts, creating workflows, creating execution plans, running executions, calling providers, deleting files, uploading evidence, mutating review artifacts, changing gates, adding shell tools, adding socket transport, adding remote HTTP listeners, exposing HTTP `full` or `admin`, or handling credentials without explicit approval.
