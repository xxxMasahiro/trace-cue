# TASK_TRACKER.md

## Current Status

Agentic Human Review source-text effort-matrix verification is implemented as a read-only local diagnostic surface. `agentic review quality source-text` requires workspace-confined standard, deep, and xhigh Agentic Human Review result artifacts generated with source-text metadata, optionally accepts a workspace-confined reference review, and emits `agentic_human_review_source_text_quality` with source-understanding completion, source-reading/source-understanding counts, source-type consistency, editorial synthesis hashes and scores, pairwise effort deltas, xhigh counterpoint/evidence-limit/conclusion-change readiness, optional reference-review comparison summaries, diagnostics, warnings, and boundary flags. The output suppresses full source text, chunk text, candidate full reviews, reference review prose, result paths, source locators, provider responses, credentials, and MCP execution. It does not call providers, write artifacts, mutate deterministic findings, change release gates, satisfy proof contracts, or authorize human-equivalent or human-superior claims.

Agentic Human Review editorial-quality comparison is implemented as a read-only local comparison surface. `agentic review compare --comparison-kind editorial-quality` accepts a workspace-confined reference review text or JSON artifact as the baseline and a candidate Agentic Human Review result, then compares the candidate `editorial_synthesis.full_review` against effort-aware reference targets. Output includes hashes, scores, deltas, strengths, gaps, target status, and diagnostics only; reference prose, candidate prose, source text, local paths, and source locators are not emitted. Unsafe raw media, binary, full-source, or credential-bearing reference inputs fail closed. This does not call providers, transfer evidence, expose MCP execution, mutate deterministic findings, change release gates, satisfy proof contracts, or authorize human-equivalent or human-superior claims.

Agentic Human Review source-text reading and source understanding are implemented as an additive full-source understanding layer for editorial synthesis. Proposals and plans can accept optional workspace-confined `--source-text` plain text or JSON for video transcripts, web-page text, PDF-extracted text, meeting notes, documents, transcripts, and other textual artifacts. TraceCue reads the full source text locally, chunks and hashes it without persisting full source text or chunk text, derives an effort-aware `source_reading_review` plus `source_understanding_review`, and lets editorial synthesis combine the full-source understanding with existing TraceCue findings, role opinions, recommendations, video evidence, and content evidence through an `editorial_integrator` record. The understanding layer records thesis, audience promise, narrative arc, turning points, must-not-miss points, tensions, limitations, reviewer implications, bounded evidence claims, assistant-reference quality target, and coverage metrics so the final review is not limited to summary-only material. The natural full-review composer now builds a target-independent editorial narrative plan from source understanding and cross-checks it against existing normalized AHR findings, reader-impact records, role opinions, and action-plan signals before composing prose. `standard` stays practical and source-grounded, `deep` adds audience value, concrete examples, and prioritization, and complete `xhigh` adds critique, verification, counterpoints, evidence limits, and conclusion-change conditions. The composer suppresses fake-provider scaffolding, `Step`/`role` markers, operational effort labels, quality-target labels, duplicate or near-duplicate source anchors, target-specific heuristics, and boundary boilerplate from the review body while keeping them in structured metadata. No-browser matrix coverage verifies standard/deep/xhigh source-text synthesis across video, web page, PDF, meeting notes, and document source types. Markdown reports now include `Source Reading` and `Source Understanding` sections with bounded key points, examples, cautions, must-not-miss points, source claims, and source excerpt refs. Report-quality now exposes source-understanding, grounded-claim, evidence-ref-resolution, useful-recommendation, and assistant-reference-quality diagnostics. Provider payloads can include only bounded source-reading and source-understanding review data under the existing page-text transfer boundary, and source-understanding refs are compacted to ids and hashes without excerpt text or source locators; raw media, raw binaries, base64/blob/data URI payloads, credential fields, full source text, chunk text, and raw/full structured fields are rejected. This does not implement FrameCue, add provider calls, store full transcripts or full documents, transfer full source text, expose AHR through MCP, mutate deterministic findings, change release gates, satisfy owner-baseline or benchmark proof, or authorize human-equivalent or human-superior claims.

Agentic Human Review effort-aware quality diagnostics are implemented as an additive report-quality classification layer. Embedded advisory `report_quality` and standalone `agentic review report-quality` now expose optional effort expectations, structured quality diagnostics, classification summaries, effort notes, and evaluator policy diagnostics. Missing dedicated critique or verification output is an expected effort gap for standard and deep results because those roles are not planned there, while incomplete xhigh still emits policy warnings, maturity gaps, and strict missing-condition diagnostics. Markdown reports separate effort notes from quality warnings and localize TraceCue-generated diagnostic chrome through artifact output language settings. This does not add standard/deep roles, change scoring formulas, call providers, store raw provider responses, expose MCP, satisfy proof contracts, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review generic content evidence intake and localized report chrome are implemented as an additive bounded-evidence extension to editorial synthesis. Proposals and plans can accept optional workspace-confined `--content-evidence` JSON for video, web page, PDF, meeting-note, document, transcript, or other content sources, while `--video-evidence` remains a compatibility input that is projected into generic supplemental `content_evidence`. The intake normalizes analyzer-neutral summaries, bounded content units, claims, limitations, coverage, and provenance; rejects raw media, raw binaries, base64/blob/data URI payloads, raw HTML/PDF bytes, full documents, full transcripts, and truthy raw/full privacy or boundary declarations while accepting explicit false-valued privacy or boundary metadata that declares raw/full content is absent; carries path-free content-evidence scope, quality, source-type display labels, bounded-evidence density, review-strength guidance, and source references into plans, packages, provider payload filters, advisory results, schemas, report-quality diagnostics, and Markdown reports; and lets editorial synthesis distinguish page-only review from page-plus-content-evidence review. TraceCue-owned Agentic Human Review report headings, labels, fallback text, composer connective phrases, source-type display labels, source-text preservation explanations, effort stance, xhigh stance, and evidence-scope lines resolve through artifact output language settings while provider-authored advisory text and source evidence text are preserved. This does not implement an analyzer, change FrameCue, download or upload remote content, call providers during planning, translate provider output or source evidence, store raw provider responses, expose MCP, create proof, satisfy owner-baseline or benchmark contracts, authorize claims, or mutate deterministic findings or release gates.

Agentic Human Review editorial synthesis is implemented as a local derived view over normalized advisory results. New results include optional `editorial_synthesis` data and Markdown reports render an `Editorial Synthesis` section with source-attributed observations, tensions, strengths, cautions, recommended direction, owner-decision context, bounded language-setting metadata, source-text preservation policy, and limited-output warnings when the result is too sparse. When usable generic `content_evidence` is present, the local composer builds the full review from bounded content summaries, excerpt units, observed claims, limitations, and existing advisory signals before generic run summaries, classifies content-evidence density to keep summary-only or metadata-only conclusions cautious, reflects standard/deep/xhigh effort stance without promoting non-xhigh output into verification, suppresses template-only paragraphs and near-duplicate sentences, and applies the same path to videos, web pages, PDFs, meeting notes, documents, transcripts, and other source types without target-specific branches. The synthesis resolves TraceCue-local artifact output language settings across the supported 14-locale contract and records source-text preservation when translation execution is disabled. The synthesis is not requested from providers or adapters, does not call APIs, does not translate source evidence or provider output, does not read raw pixels or credentials, does not store raw provider responses, does not expose MCP, does not create proof, does not satisfy owner-baseline or benchmark contracts, does not change report-quality metrics, and does not mutate deterministic findings or release gates.

Agentic Human Review owner-baseline label-obligation enforcement is implemented for reusable real-provider proof recovery. The Responses adapter now derives `required_owner_baseline_findings` from every required owner label in the approved baseline contract, preserves target-specific criterion fallback obligations only when a criterion has no linked owner label, validates canonical `owner_baseline_findings` against exact owner label ids, linked criterion ids, and catalog-backed evidence references, and filters repair templates by missing owner label ids. Human-baseline comparison now prefers evidence-backed exact owner-label or criterion findings over earlier broad unbacked text matches. This does not synthesize owner-label proof from prose, benchmark coverage rows, coverage patch output, target URLs, role-level findings, generic advisory findings, raw artifacts, raw provider responses, request payloads, credentials, or target-specific branches, and it does not expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review coverage patch repair is implemented for reusable real-provider dogfood recovery. After at least one full replacement repair attempt, when the only remaining contract failure is absent benchmark coverage rows, the Responses adapter can issue one bounded provider-authored coverage patch request with redacted prior-advisory context, exact missing-row targets, and allowed evidence-reference ids. The adapter merges only exact missing labels with catalog-backed evidence references, marks provider patch provenance, and re-runs the same fail-closed benchmark, owner-baseline, claim, and effort validation. This does not synthesize required mentions, required dimensions, owner-baseline findings, or prose-derived coverage, does not repair invalid existing rows with unknown evidence references, does not store raw provider responses or credentials, does not expose Agentic Human Review through MCP, does not authorize human-equivalent or human-superior claims, and does not mutate deterministic findings or release gates.

Agentic Human Review provider-output JSON wrapper recovery is implemented for reusable real-provider dogfood recovery. The Responses adapter now parses provider output in memory by accepting direct advisory JSON first, then only one unambiguous provider-authored advisory JSON object from JSON-string encoding, a single JSON Markdown fence, or prose wrapped around one balanced JSON object. Malformed JSON, multiple JSON candidates, arrays, primitives, non-JSON fences, and non-advisory output still fail closed. This does not increase repair attempts, synthesize advisory content, relax benchmark, owner-baseline, xhigh, claim, or evidence-reference validation, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review forbidden-claim absence completion is implemented for reusable real-provider dogfood recovery. The Responses adapter now requires nested benchmark coverage arrays in provider JSON schema when effective coverage is active, derives nested `minItems` from required coverage templates, includes path-oriented `coverage_repair_targets` in repair context, and after a provider repair attempt can complete only missing forbidden-claim absence rows from canonical required templates when the advisory does not assert the forbidden claim and a catalog-backed evidence reference is available. Completed rows are marked adapter-derived and advisory-only. This does not synthesize required mentions, required dimensions, owner-baseline findings, or prose-derived coverage, does not increase repair attempts, does not relax unknown-evidence-reference rejection, does not store raw provider responses or credentials, does not authorize human-equivalent or human-superior claims, does not expose Agentic Human Review through MCP, and does not mutate deterministic findings or release gates.

Persistent browser session Slices 0-8 are implemented locally as a generic retained-page workflow. TraceCue now has a bounded full-profile `browser_debug_supervise`, persistent `session start/status/stop/act/observe/checkpoint/review`, manual-login checkpoints, local review handoff from the retained page, explicit storageState import/export confined to the configured artifact auth directory, admin-stdio-only MCP session tools, session-action and persistent-session schema coverage, parser/API/MCP tests, redaction coverage, architecture boundaries, and browser smoke coverage. The slice remains local-only: safe MCP, full MCP, and HTTP MCP do not expose persistent session tools; existing browser profiles are not reused; OAuth/password automation remains prohibited; storageState is not persisted by default; cookie/token values are not printed; and artifact roots remain local and ignored.

Agentic Human Review owner-baseline canonical proof enforcement is implemented for reusable real-provider dogfood recovery. Responses adapter validation now accepts target-specific owner-baseline proof only from canonical `owner_baseline_findings` records with required criterion ids, owner label ids, and catalog-backed evidence refs; role-level findings, generic advisory findings, and prose can no longer satisfy that proof contract by themselves. Normalized advisory results retain `owner_baseline_findings`, merge those records into comparison-visible `agentic_human_review_findings`, preserve safe evidence-reference aliases, and keep missing canonical proof repairable. This does not increase repair attempts, synthesize proof from prose, weaken benchmark or xhigh validation, store raw provider responses or credentials, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review staged placeholder repair context is implemented for reusable real-provider dogfood recovery. The Responses adapter now treats role outputs as placeholders when they are explicitly marked, reported as not provider-authored, or summarized with role-output did-not-return/not-available/missing-output language; such outputs no longer count as reported role/round coverage, while ordinary page-content unavailable-data findings remain valid output. Aggregate contract diagnostics and repair retries now carry bounded `placeholder_outputs` metadata with stage id, stage kind, role, round, reason, and planned-role match so the provider can replace the exact role output. This does not increase default repair attempts, synthesize role opinions, accept placeholder output as proof, weaken owner-baseline or benchmark validation, store raw provider responses or request payloads, persist credential values, add target-specific branches, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review Responses adapter contract recovery is implemented for reusable real-page standard/deep/xhigh dogfood. Adapter post-validation now aggregates benchmark, owner-baseline, optional claim, and full or staged effort contract failures before repair; repair context is missing-only and carries filtered required templates plus recommended local evidence-reference ids; staged adapter validation checks the current stage roles/round and final-stage synthesis; staged and loopback adapter failures persist only allowlisted diagnostics in execution records and run receipts; and the adapter CLI exposes request/response size limits in help and startup JSON. This does not increase default repair attempts, synthesize proof from prose, weaken owner-baseline or benchmark validation, treat intermediate stages as final evidence, store raw provider responses or request payloads, persist credential values, add target-specific branches, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review adapter claim candidate filtering is implemented for reusable real-provider dogfood recovery. Provider-authored `review_claims` are treated as optional proof candidates: unsupported, placeholder, unknown-role, equality, and superiority candidates are filtered before TraceCue result normalization, safe filtering diagnostics are recorded in adapter responses and advisory results, and `claim_integrity` keeps any run with rejected candidates out of future claim-numerator evidence. Staged final synthesis may use prior normalized stage roles as claim support, while stage outputs remain non-final evidence. This does not retry providers solely for optional claim candidates, synthesize replacement claims, weaken owner-baseline or benchmark validation, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review staged effort provider execution is implemented for reusable long real-provider dogfood. `agentic review run --execution-mode staged` is accepted for approved `standard`, `deep`, and `xhigh` plans and splits provider work by planned effort roles under the same plan hash, exact transfer flags, provider/model/surface contract, live dogfood gate, and advisory-only boundary. Intermediate stage outputs are stored only as normalized stage metadata for final aggregation and cannot become final proof, calibration evidence, comparison evidence, claim evidence, release approval, or human-equivalence/superiority evidence by themselves. Responses adapter payloads now carry compact stage context with no local paths, raw provider response storage, credential-value recording, tools, or persistent storage. One-shot execution remains the default. This does not automatically rerun providers, lower quality gates, weaken owner-baseline or benchmark validation, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review long dogfood HTTP transport hardening is implemented for reusable owner-contract provider runs. The generic API provider path and the Responses adapter upstream provider path now use a dependency-free repository-local bounded HTTP(S) transport by default, preserving injected test transports while avoiding hidden bundled fetch header-timeout defaults during slow response-header waits. Generic-provider request failures now include only allowlisted cause-name and cause-code diagnostics, and the adapter server keeps request/header/socket timeout policy explicit for long dogfood. This does not change timeout defaults, automatically rerun providers, store credential values or raw provider responses, expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline recovery and adapter timeout diagnostics are implemented for reusable real-page proof-data recovery. Human-baseline comparison now records whether the candidate result was generated with a matching owner-baseline requirement contract; evidence-set comparison records preserve that contract state; claim-standard-gate rerun plans add approval-required provider result targets when owner-baseline comparison cannot be fixed by local comparison regeneration alone; evidence regeneration planning resolves those targets only from matching owner-contract plans or explicit target-registry rows; future advisory results retain the path-free owner-baseline requirement contract; the Responses adapter reports its effective upstream provider timeout and returns only allowlisted upstream failure diagnostics. This does not automatically rerun providers, change default timeout values, store credential values or raw provider responses, expose Agentic Human Review through MCP, authorize human-equivalent or human-superior claims, mutate deterministic findings, or change release gates.

Agentic Human Review provider model-resolution hardening is implemented for reusable live-provider dogfood recovery. The generic API provider now declares runtime model environment metadata, abstract model ids, and a model-resolution policy; `agentic review run` resolves a concrete provider model before API payload construction and fails before fetch with `AGENTIC_REVIEW_PROVIDER_MODEL_UNRESOLVED` when only an abstract planning model is available. Successful runtime-model fallback records non-secret `model_resolution` metadata in payloads, execution records, receipts, and advisory results. The Responses adapter now reports source-aware model-resolution diagnostics and rejects abstract request models before provider fetch. This does not hard-code an upstream model, remove provider-neutral planning placeholders, store credential values or raw provider responses, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review claim-audit forbidden-claim provenance hardening is implemented for reusable owner-claim proof diagnostics. Claim audit now treats evidence-backed absence or coverage statements about configured forbidden claims as non-blocking diagnostics while keeping affirmative or ambiguous forbidden policy text as blocking claim-audit findings, and future derived `review_claims` avoid elevating forbidden-claim compliance findings into proof claims. This preserves strict forbidden-claim policy enforcement without weakening owner-baseline absence checks, claim-standard-gate fail-closed behavior, human-equivalent or human-superior claim blocking, provider execution boundaries, raw provider response storage boundaries, credential boundaries, MCP exclusion, deterministic review separation, or release gates.

Agentic Human Review Responses adapter provider payload compaction is implemented for reusable real-provider dogfood recovery. The adapter now keeps the original inbound TraceCue request for fail-closed post-validation while deriving a compact provider-facing `review_request` view that preserves plan hash, effort, roles, rounds, xhigh strict-output requirements, effective benchmark requirements, compact owner-baseline criteria, `required_benchmark_coverage`, `required_owner_baseline_findings`, `required_owner_baseline_coverage`, and catalog-backed evidence-reference ids. Duplicated long owner-baseline arrays, nested provider-instruction owner contracts, duplicated plan/package text and visual contracts, long owner summaries, and local paths are removed from the provider-facing request; generated request-size failures now include non-secret section byte counts. This does not increase request-size limits, accept prose-only owner evidence, weaken unknown evidence-reference rejection, synthesize proof records, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline effective coverage contract hardening is implemented for reusable real-provider dogfood recovery. Approved owner-baseline required mentions, required dimensions, and forbidden claims are now merged into the effective benchmark coverage contract, exposed to the Responses adapter as compact `required_benchmark_coverage` and `required_owner_baseline_coverage` templates, prioritized in the evidence-reference catalog, included in bounded repair context, and required as exact evidence-backed `benchmark_requirement_coverage` rows after provider normalization. Owner-baseline forbidden claims remain structured absence checks. This does not lower thresholds, synthesize proof from prose, increase repair attempts, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review owner-baseline adapter contract repair hardening is implemented for reusable real-provider dogfood recovery. The Responses adapter now derives compact required owner-baseline finding records from the approved baseline contract and request evidence catalog, includes those records in provider payloads and bounded repair context, preserves recommended evidence-reference ids in repair diagnostics, normalizes provider-authored structured findings from top-level advisory arrays, owner-baseline finding arrays, and planned role findings, and keeps unknown owner labels, unknown evidence references, and free-text-only baseline discussion fail-closed. This does not increase repair attempts, synthesize findings from baseline text, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review evidence regeneration planning is implemented as a read-only recovery planner for failed owner-claim proof data. `agentic review evidence-set regenerate plan --evidence-set <workspace-json> --claim-gate <workspace-json> [--target-registry <workspace-json>] --json` consumes evidence-set and claim-standard-gate artifacts, resolves provider rerun plan paths, plan hashes, and transfer flags only after validating an explicit approved-plan registry row or the generic result -> execution -> plan artifact chain, classifies provider-result repair, claim-audit, calibration, comparison, human-baseline comparison, and downstream summary/readiness/longitudinal/gate targets, and emits dependency-ordered command templates without executing them. Missing or tampered approved-plan data stays unresolved with warnings instead of producing a trusted provider-rerun command. Missing calibration cells are now included in the claim-standard-gate rerun plan. This does not authorize provider execution, automatic reruns, artifact writes, browser launch, MCP exposure, human-equivalent or human-superior claims, deterministic review mutation, or release-gate mutation.

Agentic Human Review proof-claim integrity and minimal rerun planning are implemented as reusable proof-quality diagnostics. Normalized advisory results now expose `claim_integrity`, strict claim-numerator eligibility rejects unsupported, placeholder, equality, or superiority claim text, and the Responses adapter validates or repairs `review_claims` only when they are backed by catalog evidence references or planned role output. Direct-vs-TraceCue and human-baseline comparisons now expose metric-level regressions, critical regressed metrics, missing owner labels, missing target-specific must-not-miss criteria, and evidence-backed forbidden-claim absence gaps. The claim standard gate now carries claim-integrity audit failures into blockers and emits a read-only minimal rerun plan with command templates instead of executing reruns. This does not authorize provider execution, automatic reruns, artifact writes from the gate, human-equivalent or human-superior claims, MCP exposure, raw provider response storage, credential storage, deterministic review mutation, or release-gate mutation.

Agentic Human Review claim-standard-gate enforcement is implemented as a read-only mechanical gate. `agentic review claim standard-gate --evidence-set <workspace-json> [--policy <workspace-json>] --json` now consumes an evidence set, reuses claim-readiness, longitudinal quality, comparison, owner-baseline, xhigh, synthetic/local evidence exclusion, and per-result claim-audit diagnostics, returns `owner_claim_review_ready` only when all conditions pass, and exits non-zero with blockers when the gate fails. Human-equivalent and human-superior claim states remain false. The existing local proof packet under `.browser-debug/target-url-ahr-evidence/2026-06-28T18-46-38-215Z-owner-baseline-contract/claim-review-proof-packet-2026-06-29.md` remains owner-review evidence, not equality or superiority authorization. This does not authorize provider execution, evidence transfer, MCP exposure, raw provider response storage, credential storage, deterministic review mutation, or release-gate mutation.

Agentic Human Review proof-readiness normalization and repair diagnostics are implemented. Evidence-set summaries now unwrap supported TraceCue CLI and public API runtime-result wrappers for calibration, comparison, and human-baseline artifacts while preserving raw advisory-result validation and rejecting run-runtime pointers as result evidence. Direct human-baseline comparison also accepts supported validation wrappers by extracting and revalidating the embedded baseline contract. Claim-readiness now reports separate blocker categories for missing results, mechanical incomplete results, failed calibration, and missing comparisons. The Responses adapter now requires owner label ids for owner-baseline criteria with owner labels, prioritizes owner-baseline and benchmark evidence catalog ids before optional artifact refs, keeps provider instructions and external provider payloads size-bounded by using compact contract views, treats `present=true` plus absence wording in forbidden-claim records as a repairable semantic contradiction, and sends repair attempts only redacted criterion, owner-label, required-field, forbidden-claim semantics, xhigh-condition, and allowed evidence-reference hints. This does not authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, weaken existing validations, or change release gates.

Agentic Human Review owner-baseline contract propagation is implemented for reusable proof-quality dogfood. Approved owner-labeled human baselines can now be supplied to `agentic review propose` and `agentic review plan` through `--human-baseline`; unapproved, synthetic, incomplete, or case-mismatched baselines fail closed; verified baselines become path-free owner-baseline requirement contracts in proposals, plans, plan hashes, provider instruction contracts, review-quality benchmark contracts, result contracts, and generic-provider payloads. The Responses adapter now requires structured findings for every required owner-label obligation and target-specific criterion fallback with owner label ids, linked criterion ids when present, and catalog-backed evidence references, and human-baseline comparison prefers evidence-backed exact owner-label or criterion findings while text-only matches remain insufficient. This does not create owner approvals, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review proof evidence aggregation and adapter benchmark contracts are hardened for repeatable real-provider dogfood. Evidence-set summaries now accept TraceCue CLI runtime-result wrappers for calibration, comparison, and human-baseline artifacts through explicit data-key unwrapping, while preserving raw result artifact validation. The Responses adapter now treats forbidden-claim `present` as claim presence rather than coverage, requires explicit absence confirmation, requires catalog-backed evidence references, rejects unknown provider-supplied evidence-reference ids, and keeps repair retries bounded. This does not create owner-approved baselines, complete the direct-vs-TraceCue matrix, authorize human-equivalent or human-superior claims, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review now enforces `xhigh` as an observable mechanical effort contract instead of relying only on provider prompt wording. Provider capability snapshots, approved plans, API payloads, Responses adapter requests, advisory results, report-quality output, xhigh planning/simulation, evidence-set summaries, human-baseline comparisons, claim-readiness diagnostics, and longitudinal quality rollups now expose native effort binding, strict structured-output requirements, placeholder rejection, required role/round/critique/verification/synthesis coverage, structured benchmark records with local evidence-reference identifiers, repair-readiness metadata, multi-step execution readiness, and strict claim-numerator exclusion when the mechanical contract is incomplete. This does not claim human-equivalence or human-superiority, force provider-internal reasoning beyond public API controls, store raw provider responses or credentials, expose Agentic Human Review through MCP, mutate deterministic findings, or change release gates.

Agentic Human Review proof-readiness hardening is implemented for the real-provider dogfood pipeline. The slice strengthens the loopback Responses adapter output contract for benchmark-enabled requests, preserves structured advisory findings with evidence-reference identifiers, normalizes safe provider aliases without fabricating positive coverage, and makes evidence-set/claim-readiness diagnostics report explicit benchmark-case by effort matrices, case-level `direct-vs-tracecue` gaps, and weak calibration blockers. This work does not create owner-labeled baselines, authorize human-equivalent or human-superior claims, expose Agentic Human Review through MCP, store raw provider responses or credential values, mutate deterministic findings, or change release gates.

Agentic Human Review proof-policy hardening is implemented for the human-baseline dogfood pipeline. Synthetic, deterministic, fixture-only, fake, injected, and local-pipeline markers now prevent owner-labeled baseline verification and are reported separately from real-provider claim-numerator eligibility. Owner-label comparison scoring now requires structured candidate findings with local evidence references; text-only matches remain diagnostic and produce insufficient-evidence warnings. Deterministic fake-provider dogfood now emits structured local-reference findings for pipeline validation only, and evidence-set/claim-readiness summaries report ready comparison coverage by benchmark case plus the missing real-provider dogfood matrix while keeping equality and superiority claim flags false.

Agentic Human Review now has a read-only owner-labeled human baseline lifecycle for the next dogfood phase. `agentic review human-baseline registry|overlay|draft|approval|validate|compare|claim-readiness` supports reusable rubric/templates, target-specific case overlays, AI baseline drafts that cannot become proof, owner approval packets, approval-metadata-backed baseline validation, direct-vs-TraceCue comparison classification, evidence-set comparison readiness, and claim-readiness diagnostics. These commands operate on workspace-confined JSON and existing advisory results without provider calls, artifact writes, MCP exposure, deterministic gate mutation, credential reads, AI-draft proof, or equality/superiority claim authorization. Evidence-set summaries and longitudinal rollups now report owner-labeled baseline counts, ready human-baseline comparison counts, and missing baseline benchmark cases.

Agentic Human Review human-baseline approval now fails closed on missing target-specific must-not-miss criteria. Approval packets only produce `owner_labeled: true` when the draft includes target-specific must-not-miss criteria, every target-specific criterion is linked to a required owner label, and the linked owner label has local evidence references. Human-baseline validation repeats the same check for hand-written inputs, and human-baseline comparison scores target-specific must-not-miss coverage separately before reporting ready-for-owner-review. Generic benchmark criteria alone are no longer enough to create comparison-ready owner evidence.

Agentic Human Review now includes the first quality-gate foundation for moving standard/deep/xhigh real-page dogfood toward repeatable review evidence. Benchmark plans require structured `benchmark_requirement_coverage`, calibration now scores evidence-backed required mentions, required dimensions, and forbidden claims, `risk_and_misleading_content` is an explicit human-review dimension, and read-only diagnostics cover evidence-set validation/summary, batch comparison, evaluator policy warnings, xhigh round planning/simulation, longitudinal quality rollups, claim policy/audit checks, and claim-standard-gate enforcement. These diagnostics do not call providers, read credential values, write artifacts, expose MCP execution, mutate deterministic findings, change release gates, or authorize human-equivalent or human-superior claims.

Agentic Human Review generic API provider timeout is now optionally configurable through `AGENTIC_HUMAN_REVIEW_API_TIMEOUT_MS` for approved live dogfood runs that need more than the 30000 ms default. The timeout value is part of the provider capability contract and plan hash approval path, so changing it requires a fresh plan and cannot bypass exact transfer flags, credential non-disclosure, raw-provider-response non-storage, advisory-only output, no-MCP-execution, or no-release-gate-mutation boundaries.

Agentic Human Review now includes a loopback-only Responses-compatible adapter for manual live provider dogfood. The adapter is started separately with `npm run ahr:responses-adapter`, accepts the existing `generic-api-provider` request contract, keeps the inbound adapter token separate from the upstream provider key, builds no-store/no-tools Responses requests, strips or rejects local path and raw-pixel disclosures, parses provider output text into normalized advisory JSON, and stores no raw provider response or credential values. Existing AHR plan-hash, exact-transfer-flag, explicit `--execute`, advisory-only, no-MCP, no-release-gate-mutation, and deterministic-review-separation boundaries remain unchanged.

The first manual live upstream dogfood pass through the loopback adapter is complete. `agentic review run` completed via `generic-api-provider` and `gpt-5.4-nano` with a real upstream API call, advisory-only output, no gate mutation, no credential-value recording, and no raw provider response storage; `agentic review report-quality` returned `status=ok` with quick-effort warnings only.

Agentic Human Review now includes read-only human-review maturity diagnostics for moving from a single advisory result toward standard/deep/xhigh real-page dogfood, multiple benchmark-case comparison, and longitudinal quality evaluation. `agentic review dogfood readiness` and `agentic review dogfood plan` expose a standard/deep/xhigh maturity plan and benchmark-case matrix without provider calls or artifact writes. `agentic review report-quality` now reports `human_review_maturity` and `longitudinal_quality_evaluation`, including missing efforts, missing benchmark cases, comparison/history requirements, and explicit `human_equivalent_claim_allowed=false` / `human_superior_claim_allowed=false` boundaries.

Agentic Human Review roadmap AHR-25-40 is implemented as the completion-readiness layer on top of AHR-13-24. TraceCue now has Visual Evidence Package v2 and Visible Text Reading contracts, manual-only `agentic review dogfood readiness`, read-only `agentic review dogfood plan`, explicit live dogfood opt-in metadata, transfer approval previews, xhigh round-plan v2 metadata, Quality Evaluator v3, Human Report v3, provider failure diagnostics in execution receipts, an `article-comprehension-risk` benchmark case, and `agentic review compare --comparison-kind direct-vs-tracecue`. Dogfood readiness and planning perform no provider calls, no credential-value reads, no evidence transfer, no browser launches, no artifact writes, and no gate mutation. Real provider execution still goes only through approved `agentic review run` with matching plan hash, package hash validation, provider capability hash, exact transfer flags, and explicit `--execute`.

Agentic Human Review roadmap AHR-13-24 is implemented as the provider dogfood, benchmark calibration, and orchestration-quality layer on top of AHR-01-12. TraceCue now records provider capability contracts and hashes in readiness/plans/payloads/results, rejects provider capability drift before approved runs, plans transferable evidence by class with explicit raw-byte exclusions, records rubric profiles and privacy disclosure audits, hardens provider API endpoint policy against URL credentials, sensitive query credentials, redirects, unsupported HTTP, oversized streaming responses, and unbounded raw response storage, and exposes read-only `agentic review benchmark list/show`, `agentic review calibrate`, and `agentic review compare` commands. Result artifacts now include role instruction coverage, consensus/dissent analysis, review-quality evaluation, calibration metadata, and privacy disclosure audits. Deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, raw provider response storage, and credential storage remain unchanged.

Agentic Human Review roadmap AHR-01-12 is implemented as a strengthening layer on top of Slices 26-42. TraceCue now emits `human_review_schema_version` v2 contracts, explicit human-review dimensions, provider instruction contracts, review-quality benchmark contracts, reader-experience review, mechanical-versus-human comparison, human-review coverage, actionability scoring, and richer non-engineer-readable Markdown reporting. Provider readiness now validates plan artifacts, report-quality validates advisory result contracts and optional execution/result pairing, proposal parsing rejects conflicting content sources, and generic API provider execution rejects non-loopback HTTP endpoints while omitting plan/execution paths and deterministic review paths from transferred payload metadata. Deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, raw provider response storage, and credential storage remain unchanged.

Agentic Human Review roadmap Slice 34-42 is implemented on top of Slice 26-33. TraceCue now supports `agentic review propose`, `agentic review plan --proposal`, `agentic review provider-readiness`, generic API provider execution through the existing approved `agentic review run` path, `agentic review report-quality`, proposal/provider-readiness/report-quality schemas, provider adapter isolation, role execution records, claims, critique/rebuttal/integration records, dogfood metadata, and report-quality metadata. Proposal and readiness commands do not authorize execution or transfer; run still requires matching plan hash, explicit `--execute`, package-hash validation, and exact transfer flags; Agentic Human Review remains advisory-only and excluded from safe/full/admin MCP profiles. This slice does not mutate deterministic review findings, metrics, release gates, existing review artifacts, MCP permissions, browser capture, OS capture, package publication, parent-repository files, or consumer-repository files, and it does not store raw provider responses or credential values.

Slices 6-25 / Phase 79-155 are complete as local bounded implementation surfaces on top of Slice 5 provider MCP execution/status-list hardening, Phase 71-73 operation provider readiness, Phase 69-70 operation admin readiness, Phase 65-68 operation policy/readiness foundation, Phase 61-64 operation contracts, Phase 60 operation registry, and Phase 60.1 operation roadmap boundary contracts. TraceCue now has cleanup plan hardening, capture readiness/no-capture gates, provider-free localization and translation readiness, local release readiness, artifact-root policy and migration readiness, legacy alias audit/removal readiness, constrained shell readiness, and final hardening readiness through CLI/API/MCP-safe reports, schemas, registry/capability metadata, and fail-closed execution boundaries. This implementation does not promote Phase 60-155 into `docs/product/` commitments and does not authorize npm publication, real artifact-root migration, legacy alias removal, constrained shell execution, capture execution, translation execution, remote CI triggering, HTTP `full` or `admin`, socket transport, parent-repository changes, or consumer-repository changes.

Phase 59 local language settings foundation is complete on top of Phase 58 remote repository rename completion. TraceCue now has repository-local dashboard settings, a 14-locale policy contract, separate dashboard display locale and artifact output language roles, read-only `settings show`, `settings language`, and `settings language policy` CLI/API surfaces, safe MCP inspection, language settings schema parity, review/dashboard `language_settings` metadata, and provider-free translation boundaries. This phase is additive and does not change existing review findings, metrics, action plans, quality signals, gates, MCP write/execute permissions, provider execution, artifact roots, identity aliases, parent-repository files, or consumer-repository files.

Phase 58 remote repository rename completion is complete on top of Phase 57 physical checkout rename completion, Phase 56 rename-readiness audit, Phase 55 multi-agent visual review aggregation hardening, Phase 54 MCP visual review exposure reporting, Phase 53 read-only visual review aggregation, Phase 52 desktop review provider safety bridge, Phase 51 desktop image review body, Phase 50 desktop review provider-preparation planning, Phase 49 existing workspace image capture metadata handoff, Phase 48 screen/window capture planning, Phase 47 MCP execution gate policy, Phase 46 visual review dashboard integration, Phase 45 CLI visual review execution, Phase 44 local visual review result preparation, Phase 43 standalone image review, Phase 42 visual review provider policy planning, and Phase 41 visual evidence metadata. The local checkout directory is now `trace-cue`, GitHub repository URL is now `https://github.com/xxxMasahiro/trace-cue`, and local `origin` points at the canonical remote. Artifact-root migration, legacy-alias removal, npm publication, and marketplace registration remain separate boundaries.

Phase 40 TraceCue identity migration is locally complete for canonical package/bin/plugin/MCP identity. The canonical product identity is TraceCue / `trace-cue`, with `browser-debug` and `browser-debug-mcp` retained as compatibility aliases. MCP tool names and the `.browser-debug/` artifact root remain compatible in this slice.

Phase 39 consumer runtime-readiness guidance is complete for packaged consumer guidance, README/skill routing, product/workflow sync, and no-browser architecture assertions without changing runtime behavior, MCP permissions, target manifests automatically, publication, marketplace state, or identity names.

Phase 5 local MVP runtime implementation is complete after Phase 1 and Phase 2a. Phase 0 scaffold and document sync are complete, local Git is initialized, the initial scaffold commit exists, local CI configuration is present, and product-gate evidence has been recorded locally. Phase 7 local review-platform implementation is complete for deterministic review, target manifests, route/viewport coverage, risk classification, conservative mock metrics, local stdio MCP adapter, schema registry, and package API/file-set readiness. Phase 8 local dogfood/plugin-readiness work is complete for target manifest generation, developer action plans, local heuristic review advisory data, target Markdown reports, plugin metadata, MCP target tools, reusable manifest template, and package file-set readiness without publication. Phase 9 local review-quality work is complete for quality signals, expanded local heuristics, developer handoff, local release decision support, and explicit model-review boundaries. Phase 10 local dogfood route-readiness work is complete for expected-route execution, expected-route coverage artifacts, route-budget skip accounting, and fixture-backed validation without target-specific runtime branches. Phase 11 local page-expectation review work is complete for optional manifest pages, page-specific viewports, deterministic page-state checks, page-level mock metrics, local review artifact indexes, and fixture-backed validation. Phase 12 local rendered-state dogfood hardening is complete for broken-image, lingering-loading, empty-data-container, developer triage report, manifest suggestion, and fixture-backed validation support. Phase 13 local dogfood signal refinement is complete for loading-indicator precision around ready/progress business-state text and Control Center recheck validation without target-specific runtime branches. Phase 14 local content UX advisory work is complete for manifest opt-in source-to-screen advisory checks, schema parity, bounded report output, and fixture-backed invariance validation without changing review findings, metrics, action plans, release readiness, or target-specific runtime branches. Phase 15 local content UX heuristic strengthening is complete for selector-scoped text, attribute, state, risk, required user-question advisory checks, reusable status-dashboard manifest templates, and fixture-backed invariance validation without changing review findings, metrics, action plans, release readiness, or target-specific runtime branches. Phase 16 local content UX handoff output is complete for additive `content_ux_findings`, `content_ux_action_plan`, `content_ux_readiness`, Markdown handoff output, and fixture-backed invariance validation without changing review findings, metrics, existing action plans, release readiness, or target-specific runtime branches. Phase 17 local content UX practical handoff is complete for expanded advisory categories, additive `content_ux_page_handoff`, additive `content_ux_manifest_authoring`, report page/authoring summaries, and fixture-backed invariance validation without changing review findings, metrics, existing action plans, release readiness, or target-specific runtime branches. Phase 18 local content UX review brief/rubric work is complete for additive `content_ux_review_brief`, additive `content_ux_rubric_evaluation`, report brief/rubric summaries, and fixture-backed invariance validation without changing review findings, metrics, existing action plans, release readiness, or target-specific runtime branches. Phase 19 local target manifest validation work is complete for no-browser `target validate`, manifest count output, authoring suggestions, MCP tool coverage, API export, and source-value non-disclosure without launching browsers, mutating manifests, uploading evidence, reusing profiles, or changing existing review outputs. Phase 20 local resource status preflight work is complete for no-browser `resource status`, memory/swap/cgroup/pressure/process-memory output, warnings and recommendations, MCP tool coverage, API export, and read-only local boundaries. Phase 21-24 local resource safety work is complete for review resource guard preflight/rechecks, optional fail-critical stopping, screenshot/trace pressure warnings, daemon idle/max-lifetime lifecycle guards, artifact usage planning, explicit artifact-root-only cleanup receipts, MCP artifact-plan-only wiring, API exports, and boundaries without mutating system cache, changing swap, executing shell commands, using privileged helpers, uploading evidence, reusing profiles, controlling arbitrary processes, or exposing cleanup execution through MCP. Phase 25 local agent advisory handoff work is complete for agent surface listing, bounded evidence package generation, prompt and receipt artifacts, untrusted advisory ingest, advisory report output, schema/API exports, no-browser tests, and boundaries without direct provider API calls, automatic upload, credential storage, MCP agent execution, or deterministic gate changes. Phase 26 local agent request status work is complete for read-only package/result status listing, pending/imported handoff tracking, schema/API exports, no-browser tests, and boundaries without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, review artifact mutation, or deterministic gate changes. Phase 27 local agent request detail work is complete for read-only single-package detail output, selected imported result summaries, dashboard handoff hints, schema/API exports, no-browser tests, and boundaries without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, artifact writes, review artifact mutation, or deterministic gate changes. Phase 28 local agent workflow status work is complete for local workflow manifests, dashboard handoff step state, post-ingest status recomputation, workflow index aggregation, workflow report output, schema/API exports, no-browser tests, and provider-boundary metadata without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, external evidence transfer, review artifact mutation, or deterministic gate changes. Phase 29 agent execution integration is complete for `agent_execution` schema/API parity, dry-run plan receipts, explicit `--execute` plus `--execution` enforcement, provider adapter isolation, deterministic fake provider execution, configured local runner callback support without free-form shell input, env-only generic API provider execution with bounded package/prompt disclosure, dashboard status/list fields, local receipts, advisory-result normalization, no raw provider response storage, no credential value recording, no MCP execution exposure, no review artifact mutation, and unchanged deterministic gates. Phase 30 release hardening is complete for no-publish packed install smoke coverage, CI/release/product-gate wiring, package API import verification, packed CLI/MCP/schema/template/plugin file-set verification, and local release boundaries without npm publication, package rename, license change, external upload, provider SDK dependencies, or marketplace mutation. Phase 31 MCP profile gating is complete for a reusable profile registry, default/full compatibility, explicit `safe`/`full`/`admin` launch profiles, MCP-only workspace-confined `@file` input handling, CLI metadata, API exports, no-browser tests, packed-install smoke coverage, and boundaries without cleanup execution, agent/API execution exposure, HTTP/socket transport, shell tools, external upload, profile reuse, provider credentials, or arbitrary process control. Phase 32 rename readiness is complete for centralized product identity metadata, package/plugin/MCP alignment tests, MCP identity metadata, package API identity exports, identity-derived package dry-run and packed-install smoke paths, and unchanged current names without package rename, repository rename, plugin rename, MCP server rename, CLI command rename, license change, marketplace registration, npm publication, or external evidence transfer. Phase 33 MCP read-only agent status is complete for local stdio MCP tools that inspect agent surfaces, request status/detail, workflow status/index, and execution status/list through the same CLI/core contracts without package generation, ingest, report writing, workflow creation, execution planning, `agent execution run`, cleanup execution, provider/API execution, shell tools, HTTP/socket transport, external upload, credential handling, or gate mutation. Phase 34 safe HTTP MCP foundation is complete for an explicit loopback-only, bearer-token-gated, safe-profile-only HTTP MCP transport, CLI/API transport metadata, packed-install API/file coverage, integration-mode documentation, identity migration runbook, and architecture/security isolation without HTTP `full` or `admin`, socket transport, remote HTTP listeners, cleanup execution, provider/API execution, `agent execution run`, shell tools, external upload, profile reuse, credential storage, or gate mutation. Phase 35 HTTP MCP integration hardening is complete for token-free MCP client configuration output, reusable stdio and safe HTTP setup metadata, and packed-install HTTP handshake smoke coverage without expanding the safe HTTP MCP tool surface. Phase 36 MCP capability policy is complete for read-only `mcp capabilities` CLI/API/MCP output that reports profile, transport, admin policy, and excluded-operation boundaries without exposing write/execute/admin operations. Phase 37 external-repository usage quickstart is complete for packaged consumer usage guidance, README/skill connection guidance, package file-set inclusion, and packed-install coverage without expanding MCP permissions or publishing. Phase 38 local-checkout MCP config dogfood hardening is complete for generated `local_checkout` stdio and safe HTTP launch metadata, external-repository dogfood coverage, README/skill/consumer guide routing, and no-browser/packed-install assertions without changing MCP permissions, starting listeners, writing config files, reading credentials, emitting token values, publishing, or renaming identities.

This file is paired with `docs/workflow/HANDOFF.md`. Keep the TASK_TRACKER and HANDOFF workflow-state pair synchronized whenever task state changes.

## Completed Work

- [x] Added Agentic Human Review source-text effort-matrix quality verification with `agentic review quality source-text`, standard/deep/xhigh result validation, source-understanding completion checks, source-text non-persistence diagnostics, pairwise effort deltas, optional reference-review scoring, schema/API/package coverage, and preserved read-only/no-provider/no-MCP/no-proof/no-gate-mutation boundaries.
- [x] Added the Agentic Human Review claim-review proof packet and non-authoritative product claim-standard draft for owner review, preserving disabled equality/superiority claims and advisory-only/no-provider/no-MCP/no-gate-mutation boundaries.
- [x] Added Agentic Human Review evidence regeneration planning with read-only claim-gate-to-dependency-plan conversion, missing calibration rerun targets, validated approved-plan resolution from explicit registry rows or the generic result -> execution -> plan artifact chain, fail-closed unresolved placeholders for missing or tampered plan/hash/flag inputs, approval-required provider result command templates, downstream summary/readiness/longitudinal/gate command templates, schema/API/package coverage, and preserved no-provider-call/no-artifact-write/no-browser/no-MCP/no-auto-rerun/no-gate-mutation boundaries.
- [x] Added Agentic Human Review proof-claim integrity and minimal rerun planning with fail-closed `review_claims` normalization, adapter repair validation, strict claim-numerator exclusion for unsupported claims, comparison metric regressions, human-baseline evidence-gap diagnostics, claim-standard-gate `rerun_plan` output, schema coverage, and preserved advisory-only/no-provider-call/no-auto-rerun/no-MCP/no-gate-mutation boundaries.
- [x] Added Agentic Human Review proof-readiness normalization and repair diagnostics with supported CLI/API runtime-result wrapper unwrapping for calibration, comparison, and human-baseline artifacts, raw run-wrapper result rejection, claim-readiness blocker classification, owner-baseline owner-label id enforcement, prioritized evidence-reference catalogs, bounded redacted adapter repair hints, no-browser regression coverage, and preserved advisory-only/no-provider-call-by-plan/no-MCP/no-gate-mutation boundaries.
- [x] Added Agentic Human Review owner-baseline contract propagation with `--human-baseline` proposal/plan intake, verified-baseline fail-closed checks, path-free plan-hash-bound requirement contracts, provider payload propagation, Responses adapter structured-finding validation and repair, evidence-backed comparison fallback, no-browser CLI/adapter/comparison coverage, and preserved advisory-only/no-provider-call-by-plan/no-MCP/no-gate-mutation boundaries.
- [x] Added Agentic Human Review `xhigh` mechanical effort enforcement with provider-native effort binding metadata, strict structured-output contracts, Responses adapter validation before normalization, placeholder rejection, required role/round/critique/verification/synthesis coverage, benchmark evidence-reference validation, repair/multi-step readiness metadata, strict claim-numerator exclusion, schema/package coverage, and preserved advisory-only/no-gate-mutation/no-MCP boundaries.
- [x] Complete Agentic Human Review proof-readiness hardening for adapter structured-output contracts, evidence-reference identifiers, safe alias normalization, explicit benchmark-case by effort matrices, case-level direct-vs-TraceCue comparison blockers, weak calibration claim-readiness blockers, and no-browser regression coverage.
- [x] Added the Agentic Human Review quality-gate foundation with structured benchmark requirement coverage, strict calibration scoring, risk/misleading-content dimension coverage, evidence-set validation/summary, batch comparison, evaluator policy warnings, xhigh round planning/simulation, longitudinal quality rollups, claim policy/audit diagnostics, claim-standard-gate enforcement, schema/API/package coverage, and preserved read-only/no-provider-call/no-artifact-write/no-MCP/no-gate-mutation boundaries.
- [x] Added Agentic Human Review owner-labeled human baseline validation/comparison with evidence-set and longitudinal baseline coverage, schema/API/package coverage, and preserved read-only/no-provider-call/no-artifact-write/no-MCP/no-gate-mutation/no-equality-claim boundaries.
- [x] Added the Agentic Human Review Responses adapter with loopback-only HTTP startup, local bearer-token validation, separate upstream provider credential loading, no-store/no-tools Responses request conversion, provider output-text advisory parsing, raw-pixel and local-path safeguards, API exports, package script, architecture coverage, pack-install coverage, product security allowlisting, and documentation sync.
- [x] Completed manual live upstream dogfood through the loopback adapter with `generic-api-provider`, `gpt-5.4-nano`, `quick` effort, real upstream API execution, report-quality validation, adapter shutdown, no remaining port 8787 listener, no raw provider response storage, no credential-value recording, and no deterministic gate mutation.
- [x] Added read-only Agentic Human Review maturity diagnostics for standard/deep/xhigh dogfood planning, benchmark-case matrix coverage, longitudinal quality gaps, explicit equality/superiority claim blocking, schema parity, and CLI coverage without provider calls, artifact writes, MCP exposure, deterministic gate mutation, credential storage, or raw provider response storage.
- [x] Added Agentic Human Review roadmap AHR-13-24 with provider capability contracts/hashes and drift rejection, evidence plans, rubric profiles, privacy disclosure audits, endpoint hardening, bounded streaming provider response handling, role instruction coverage, consensus/dissent analysis, review-quality evaluation, calibration metadata, read-only benchmark/calibrate/compare CLI/API/schema/package coverage, and preserved advisory-only/no-gate-mutation/no-MCP-execution boundaries.
- [x] Added Agentic Human Review roadmap AHR-01-12 strengthening with schema v2 contracts, explicit human-review dimensions, provider instruction and benchmark contracts, reader-experience review, mechanical-versus-human comparison, human-review coverage/actionability scoring, stricter report-quality validation, parser conflict rejection, provider payload minimization, non-loopback HTTP rejection, schema/API/package tests, and preserved advisory-only/no-gate-mutation boundaries.
- [x] Added Agentic Human Review roadmap Slice 34-42 with conversational proposals, proposal-to-plan conversion, provider-readiness diagnostics, generic API provider execution behind plan-hash and exact-transfer-flag gates, role/claim/critique/rebuttal/integration/dogfood/report-quality metadata, schema/API/package/security/MCP-exclusion coverage, and preserved advisory-only/no-gate-mutation boundaries.
- [x] Added Agentic Human Review roadmap Slice 26-33 with `agentic review plan/run/status/list`, effort-mode role orchestration, local packages/plans/receipts/results/reports, plan-hash and exact transfer-flag enforcement, injected/fake runner normalization, generic `agent execution` bypass prevention, schema/API/package/security/MCP-exclusion coverage, and preserved advisory-only/no-gate-mutation boundaries.
- [x] Added Phase 59 TraceCue-local language settings with separate dashboard display locale and artifact output language, 14-locale policy metadata, read-only settings CLI/API/MCP inspection, review/dashboard metadata, schema parity, docs, and provider-free translation boundaries.
- [x] Added Phase 60 read-only operation registry and roadmap risk taxonomy with CLI/API/MCP-safe inspection, registry-derived MCP capability/gate policy data, schema/package coverage, and preserved no-execution boundaries for roadmap groups 1-8.
- [x] Added Phase 60.1 read-only operation roadmap boundary contracts with phase A/B/C status, group/risk filtering, safe MCP inspection, schema/API/package coverage, and preserved draft-only/no-live-execution boundaries for Phase 61-155.
- [x] Added Phase 61-64 read-only operation contracts with risk taxonomy, gate schema, execute-token shape, receipt shape, selected registry operation context, safe MCP inspection, schema/API/package coverage, unsupported execution-option rejection, and preserved no-token/no-receipt/no-harness/no-live-execution boundaries.
- [x] Added Phase 65-68 read-only operation policy/readiness with repository-local admin policy config, CLI plan readiness, disabled harness readiness, safe MCP readiness, schema/API/package coverage, unsupported execution-option rejection, and preserved no-policy-mutation/no-token/no-receipt/no-harness/no-admin-MCP-execution/no-live-execution boundaries.
- [x] Added Phase 69-70 read-only operation admin readiness with MCP admin execute-token flow readiness, MCP admin harness bridge readiness, selected registry operation context, safe MCP inspection, schema/API/package coverage, unsupported execution-option rejection, and preserved no-token/no-token-storage/no-harness/no-admin-execute/no-live-execution boundaries.
- [x] Added Phase 71-73 read-only operation provider readiness with provider MCP plan readiness, bounded disclosure contract readiness, env credential guard names, selected registry operation context, safe MCP inspection, schema/API/package coverage, credential sentinel non-disclosure coverage, unsupported execution-option rejection, and preserved no-provider-call/no-credential-read/no-evidence-transfer/no-MCP-provider-execution boundaries.
- [x] Added Slice 5 / Phase 74-78 provider MCP execution and status/list hardening with admin-only stdio MCP agent execution plan/run, fake/local/API provider coverage, idempotency-key validation, realpath confinement, env-only credentials, bounded disclosure, receipts, safe/full/HTTP exclusion, safe MCP status/list contract metadata, MCP tool purpose tags, capability-id alias normalization, schema/API/package coverage, and preserved no-provider-call/no-local-runner-execute/no-credential-read/no-evidence-transfer boundaries for readiness reports.
- [x] Chosen product repository: current TraceCue checkout root, now using the `trace-cue` checkout directory after the approved physical rename flow.
- [x] Chosen product direction: OSS browser debugging CLI built on Playwright without MCP runtime dependency.
- [x] Documented headless and headed browser roles.
- [x] Documented future Git, GitHub, CI, npm, MVP, and release phases.
- [x] Created initial requirements, specification, implementation plan, tracker, and handoff drafts.
- [x] Installed product-local skills, tools, ops manifests, evidence-detail manifest, `src/`, and `tests/`.
- [x] Ran product-local gate successfully.
- [x] Ran lesson-side scaffold, authority, and workflow-pair checks successfully.
- [x] Confirmed `AGENTS.MD` is the root agent entry and no legacy `AGENT.md` exists.
- [x] Confirmed `ops/PRODUCT_PROFILE.json` keeps `menu_id=free-development` and current display name `TraceCue`.
- [x] Confirmed `ops/PRODUCT_OPERATION_MODE.tsv` keeps `parent_managed` and `managed_by_parent=true`.
- [x] Initialized local Git and renamed the initial branch to `main`.
- [x] Created the first scaffold commit.
- [x] Recorded product-gate evidence under `.git/product-gate-evidence/`.
- [x] Started Phase 2a package/runtime design without GitHub creation, dependency installation, browser launch, CI, or npm publication.
- [x] Selected `browser-debug` as the working CLI binary name.
- [x] Recorded the Node.js 20+, ESM, local-first, ephemeral-context design baseline.
- [x] Defined the first implementation slice as `doctor`, command parsing, deterministic JSON errors, and focused tests.
- [x] Defined the first Playwright slice as one-shot `observe --url <url> --json` with artifact-safe local output.
- [x] Ran `./tools/product-gate` for Phase 2a design verification.
- [x] Added private local `package.json` with the `browser-debug` executable.
- [x] Implemented no-browser CLI parsing, deterministic JSON envelopes, and `doctor`.
- [x] Added focused `node:test` coverage for `doctor`, missing commands, `observe` validation, planned stubs, and session parsing.
- [x] Updated product manifests and aggregate product-gate wiring to include `npm test`.
- [x] Verified `npm test`, `./tools/product-gate`, `git diff --check`, `doctor --json`, and the no-browser `observe` stub.
- [x] Installed the Playwright runtime dependency.
- [x] Implemented Playwright-backed `observe --url <url> --json` with ephemeral Chromium contexts.
- [x] Implemented local artifact handling for observations, screenshots, sessions, reports, and spec exports under ignored `.browser-debug/`.
- [x] Implemented `observe --trace` with local Playwright trace zip artifacts and trace-content warnings.
- [x] Implemented `session start`, `session close`, simple `act`, `report`, and `spec export`.
- [x] Implemented `supervise --url <url> --actions <json-array>` for process-scoped ephemeral browser supervision.
- [x] Implemented `daemon start`, `daemon status`, and `daemon stop` for local background ephemeral browser supervision.
- [x] Added browser smoke tests for local file observation and click actions.
- [x] Strengthened browser smoke coverage for form controls, keyboard input, deterministic scroll, wait actions, screenshots, reports, and spec export.
- [x] Added deterministic headed/devtools launch-mode regression coverage without requiring a GUI display.
- [x] Added `npm run test:pack` local package dry-run verification and aggregate gate wiring.
- [x] Added local release readiness notes in `CHANGELOG.md` and `docs/workflow/RELEASE.md`.
- [x] Added browser smoke coverage for supervised ordered actions in one ephemeral context.
- [x] Added architecture regression coverage for generic runtime boundaries, shared evidence helpers, and local Node CLI packaging.
- [x] Added architecture and browser smoke coverage for local daemon boundaries and start/status/stop.
- [x] Added local GitHub Actions CI configuration and `ops/CI_MANIFEST.tsv`.
- [x] Added `tools/check_product_ci.sh` and wired it into product-local aggregate checks.
- [x] Added `npm run release:check` for local release-readiness verification without publishing.
- [x] Closed local JSON schema-versioning and artifact-retention decisions with `doctor` metadata, product docs, and deterministic tests.
- [x] Created the public GitHub repository and later renamed it to `https://github.com/xxxMasahiro/trace-cue`.
- [x] Fast-forwarded local `main` to the local MVP runtime commit and pushed `main` to `origin/main`.
- [x] Confirmed GitHub Actions `main` CI passed on push for Node 20, Node 22, and browser smoke jobs.
- [x] Updated CI action versions to `actions/checkout@v5` and `actions/setup-node@v5` after the first remote run reported Node 20 action-runtime deprecation annotations.
- [x] Verified the running Dashboard Control Center at `http://127.0.0.1:5173/` with screenshot and trace capture.
- [x] Confirmed `http://127.0.0.1:5174/` was not listening during verification.
- [x] Re-ran product-local `./tools/product-gate`, `npm test`, `npm run test:browser`, and `git diff --check`.
- [x] Re-ran lesson-side product scaffold, product repository authority, and workflow-pair checks successfully.
- [x] Recorded the pre-implementation review-platform direction in developer memory after xhigh sub-agent review.
- [x] Defined the Phase 7 implementation plan for review MVP, target manifests, site review, route discovery, viewport matrices, risk-gated actions, mock comparison, local MCP adapter, optional model review, and public API readiness.
- [x] Implemented machine-readable schemas and `schema list` / `schema get`.
- [x] Implemented `browser-debug review --url <url> --json` with local observation, layout, screenshot, review, report, and mock-metric artifacts.
- [x] Implemented `browser-debug review --target <manifest> --json` with target manifest loading, route discovery, viewport execution, and coverage artifacts.
- [x] Implemented action candidate risk classification for navigation, state-revealing, input-required, mutating, destructive, and external actions.
- [x] Implemented shell-safe structured input for `--input -`, `--target @file`, `--actions @file`, and `--action @file`.
- [x] Implemented conservative local mock metrics under `.browser-debug/diffs/`.
- [x] Implemented local stdio MCP adapter entrypoints `trace-cue-mcp` and legacy `browser-debug-mcp` and `browser-debug mcp serve` metadata.
- [x] Added public local package API exports and schema package files while keeping the package private and unreleased.
- [x] Strengthened product structure, security, architecture, CLI, and browser smoke tests for review/MCP boundaries.
- [x] Synchronized requirements, specification, implementation plan, security, verification, task tracker, handoff, manifests, README, and changelog with the Phase 7 implementation.
- [x] Implemented `target init --url <url> --json` to generate local target manifest artifacts.
- [x] Added developer-facing finding enrichment with priority, impact, recommendations, fix candidates, and implementation notes.
- [x] Added `action_plan` and `review_advisory` to review results.
- [x] Added target review Markdown reports through `review --target <manifest> --report --json`.
- [x] Added MCP tools for target manifest initialization and target review.
- [x] Added local Codex plugin metadata, local MCP configuration, and plugin-facing review skill.
- [x] Added reusable `templates/review-target-manifest.json`.
- [x] Included plugin metadata, plugin skill, and target template in the local package dry-run file set without publishing.
- [x] Added `quality_signals` to single-URL and target review output.
- [x] Added generic quality signals for visual hierarchy, responsive layout, interaction affordance, accessibility structure, evidence completeness, developer handoff, release readiness, and model-review boundary state.
- [x] Expanded local review evidence and findings for headings, landmarks, image alt text, contrast, visible overlaps, and mobile touch-target sizing.
- [x] Added Quality Signals sections to Markdown review reports.
- [x] Strengthened browser smoke coverage for quality signals, alt text, contrast, overlap, target route coverage summaries, local release readiness, and disabled model-review boundaries.
- [x] Target review now enqueues manifest `expectedRoutes` as explicit review targets.
- [x] Coverage artifacts now include `coverage.routes.expected`.
- [x] Target review records `route_budget_exceeded` skipped routes when `budgets.maxRoutes` prevents full coverage.
- [x] Target quality signals report expected manifest route counts and route-budget-exceeded counts.
- [x] Browser smoke coverage verifies unlinked expected routes and route-budget skip accounting.
- [x] Target manifests support optional `pages` entries for named page expectations, page-specific viewports, page-specific mock paths, and priority.
- [x] Target review checks manifest page expected visible text and selectors against local browser evidence.
- [x] Target coverage now includes page expectation expected, checked, failed, and skipped records.
- [x] Target quality signals now include `page_expectations` counts for checked pages, failed pages, skipped pages, and missing expectations.
- [x] Single-URL and target reviews write local `review_artifact_index` artifacts with evidence classes, rerun guidance, and local safety boundaries.
- [x] Browser smoke coverage verifies page expectation checks, page-specific mock metrics, artifact indexes, and report output.
- [x] Layout evidence records image load state, visible loading indicators, and empty table/list/grid containers.
- [x] Review findings flag broken visible images, lingering loading indicators after the review wait, and empty data containers without visible empty-state messaging.
- [x] Single-URL and target quality signals include rendered-state summaries.
- [x] Markdown reports include a Developer Triage section and rendered-state quality signal status.
- [x] Target review output includes manifest suggestions for missing page expectations, unpinned expected routes, route-budget exhaustion, failed page checks, and rendered-state gaps.
- [x] Browser smoke coverage verifies rendered-state findings, evidence summaries, developer triage reports, and manifest suggestions.
- [x] Refined loading indicator evidence so normal ready/progress business-state text does not produce lingering loading UI findings without explicit loading semantics.
- [x] Browser smoke coverage verifies ready/progress business-state text is not reported as a loading indicator.
- [x] Added target manifest `sourceData`, `localContentUxAdvisory`, and page `expectations.dataBindings` support for opt-in content UX advisory.
- [x] Added pure `src/content-ux-advisory.js` advisory logic without Playwright, filesystem access, artifact reads, external transfer, or target-specific literals.
- [x] Added target review `local_content_ux_advisory` and `quality_signals.content_ux` output only when manifest opt-in is enabled.
- [x] Preserved existing review findings, `metrics.finding_count`, `action_plan`, and `quality_signals.release_readiness` when content UX advisory is enabled.
- [x] Synchronized schema registry and schema files for review and target manifest fields, with no-browser parity coverage.
- [x] Added Markdown report output for Content UX Advisory without copying source values or full page text.
- [x] Updated generated target manifests and reusable target manifest template with disabled advisory scaffolding.
- [x] Added no-browser, architecture, and browser smoke coverage for content UX advisory opt-in behavior, non-disclosure, and local-first boundaries.
- [x] Added bounded element evidence summaries for selector-scoped content UX advisory checks.
- [x] Added selector-scoped `text`, `attribute`, `data-state`, and `data-risk` data binding evaluation.
- [x] Added `localContentUxAdvisory.requiredUserQuestions` and page `expectations.userQuestions` advisory checks for information architecture and user journey review.
- [x] Added `templates/status-dashboard-content-ux-target-manifest.json` as a reusable disabled-by-default status-dashboard manifest example.
- [x] Added additive `content_ux_findings` output that stays separate from review `findings`.
- [x] Added additive `content_ux_action_plan` output that stays separate from the existing `action_plan`.
- [x] Added additive `content_ux_readiness` output that stays separate from `quality_signals.release_readiness`.
- [x] Added bounded Content UX Developer Handoff Markdown report output.
- [x] Added no-browser and browser smoke coverage for dedicated content UX handoff output, source-value non-disclosure, and unchanged legacy review fields.
- [x] Added expanded content UX advisory categories for status clarity, action clarity, navigation clarity, information architecture, source alignment, content contracts, coverage contracts, and review scope.
- [x] Added additive `content_ux_page_handoff` output for page-level content UX triage.
- [x] Added additive `content_ux_manifest_authoring` output for manifest-only authoring suggestions.
- [x] Strengthened generated and reusable target manifest scaffolding with the expanded content UX check vocabulary.
- [x] Added no-browser and browser smoke coverage for page handoff, manifest authoring suggestions, report output, and unchanged legacy review fields.
- [x] Confirmed arbitrary source-data file and URL loaders remain unimplemented and approval-bound.
- [x] Added optional `localContentUxAdvisory.reviewBrief`, `localContentUxAdvisory.rubric`, and page `role` manifest fields.
- [x] Added additive `content_ux_review_brief` output for audience, page role, and decision-need triage.
- [x] Added additive `content_ux_rubric_evaluation` output for manifest-declared content UX criteria.
- [x] Added bounded Content UX Review Brief Markdown report output.
- [x] Added no-browser and browser smoke coverage for review brief, rubric evaluation, source-value non-disclosure, disabled-output absence, and unchanged legacy review fields.
- [x] Moved packaged sample vocabulary to a domain-neutral status-dashboard template while keeping lesson-specific Control Center semantics in target-owned manifests.
- [x] Added preferred `status_clarity` and `action_clarity` content UX categories while preserving legacy rubric category aliases for existing manifests.
- [x] Added `target validate --target <manifest> --json` and `target validate --input - --json` for no-browser target manifest validation.
- [x] Added local validation output for normalized manifest counts, authoring suggestions, review next commands, and explicit local-first boundaries.
- [x] Added MCP `browser_debug_target_validate` and local API `runTargetValidate` without adding HTTP/socket transport, shell tools, cleanup execution tools, external upload, or profile reuse.
- [x] Added no-browser coverage for successful validation, invalid manifest errors, source-value non-disclosure, and MCP validation wiring.
- [x] Added `resource status --json` for no-browser local memory, swap, cgroup, pressure, and process memory preflight.
- [x] Added MCP `browser_debug_resource_status` and local API exports for resource status collection and parsing.
- [x] Added no-browser and architecture coverage for deterministic resource fixture output, MCP resource status wiring, and read-only local boundaries without browser launch, artifact writes, cache mutation, swap mutation, file deletion, shell execution, profile reuse, external upload, or arbitrary process control.
- [x] Added review `--resource-guard advisory|fail-critical|off` with additive resource guard output, preflight, target route/viewport rechecks, screenshot/trace pressure warnings, and fail-critical no-launch behavior.
- [x] Added daemon `--idle-timeout` and `--max-lifetime` lifecycle guards with metadata and local worker shutdown behavior.
- [x] Added `resource artifacts plan`, `resource artifacts cleanup --dry-run`, and MCP `browser_debug_resource_artifacts_plan` without exposing cleanup execution through MCP.
- [x] Added `resource artifacts cleanup --execute` for selected regular files under the configured artifact root with local cleanup receipts.
- [x] Hardened cleanup planning and CLI cleanup execution with artifact-root realpath confinement, deterministic plan hashes, path/type/size/mtime/content-hash candidate locks, pre-delete lock revalidation, no directory deletion, MCP plan-only argument rejection, and receipt audit fields while keeping MCP cleanup execution unavailable.
- [x] Added no-browser and architecture coverage for resource guard, artifact planning, explicit cleanup receipts, daemon lifecycle options, and local safety boundaries.
- [x] Added local agent advisory surface listing, bounded package generation, prompt and receipt artifacts, advisory ingest, and separate advisory report output.
- [x] Added agent advisory schemas, package API exports, no-browser coverage, and architecture boundaries without provider API calls, automatic upload, credential storage, MCP agent execution, or deterministic gate changes.
- [x] Added local `agent requests list` status output, request-status schema/API export, pending/imported no-browser coverage, and read-only boundaries without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, review artifact mutation, or deterministic gate changes.
- [x] Added local `agent requests show` detail output, request-detail schema/API export, selected-result matching, pending/imported no-browser coverage, and read-only boundaries without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, artifact writes, review artifact mutation, or deterministic gate changes.
- [x] Added local `agent workflow create/status/index/report` output, workflow schema/API exports, dashboard handoff step state, post-ingest status recomputation, index aggregation, Markdown workflow reports, and no-browser coverage without provider API calls, automatic upload, credential storage, MCP agent execution, browser launch, external evidence transfer, review artifact mutation, or deterministic gate changes.
- [x] Synchronized the Phase 29 agent execution integration plan across product and workflow documents without implementing runtime provider calls, credential handling, raw artifact transfer, MCP execution, or review gate changes.
- [x] Completed Phase 29 execution adapters with `src/agent-execution-providers.js`, deterministic fake provider coverage, configured local runner callback support, env-only generic API execution, local run receipts, normalized advisory result output, dashboard status/list fields, and architecture tests that keep provider calls out of review, resource, daemon, cleanup, Playwright, and MCP execution paths.
- [x] Added Phase 30 packed install smoke coverage for the packed tarball layout, packaged CLI entrypoints, package API imports, MCP stdio tool listing, schema/template/plugin file presence, selected workflow security docs, and no-publish release boundaries.
- [x] Wired `npm run test:pack-install` into local release checks, product-gate, CI validation, and the Node GitHub Actions job without changing publication, package name, license, marketplace, or external evidence transfer boundaries.

## Remaining Work

- [x] Phase 40: complete TraceCue identity migration with canonical package/bin/plugin/MCP metadata, legacy alias checks, product docs sync, and local verification.
- [x] Phase 41: add Visual Evidence Core metadata records, schema/API exports, and no-browser/browser/package boundary tests.
- [x] Phase 42: add visual review provider policy planning to `agent execution plan` without provider calls, external transfer, raw pixels, or MCP execution.
- [x] Phase 43: add standalone image review over workspace-confined image files using the existing visual evidence core.
- [x] Phase 44: add local visual review result preparation for future AI provider execution while keeping execution disabled by default.
- [x] Phase 45: add CLI visual review execution after the visual result preparation and disclosure gates remain stable.
- [x] Phase 46: add read-only visual review dashboard integration after CLI execution remains stable.
- [x] Phase 47: add read-only MCP planning and execution-gate design after the read-only dashboard remains stable.
- [x] Phase 48: add read-only screen/window capture planning after MCP execution gates remain stable.
- [x] Phase 49: add existing workspace image capture metadata handoff after capture planning remains stable.
- [x] Phase 50: add desktop review provider-preparation planning after capture metadata handoff remains stable.
- [x] Phase 51: add desktop image review body over capture handoff provenance while preserving image review compatibility.
- [x] Phase 52: add desktop review provider safety bridge with path/hash handoff verification and stdin support.
- [x] Phase 53: add read-only multi-agent visual review aggregation over existing local visual review results.
- [x] Phase 54: add MCP visual review aggregation exclusion and execution-gate reporting without exposing a tool.
- [x] Phase 55: add aggregation hardening, source attribution, malformed artifact warnings, tests, docs, and package smoke coverage.
- [x] Phase 56: add rename-readiness audit, repository identity boundaries, legacy MCP config compatibility, renamed-checkout checks, docs, CI wiring, and package smoke coverage before physical directory rename.
- [x] Phase 57: complete the local physical checkout directory rename to `trace-cue`, update readiness status reporting, synchronize docs, and verify the moved checkout without changing remotes, artifact roots, package identity, or legacy aliases.
- [x] Phase 58: complete the GitHub repository rename to `xxxMasahiro/trace-cue`, update local `origin`, product identity metadata, plugin metadata, docs, tests, and remote rename status while preserving artifact roots, package identity, and legacy aliases.
- [x] Phase 59: add TraceCue-local language settings, separated dashboard/artifact language roles, safe read-only MCP inspection, schema/API/docs/test coverage, and provider-free translation boundaries without changing existing review gates.
- [x] Phase 60: add a shared read-only operation registry and risk taxonomy for roadmap groups 1-8, registry-derived MCP capability/gate reporting, safe MCP inspection, schema/API/docs/test coverage, and no provider/delete/capture/translation/release/shell execution expansion.
- [x] Phase 60.1: add read-only operation roadmap boundary contracts for draft phases 60-155, safe MCP inspection, schema/API/docs/test coverage, unsupported execution-option rejection, and no draft-plan promotion or live execution expansion.
- [x] Phase 61-64: add read-only operation contracts for risk taxonomy, gate schema, execute-token shape, receipt shape, safe MCP inspection, schema/API/docs/test coverage, unsupported execution-option rejection, and no token issuance, receipt writing, harness enablement, or live execution expansion.
- [x] Phase 65-68: add read-only operation policy and readiness reporting for admin policy config, CLI plan readiness, disabled harness readiness, safe MCP readiness, schema/API/docs/test coverage, unsupported execution-option rejection, and no policy mutation, token issuance, receipt writing, harness enablement, admin MCP execution, or live execution expansion.
- [x] Phase 85-95: add capture readiness/status, static platform-only capability probe, privacy policy, capture artifact/receipt schema contracts, safe MCP readiness exposure, stricter capture-plan MCP argument validation, operation registry/capability reporting, and fail-closed `capture run --execute` without live capture, OS capture APIs, native capture dependencies, window/process enumeration, raw pixel reads/writes, artifact writes, or MCP capture execution.
- [x] Phase 96-119: add provider-free UI locale resources, report template resources, fallback/RTL guards, raw-evidence and canonical-enum non-translation policy, translation readiness, deterministic fake translation dry-run, safe MCP readiness/resource inspection, operation registry/capability reporting, schemas/API/docs/test coverage, and fail-closed `translation run --execute` without provider calls, credential reads, artifact writes, live translation execution, or MCP admin translation execution.
- [x] Phase 120-125: add local release-readiness, publication decision/provenance/2FA boundaries, local package candidate and no-publish reporting, schema/API/MCP-safe/registry/capability coverage, and fail-closed publication boundaries without npm publish, registry upload, package rename, license change, credential reads, marketplace mutation, remote CI, or external evidence transfer.
- [x] Phase 126-133: add artifact-root policy, configurable canonical/future root metadata, dual-root compatibility, migration planning/readiness, fixture-confined migration boundary coverage, schema/API/MCP-safe/registry/capability coverage, and local verification without real artifact-root migration or legacy compatibility removal.
- [x] Phase 134-138: add legacy alias audit, compatibility reporting, migration/removal-readiness metadata, schema/API/MCP-safe/registry/capability coverage, and local verification while preserving existing CLI, MCP, package, plugin, and artifact-root aliases.
- [x] Phase 139: add legacy alias removal readiness and fail-closed `identity aliases remove --execute` gating with schema/API/MCP-safe/registry/capability coverage while keeping actual alias removal approval-bound.
- [x] Phase 140-148: add constrained shell use-case/threat/schema/plan/readiness reporting, fail-closed `shell run --execute`, safe MCP readiness inspection, schema/API/registry/capability coverage, and architecture tests that keep child-process execution, shell interpreters, environment reads, credential reads, file mutation, network access, and MCP shell execution unavailable.
- [x] Phase 149-155: add final hardening readiness with cross-feature regression matrix metadata, local gate plan metadata, schema/API/MCP-safe/registry/capability coverage, and no-execution final report boundaries for browser smoke, MCP smoke, remote CI, publication, Git mutation, provider calls, shell execution, artifact migration, alias removal, and product-doc promotion.
- [x] Phase 29a: add `agent_execution` schema and document/security/schema parity without changing existing workflow or review schemas except additive references.
- [x] Phase 29b: add CLI parser and package API surfaces for `agent execution plan/run/status/list`.
- [x] Phase 29c: add dedicated execution and provider adapter modules while keeping provider calls out of review, resource, daemon, cleanup, Playwright, and MCP execution paths.
- [x] Phase 29d: implement dry-run execution plans, local receipts, boundary flags, and dashboard next-command hints.
- [x] Phase 29e: implement the provider runner abstraction and deterministic fake provider tests.
- [x] Phase 29f: implement configured local subscription-agent runner support without free-form shell input or SaaS web UI automation.
- [x] Phase 29g: implement one-shot API provider execution with explicit `--execute`, env-only credentials, bounded disclosure, advisory normalization, and no raw response storage.
- [x] Phase 29h: add dashboard execution status/list contract fields while keeping existing workflow status semantics and MCP execution non-exposure.
- [x] Phase 30a: add no-publish packed install smoke coverage for packaged CLI, API, MCP, schemas, templates, plugin metadata, and workflow-security docs.
- [x] Phase 30b: wire packed install smoke into release checks, product-gate, CI validation, and workflow documents.
- [x] Phase 31: add MCP profile gating, safe/full/admin stdio launch profiles, and MCP-only file-input confinement.
- [x] Phase 32: add product identity metadata and rename-readiness checks without renaming current identities.
- [x] Phase 33: expose read-only local agent advisory/status inspection through MCP without write or execution tools.
- [x] Phase 34: add explicit safe-profile loopback HTTP MCP transport, integration docs, identity migration runbook, and transport/package/security tests.
- [x] Phase 35: add token-free MCP client configuration output and packed-install safe HTTP MCP handshake smoke coverage without exposing HTTP `full` or `admin`, socket transport, execution, cleanup, provider/API, shell, or credential-bearing tools.
- [x] Phase 36: add read-only MCP capability policy output for profile, transport, admin, and excluded-operation inspection without enabling write/execute/admin tools.
- [x] Phase 37: add packaged external-repository usage guidance and quickstart coverage without expanding runtime permissions, MCP permissions, publication, marketplace, or rename scope.
- [x] Phase 38: add local-checkout MCP config metadata for unpublished checkout use without expanding MCP permissions, starting listeners, writing config files, reading credentials, emitting token values, publishing, or renaming identities.
- [x] Phase 39: add consumer runtime-readiness guidance for frontend-only dev servers, missing API/backend services, API base configuration, and `needs_attention` interpretation without expanding runtime behavior or adding consumer-specific branches.
- [x] AHR-41-44: add real-provider dogfood opt-in enforcement, visible-text provenance, benchmark-completion readiness, transfer-flag masking for plan/package AHR payloads, and mechanical `xhigh` completion assessment without changing deterministic release gates, MCP exposure, credential storage, or raw provider response storage.

## Future Approval-Bound Work

- [ ] If approved later, integrate model/API review outside the Phase 29 agent execution adapter boundary or send review evidence beyond the bounded package/prompt disclosure policy.
- [ ] If approved later, extend provider execution beyond the Phase 29 dry-run, explicit `--execute`, env-only credential, bounded-disclosure, local-receipt, advisory-only boundary.
- [x] Exposed approved `agent execution plan/run` through stdio MCP admin only for Phase 74-76 provider fake/local/API execution.
- [ ] If approved later, expose additional execution operations through MCP. Cleanup, capture, translation, shell, daemon/session, visual review execution, and HTTP full/admin remain excluded.
- [ ] If approved later, add socket MCP transport, remote HTTP MCP listeners, HTTP `full` or `admin` profiles, MCP execution tools, or remote control channels.
- [ ] If approved later, run real headed visual regression checks in an environment with a display.
- [ ] If approved later, choose the public package name and license.
- [ ] If approved later, publish the npm package after release checklist, CI, package name, license, and credential handling are complete.
- [ ] If approved later, register or install the Codex plugin in a marketplace.
- [ ] If approved later, design any host cleanup, system cache mutation, swap configuration, cleanup outside the configured artifact root, privileged helper execution, MCP cleanup execution, or arbitrary process-control workflow with separate security documentation and tests.

## Next Step

Slices 0-25 / Phase 60-155, persistent browser session Slices 0-8, Agentic Human Review AHR-01-44, the Responses adapter implementation, adapter contract recovery, the first manual live upstream dogfood pass, the local maturity/longitudinal-quality diagnostics, owner-labeled human baseline comparison support, and proof-policy hardening are complete locally as bounded readiness, schema, dry-run, compatibility, fail-closed, or advisory-only surfaces. The next practical AHR step is to rerun approved standard/deep/xhigh real-page dogfood across multiple benchmark cases using real-provider structured findings with local evidence references and independently owner-approved baselines that do not carry synthetic or fixture-only markers, then regenerate report-quality, calibration, human-baseline comparison, direct/provider/benchmark comparisons, evidence-set summary, claim-readiness, longitudinal quality, and claim-standard-gate outputs as longitudinal owner-reviewed evidence. The `operation roadmap`, `operation contracts`, `operation policy`, `operation admin-readiness`, `operation provider-readiness`, release readiness, artifact-root readiness, alias removal readiness, constrained shell readiness, and final readiness reports do not make Phase 60-155 formal `docs/product/` commitments or live execution permission. Evidence transfer outside the Phase 29 bounded disclosure policy, translation/provider execution beyond approved agent execution, arbitrary source-data file or URL loaders, socket MCP transport, remote HTTP MCP listeners, HTTP `full` or `admin` MCP profiles, authentication automation, external daemon control channels, external upload, existing-browser-profile reuse, persistent credential storage, public package naming, license changes, plugin marketplace registration, npm publication, real artifact-root migration, legacy alias removal, cleanup outside the configured artifact root, automatic cleanup, host memory-cache mutation, swap configuration, privileged helper execution, MCP cleanup execution, shell tools, persistent session control outside stdio `admin`, credential-bearing MCP workflows beyond env-only agent execution, arbitrary process control, parent-repository changes, consumer-repository changes, or TraceCue runtime branches for consumer-specific backend/API startup state remain separate approval-bound work.

## Stop Conditions

- Runtime Playwright implementation is requested before Phase 0 checks pass.
- GitHub public repository creation is requested without explicit approval.
- npm publish is requested before CI and release planning exist.
- Any secret, cookie, storage state, or credential-like data appears in repository files.
- A design change would require existing browser profile reuse, credential storage, OAuth, webhooks, external upload, or arbitrary shell execution without a security plan and approval.
- Review-platform runtime code introduces Dashboard Control Center or FrameCue Control Center-specific branches instead of generic target manifests.
- MCP adapter work introduces socket listeners, remote HTTP listeners, HTTP `full` or `admin`, arbitrary shell execution, cleanup execution tools, external upload, or persistent storage without explicit approval and security documentation.
- Resource safety work mutates system cache, changes swap configuration, deletes outside the configured artifact root, executes shell commands, uses privileged helpers, exposes MCP cleanup execution, or controls arbitrary processes without explicit approval and security documentation.
- Agent execution work changes existing deterministic review outputs, existing `agent_workflow` status meanings, resource guard behavior, artifact cleanup behavior, or release-readiness semantics.
- Agent execution work stores credential values, raw provider responses, raw screenshots, trace contents, raw DOM, console payloads, network payloads, sourceData values, report bodies, cookies, storage state, existing browser profile data, or raw review artifacts.
- Agent execution work bypasses dry-run planning, explicit `--execute`, env-only credential loading, local receipts, bounded disclosure, or advisory-only normalization.
- Agent execution work exposes execution through MCP, accepts free-form shell commands, automates SaaS web UIs, adds OAuth/login automation, persists credentials, or adds HTTP/socket provider control paths.
