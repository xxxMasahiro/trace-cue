---
name: browser-debug-review
description: Legacy compatibility wrapper for the TraceCue local visual review workflow.
---

# Browser Debug Review Compatibility

This legacy skill name remains for existing Codex/plugin setups. Use the canonical TraceCue skill at:

`skills/trace-cue-review/SKILL.md`

The legacy CLI aliases `browser-debug` and `browser-debug-mcp` remain available, but new documentation should prefer `trace-cue` and `trace-cue-mcp`.
